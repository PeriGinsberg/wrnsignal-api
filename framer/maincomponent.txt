// START FILE
import * as React from "react"
import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm"

/**
 * SIGNAL Tabs (Framer Code Component) — FULL REWRITE (DEV)
 *
 * Latest fixes in THIS version:
 * - Left nav cut-off: add outer padding + ensure no clipping (shell overflow visible)
 * - Positioning: on landing (before Run), CTA is BELOW Step Notes
 * - Positioning: the intro summary line above Primary M<div style={sideAccountRow}>atch Drivers renders in WRN_BLUE
 * - Cover Letter: on landing (before Run), CTA is BELOW Step Notes
 * - Cover Letter: remove duplicate "Dear" (strip any leading salutation from body before templating)
 * - Networking: on landing (before Run), CTA is BELOW Step Notes
 *function renderJobFit
 * Previous fixes preserved:
 * - Magic link loop (exchange code and strip from URL)
 * - Robust JobFit bullets on REVIEW
 * - Robust Positioning edit key mapping
 * - Diff highlight is TEAL (not orange)
 * - No user-facing mentions of “API”
 * - Decision-first JobFit layout and accordions
 * - needced new message
 * Avoid em dashes.
 */

/**
 * @framerSupportedLayoutWidth any
 * @framerSupportedLayoutHeight any
 */

// ---------- ENV (PROD) ----------
const PROD_SITE_URL = "https://wrnsignal.workforcereadynow.com"
function getMagicLinkRedirect() {
    return PROD_SITE_URL + "/signal/jobfit"
}
const API_BASE = "https://wrnsignal-api.vercel.app"
const UPGRADE_URL = "https://www.workforcereadynow.com/signal"
const SUPABASE_URL = "https://ejhnokcnahauvrcbcmic.supabase.co"
const SUPABASE_ANON_KEY =
    "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVqaG5va2NuYWhhdXZyY2JjbWljIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjgxMzczNzMsImV4cCI6MjA4MzcxMzM3M30.LzTMZDzDrx4UMWGu9y5qeg4AzwxukEWUu06q7Ts9Wb0"

const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY, {
    auth: {
        persistSession: true,
        autoRefreshToken: true,
        detectSessionInUrl: true,
    },
})

/* ---------- BRAND ---------- */
const WRN_BLUE = "#51ADE5"
const WRN_ORANGE = "#FEB06A"
const ICE = "#FFFFFF"
const WRN_TEAL = "#218C8C"

/* ---------- UI COLORS ---------- */
const BG = "#13294A"
const NAV_BG = "#091629"
const CARD = "#162E50"
const CARD_ALT = "#112240"
const GLASS = "rgba(255,255,255,0.07)"
const BORDER = "rgba(255,255,255,0.12)"
const BORDER_SOFT = "rgba(255,255,255,0.08)"
const TEXT = "rgba(255,255,255,0.92)"
const MUTED = "rgba(255,255,255,0.60)"
const DIM = "rgba(255,255,255,0.35)"
const ERROR = "rgba(255,120,120,0.95)"

type TabKey = "jobfit" | "positioning" | "coverletter" | "networking"
type AccessMode = "full" | "jobfit_only"

function safePreview(s: string, max = 1400) {
    const t = (s || "").trim()
    return t.length > max ? t.slice(0, max) + "\n…(truncated)" : t
}

function looksLikeHtml(text: string) {
    const t = (text || "").trim().toLowerCase()
    return (
        t.startsWith("<!doctype") ||
        t.startsWith("<html") ||
        t.includes("<head") ||
        t.includes("<body")
    )
}

async function postJsonWithDebug(
    url: string,
    payload: any,
    onDebug: (d: any) => void,
    accessToken?: string | null
) {
    const startedAt = Date.now()
    let res: Response

    const headers: Record<string, string> = {
        "Content-Type": "application/json",
    }
    if (accessToken) headers["Authorization"] = `Bearer ${accessToken}`

    try {
        res = await fetch(url, {
            method: "POST",
            headers,
            body: JSON.stringify(payload),
        })
    } catch (err: any) {
        onDebug({
            stage: "fetch_throw",
            url,
            ms: Date.now() - startedAt,
            errorName: err?.name,
            errorMessage: err?.message,
            errorStackPreview: safePreview(err?.stack || "", 900),
        })
        throw new Error(`Request failed. Please try again.`)
    }

    const ms = Date.now() - startedAt
    const contentType = res.headers.get("content-type") || ""
    const raw = await res.text()

    onDebug({
        stage: "fetch_response",
        url,
        ms,
        status: res.status,
        ok: res.ok,
        contentType,
        responsePreview: safePreview(raw),
    })

    if (looksLikeHtml(raw) || contentType.includes("text/html")) {
        throw new Error(`Unexpected response. Please try again.`)
    }

    let data: any = null
    try {
        data = JSON.parse(raw)
    } catch {
        throw new Error(`Unexpected response. Please try again.`)
    }

    if (!res.ok) {
        throw new Error(
            data?.detail || data?.error || `Request failed. Please try again.`
        )
    }

    return data
}

/* ---------- STORAGE KEYS ---------- */
const TRIAL_UNLOCK_KEY = "jobfit_unlocked"
const TRIAL_EMAIL_KEY = "jobfit_email"

function getTrialUnlocked() {
    if (typeof window === "undefined") return false
    return localStorage.getItem(TRIAL_UNLOCK_KEY) === "true"
}

function getTrialEmail() {
    if (typeof window === "undefined") return ""
    return localStorage.getItem(TRIAL_EMAIL_KEY) || ""
}

function openUpgradePage() {
    if (typeof window === "undefined") return
    window.open(UPGRADE_URL, "_blank", "noopener,noreferrer")
}

/* ---------- DIFF HIGHLIGHT (TEAL) ---------- */
function renderDiffTeal(before: string, after: string) {
    const normalize = (w: string) =>
        w
            .toLowerCase()
            .replace(/^[^a-z0-9]+|[^a-z0-9]+$/gi, "")
            .trim()

    const stop = new Set([
        "a",
        "an",
        "the",
        "and",
        "or",
        "but",
        "to",
        "of",
        "in",
        "on",
        "for",
        "with",
        "at",
        "by",
        "from",
        "as",
        "is",
        "are",
        "was",
        "were",
        "be",
        "been",
        "being",
        "this",
        "that",
        "these",
        "those",
        "it",
        "its",
        "you",
        "your",
        "we",
        "our",
        "i",
    ])

    const beforeWords = new Set(
        (before || "")
            .split(/\s+/)
            .map(normalize)
            .filter((w) => w && w.length >= 4 && !stop.has(w))
    )

    const parts = (after || "").split(/(\b[\w'/-]+\b)/g)

    return (
        <>
            {parts.map((p, i) => {
                if (!p || !/\b[\w'/-]+\b/.test(p))
                    return <span key={i}>{p}</span>

                const w = normalize(p)
                const shouldBold =
                    !!w && w.length >= 4 && !stop.has(w) && !beforeWords.has(w)

                return shouldBold ? (
                    <span key={i} style={{ fontWeight: 950, color: WRN_TEAL }}>
                        {p}
                    </span>
                ) : (
                    <span key={i}>{p}</span>
                )
            })}
        </>
    )
}

/* ---------- HEADER ---------- */
function Header() {
    return <div style={headerTop} />
}

/* ---------- JOB CONTEXT HELPERS ---------- */
function extractJobContextFromJobFit(r: any) {
    const x = r?.result ?? r ?? {}

    const title = String(
        x?.job_title ??
            x?.jobTitle ??
            x?.title ??
            x?.role ??
            x?.position ??
            x?.position_title ??
            x?.positionTitle ??
            x?.job ??
            ""
    ).trim()

    const company = String(
        x?.company ??
            x?.company_name ??
            x?.companyName ??
            x?.employer ??
            x?.employer_name ??
            x?.organization ??
            x?.org ??
            x?.client ??
            ""
    ).trim()

    return { title, company }
}

function extractJobContextFromJobText(jobText: string) {
    const text = String(jobText || "")
        .replace(/\r/g, "")
        .trim()
    if (!text) return { title: "", company: "" }

    const lines = text
        .split("\n")
        .map((l) => l.trim())
        .filter(Boolean)

    const firstN = lines.slice(0, 40)

    const clean = (s: string) =>
        String(s || "")
            .replace(/[–—]/g, "-")
            .replace(/\s+/g, " ")
            .trim()

    const looksLikeMarketingFluff = (s: string) => {
        const t = (s || "").toLowerCase()
        const phrases = [
            "best place to work",
            "great place to work",
            "fastest growing",
            "award",
            "top workplace",
            "fortune",
            "inc.",
            "recognition",
            "ranked",
            "named",
        ]
        if (phrases.some((p) => t.includes(p))) return true
        const quoteCount = (s.match(/["“”']/g) || []).length
        if (quoteCount >= 4) return true
        const commaCount = (s.match(/,/g) || []).length
        const words = s.split(/\s+/).filter(Boolean)
        const upperWords = words.filter((w) => /^[A-Z]{3,}$/.test(w)).length
        if (commaCount >= 2 && upperWords >= 3) return true
        return false
    }

    const looksLikeRealRoleTitle = (s: string) => {
        const t = (s || "").toLowerCase()
        const roleWords = [
            "intern",
            "analyst",
            "associate",
            "manager",
            "director",
            "coordinator",
            "specialist",
            "engineer",
            "consultant",
            "developer",
            "designer",
            "strategist",
            "assistant",
            "representative",
            "officer",
            "lead",
            "head",
        ]
        return roleWords.some((w) => t.includes(w))
    }

    const isJunkTitle = (s: string) => {
        const t = (s || "").toLowerCase()
        if (!t) return true
        if (t.length < 4) return true
        if (t.length > 90) return true
        if (looksLikeMarketingFluff(s)) return true
        if (
            t.includes("overview") ||
            t.includes("responsibilities") ||
            t.includes("qualifications") ||
            t.includes("requirements") ||
            t.includes("equal opportunity") ||
            t.includes("accommodation")
        )
            return true
        if (!looksLikeRealRoleTitle(s)) return true
        return false
    }

    const isJunkCompany = (s: string) => {
        const t = s.toLowerCase()
        if (!t) return true
        if (t.length < 2) return true
        if (t.length > 70) return true
        if (
            t.includes("overview") ||
            t.includes("position overview") ||
            t.includes("responsibilities") ||
            t.includes("qualifications") ||
            t.includes("equal opportunity")
        )
            return true
        return false
    }

    let company = ""

    for (const l of firstN.slice(0, 20)) {
        const m = l.match(/^([A-Z][A-Za-z0-9&.'-]{2,40})\s+Overview$/)
        if (m?.[1]) {
            const c = clean(m[1])
            if (!isJunkCompany(c)) company = company || c
        }
    }

    if (!company) {
        for (const l of firstN) {
            const m = l.match(
                /^([A-Z][A-Za-z0-9&.'-]{2,60})\s+is\s+(?:currently\s+)?looking\b/i
            )
            if (m?.[1]) {
                const c = clean(m[1])
                if (!isJunkCompany(c)) {
                    company = c
                    break
                }
            }
        }
    }

    if (!company) {
        for (const l of firstN) {
            const m = l.match(/^(Company|Employer)\s*:\s*(.+)$/i)
            if (m?.[2]) {
                const c = clean(m[2])
                if (!isJunkCompany(c)) {
                    company = c
                    break
                }
            }
        }
    }

    let title = ""

    for (const l of firstN) {
        const m = l.match(/^(Position Title|Job Title|Role)\s*:\s*(.+)$/i)
        if (m?.[2]) {
            const t = clean(m[2])
            if (!isJunkTitle(t)) {
                title = t
                break
            }
        }
    }

    if (!title) {
        for (const l of firstN) {
            const m = l.match(/\bas\s+(?:an?|the)\s+(.{4,80})$/i)
            if (m?.[1]) {
                const t = clean(m[1])
                if (!isJunkTitle(t)) {
                    title = t
                    break
                }
            }
        }
    }

    title = clean(title)
    company = clean(company)

    if (isJunkTitle(title)) title = ""
    if (isJunkCompany(company)) company = ""

    return { title, company }
}

/* ---------- TEXT HELPERS (SAFE) ---------- */
function pickString(obj: any, keys: string[]) {
    for (const k of keys) {
        const v = obj?.[k]
        if (typeof v === "string" && v.trim()) return v.trim()
    }
    return ""
}

function toSafeLine(x: any) {
    if (x === null || x === undefined) return ""
    if (typeof x === "string") return x.trim()
    if (typeof x === "number") return String(x)
    try {
        return JSON.stringify(x)
    } catch {
        return String(x)
    }
}

function pickFirstArray(...vals: any[]) {
    for (const v of vals) {
        if (Array.isArray(v) && v.length) return v
    }
    return []
}

/* ---------- COVER LETTER HELPERS ---------- */
function normalizeLetterBody(raw: string) {
    const s = String(raw || "")
        .replace(/\r/g, "")
        .trim()
    if (!s) return ""

    const lines = s.split("\n").map((l) => l.replace(/[ \t]+$/g, ""))
    const out: string[] = []
    let lastWasBlank = false

    for (const line of lines) {
        const isBlank = !line.trim()
        if (isBlank) {
            if (!lastWasBlank) out.push("")
            lastWasBlank = true
        } else {
            out.push(line.trimStart())
            lastWasBlank = false
        }
    }

    while (out.length && !out[0].trim()) out.shift()
    while (out.length && !out[out.length - 1].trim()) out.pop()

    return out.join("\n")
}

// Strip any leading salutation like "Dear X," so we never print two "Dear" lines.
function stripLeadingSalutation(raw: string) {
    const s = normalizeLetterBody(raw)
    if (!s) return ""

    const lines = s.split("\n")
    const firstNonBlankIndex = lines.findIndex((l) => l.trim().length > 0)
    if (firstNonBlankIndex === -1) return s

    const first = lines[firstNonBlankIndex].trim()
    const isDear = /^dear\s+.+/i.test(first)

    if (!isDear) return s

    // Remove that line and any immediate blank line after it
    const remaining = lines.slice(firstNonBlankIndex + 1)
    while (remaining.length && !remaining[0].trim()) remaining.shift()

    return normalizeLetterBody(remaining.join("\n"))
}

function stripExistingSignatureBlock(raw: string) {
    const s = normalizeLetterBody(raw)
    if (!s) return ""

    const signoffs = [
        "sincerely,",
        "sincerely",
        "best,",
        "best regards,",
        "best regards",
        "regards,",
        "regards",
        "thank you,",
        "thank you",
        "respectfully,",
        "respectfully",
    ]

    const lines = s.split("\n")
    const start = Math.max(0, lines.length - 10)

    for (let i = lines.length - 1; i >= start; i--) {
        const t = lines[i].trim().toLowerCase()
        if (signoffs.includes(t)) {
            return normalizeLetterBody(lines.slice(0, i).join("\n"))
        }
        for (const so of signoffs) {
            if (t.startsWith(so)) {
                return normalizeLetterBody(lines.slice(0, i).join("\n"))
            }
        }
    }

    return s
}

function buildFinalLetter(args: {
    bodyRaw: string
    jobTitle: string
    companyName: string
    fullName: string
    phone: string
    email: string
    hiringManager?: string
}) {
    const today = new Date().toLocaleDateString("en-US", {
        year: "numeric",
        month: "long",
        day: "numeric",
    })

    // Fix: remove salutation + signature inside the model body
    const bodyNoDear = stripLeadingSalutation(args.bodyRaw)
    const body = stripExistingSignatureBlock(bodyNoDear)

    // If you are temporarily not using company/title, blank these out later.
    const hiringManager = args.hiringManager?.trim() || "[Hiring Manager]"
    const company = args.companyName || "[Company Name]"
    const title = args.jobTitle || "[Position Title]"

    // IMPORTANT: only use args here. Never reference React state variables inside this helper.
    const name = String(args.fullName || "").trim() || "[Your Name]"
    const phone = String(args.phone || "").trim() || "[Phone Number]"
    const email = String(args.email || "").trim() || "[Email Address]"

    // If your model body still includes an opener like "I am applying for X at Y", fine.
    // This template only guarantees the header + date + greeting + cleaned body + signature.

    return `${name}
${phone}
${email}

${today}

${hiringManager}
${company}

Dear ${hiringManager},

${body}

Sincerely,
${name}
`
}

/* ---------- NETWORKING EXTRACTORS ---------- */
function extractNetworkingTarget(a: any) {
    if (!a) return ""

    const roles = a?.target_roles
    if (Array.isArray(roles) && roles.length) {
        const first =
            roles.find((x: any) => typeof x === "string" && x.trim()) ||
            roles.find(
                (x: any) => typeof x?.title === "string" && x.title.trim()
            )
        if (typeof first === "string") return first.trim()
        if (first?.title) return String(first.title).trim()
    } else if (typeof roles === "string" && roles.trim()) {
        return roles.trim()
    }

    const person = pickString(a, ["person_to_pick", "personToPick"])
    if (person) return person

    const terms = a?.search_terms
    if (Array.isArray(terms) && terms.length) {
        const firstTerm = terms.find(
            (x: any) => typeof x === "string" && x.trim()
        )
        if (firstTerm) return firstTerm.trim()
    } else if (typeof terms === "string" && terms.trim()) {
        return terms.trim()
    }

    return pickString(a, [
        "linkedin_title",
        "linkedinTitle",
        "search_title",
        "searchTitle",
        "target_title",
        "targetTitle",
        "role",
        "title",
        "persona",
        "label",
    ])
}

function extractNetworkingMessageParts(action: any) {
    const msg =
        action?.message ??
        action?.outreach_message ??
        action?.outreachMessage ??
        action?.copy ??
        action?.draft ??
        ""

    if (typeof msg === "string") {
        return { initial: msg.trim(), followUp: "" }
    }

    if (msg && typeof msg === "object") {
        const initial =
            pickString(msg, ["initial", "first", "message", "text", "body"]) ||
            pickString(msg?.content, ["text", "body", "content"]) ||
            pickString(msg?.message, ["text", "body", "content"]) ||
            ""

        const followUp =
            pickString(msg, ["follow_up", "followUp", "followup", "second"]) ||
            pickString(msg?.follow_up, ["text", "body", "content"]) ||
            pickString(msg?.followUp, ["text", "body", "content"]) ||
            ""

        return { initial, followUp }
    }

    return { initial: "", followUp: "" }
}

/* ---------- STEP LANDING ---------- */
function StepLanding({
    title,
    items,
    activeStep = 0,
}: {
    title: string
    items: Array<{ head: string; desc: string }>
    activeStep?: number
}) {
    const TAB_KEYS = ["jobfit", "positioning", "coverletter", "networking"]
    const STEP_META = [
        { tag: "Decision", head: "JobFit" },
        { tag: "Resume", head: "Positioning" },
        { tag: "Letter", head: "Cover Letter" },
        { tag: "Outreach", head: "Networking" },
    ]
    return (
        <div
            style={{
                background: "#0d1b2e",
                borderRadius: 14,
                padding: "28px 32px 32px",
                marginBottom: 10,
            }}
        >
            {/* Track eyebrow */}
            <div
                style={{
                    fontSize: 10,
                    letterSpacing: "0.14em",
                    textTransform: "uppercase" as const,
                    color: "#3d5878",
                    fontWeight: 600,
                    marginBottom: 24,
                }}
            >
                How Signal works
            </div>

            {/* Track bar */}
            <div
                style={{
                    display: "flex",
                    alignItems: "center",
                    marginBottom: 24,
                }}
            >
                {[1, 2, 3, 4].map((n, i) => {
                    const isActive = i === activeStep
                    const isDone = i < activeStep
                    return (
                        <React.Fragment key={n}>
                            <div
                                style={{
                                    width: 44,
                                    height: 44,
                                    borderRadius: "50%",
                                    display: "flex",
                                    alignItems: "center",
                                    justifyContent: "center",
                                    fontSize: 14,
                                    fontWeight: 700,
                                    flexShrink: 0,
                                    border: isActive
                                        ? "1.5px solid #f97316"
                                        : isDone
                                          ? "1.5px solid rgba(249,115,22,0.5)"
                                          : "1.5px solid rgba(255,255,255,0.08)",
                                    background: isActive
                                        ? "#f97316"
                                        : isDone
                                          ? "rgba(249,115,22,0.15)"
                                          : "rgba(255,255,255,0.03)",
                                    color: isActive
                                        ? "#fff"
                                        : isDone
                                          ? "#f97316"
                                          : "#3d5878",
                                }}
                            >
                                {n}
                            </div>
                            {i < 3 && (
                                <div
                                    style={{
                                        flex: 1,
                                        height: 1,
                                        background:
                                            i < activeStep
                                                ? "rgba(249,115,22,0.45)"
                                                : "rgba(255,255,255,0.07)",
                                    }}
                                />
                            )}
                        </React.Fragment>
                    )
                })}
            </div>

            {/* Step cards — top row shows workflow context, bottom shows current step detail */}
            <div
                style={{
                    display: "grid",
                    gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
                    gap: 10,
                    marginBottom: 20,
                }}
            >
                {STEP_META.map((meta, i) => {
                    const isActive = i === activeStep
                    const isDone = i < activeStep
                    return (
                        <div
                            key={i}
                            style={{
                                padding: "16px 14px 14px",
                                borderRadius: 8,
                                borderTop: isActive
                                    ? "2px solid #f97316"
                                    : isDone
                                      ? "2px solid rgba(249,115,22,0.3)"
                                      : "2px solid rgba(255,255,255,0.06)",
                                background: isActive
                                    ? "rgba(249,115,22,0.1)"
                                    : isDone
                                      ? "rgba(249,115,22,0.04)"
                                      : "rgba(255,255,255,0.025)",
                            }}
                        >
                            <div
                                style={{
                                    fontSize: 9,
                                    letterSpacing: "0.1em",
                                    textTransform: "uppercase" as const,
                                    fontWeight: 600,
                                    color:
                                        isActive || isDone
                                            ? "#f97316"
                                            : "#3d5878",
                                    marginBottom: 8,
                                }}
                            >
                                {meta.tag}
                            </div>
                            <div
                                style={{
                                    fontSize: 14,
                                    fontWeight: 600,
                                    color: isActive
                                        ? "#f0f4ff"
                                        : isDone
                                          ? "#8fa5be"
                                          : "#4a5f78",
                                    marginBottom: isActive ? 6 : 0,
                                }}
                            >
                                {meta.head}
                            </div>
                            {isActive && (
                                <div
                                    style={{
                                        fontSize: 11,
                                        color: "#7a9ab8",
                                        lineHeight: "1.65",
                                    }}
                                >
                                    {items[0]?.desc ?? ""}
                                </div>
                            )}
                        </div>
                    )
                })}
            </div>

            {/* Current step detail items */}
            <div
                style={{
                    borderTop: "1px solid rgba(255,255,255,0.06)",
                    paddingTop: 20,
                    display: "grid",
                    gridTemplateColumns: "repeat(4, minmax(0, 1fr))",
                    gap: 10,
                }}
            >
                {items.slice(0, 4).map((it, idx) => (
                    <div
                        key={idx}
                        style={{
                            padding: "14px 12px",
                            borderRadius: 8,
                            background: "rgba(255,255,255,0.025)",
                            border: "1px solid rgba(255,255,255,0.06)",
                        }}
                    >
                        <div
                            style={{
                                width: 22,
                                height: 22,
                                borderRadius: "50%",
                                background: "rgba(249,115,22,0.2)",
                                border: "1px solid rgba(249,115,22,0.4)",
                                display: "flex",
                                alignItems: "center",
                                justifyContent: "center",
                                fontSize: 11,
                                fontWeight: 700,
                                color: "#f97316",
                                marginBottom: 10,
                            }}
                        >
                            {idx + 1}
                        </div>
                        <div
                            style={{
                                fontSize: 12,
                                fontWeight: 700,
                                color: "#c8daf0",
                                marginBottom: 5,
                            }}
                        >
                            {it.head}
                        </div>
                        <div
                            style={{
                                fontSize: 11,
                                color: "#4a6680",
                                lineHeight: "1.6",
                            }}
                        >
                            {it.desc}
                        </div>
                    </div>
                ))}
            </div>
        </div>
    )
}

export default function WRNSignalTabs(props: { style?: React.CSSProperties }) {
    console.log("WRNSignalTabs loaded", {
        marker: "RUN-CHECK-0317-B",
        path:
            typeof window !== "undefined"
                ? window.location.pathname
                : "no-window",
    })

    const [tab, setTab] = React.useState<TabKey>("jobfit")

    // Auth
    const [email, setEmail] = React.useState("")
    const [password, setPassword] = React.useState("")
    const [authStatus, setAuthStatus] = React.useState<
        "checking" | "logged_out" | "email_sent" | "logged_in"
    >("checking")
    const [magicLinkSent, setMagicLinkSent] = React.useState(false)
    const [accessToken, setAccessToken] = React.useState<string | null>(null)
    const [authedEmail, setAuthedEmail] = React.useState<string | null>(null)

    const [copiedKey, setCopiedKey] = React.useState<string | null>(null)
    const copyText = (txt: string, key?: string) => {
        if (typeof navigator === "undefined" || !navigator.clipboard) return
        navigator.clipboard.writeText(String(txt || ""))
        const k = key ?? txt.slice(0, 40)
        setCopiedKey(k)
        setTimeout(() => setCopiedKey(null), 2000)
    }
    const wasCopied = (key: string) => copiedKey === key

    // Access mode
    const [accessMode, setAccessMode] = React.useState<AccessMode>("full")

    // Inputs
    const [job, setJob] = React.useState("")
    // Optional user-supplied company name and job title. When set, these
    // override whatever the deterministic extractor pulls from the JD body
    // so the top-of-screen display (and signal_applications row, and cover
    // letter strategy, etc.) show the user's authoritative value instead
    // of extraction garbage like "UBS, we know that it's our people...".
    const [manualCompanyName, setManualCompanyName] = React.useState("")
    const [manualJobTitle, setManualJobTitle] = React.useState("")
    const [manualPasteStep, setManualPasteStep] = React.useState<"paste" | "confirm">("paste")
    const [manualPasteText, setManualPasteText] = React.useState("")
    const [manualExtracting, setManualExtracting] = React.useState(false)
    const [jobInputOpen, setJobInputOpen] = React.useState(true)
    const [jobUrl, setJobUrl] = React.useState("")
    const [urlLoading, setUrlLoading] = React.useState(false)
    const [urlError, setUrlError] = React.useState("")
    const [showLinkedInHelper, setShowLinkedInHelper] = React.useState(false)
    const [showBlockedPopup, setShowBlockedPopup] = React.useState(false)
    const [linkedInPasteText, setLinkedInPasteText] = React.useState("")
    const [linkedInPasteLoading, setLinkedInPasteLoading] = React.useState(false)
    // Personas
    const [personas, setPersonas] = React.useState<any[]>([])
    const [selectedPersonaId, setSelectedPersonaId] = React.useState<
        string | null
    >(null)
    const [personasLoading, setPersonasLoading] = React.useState(false)

    // Persistent context
    const [jobContext, setJobContext] = React.useState<{
        title: string
        company: string
    }>({
        title: "",
        company: "",
    })

    // Results
    const [jobFitResult, setJobFitResult] = React.useState<any>(null)
    const [positioningResult, setPositioningResult] = React.useState<any>(null)
    const [coverLetterResult, setCoverLetterResult] = React.useState<any>(null)
    const [networkingResult, setNetworkingResult] = React.useState<any>(null)
    const [clName, setClName] = React.useState("")
    const [clCompany, setClCompany] = React.useState("")
    const [clHiringManager, setClHiringManager] = React.useState("")

    // Loading
    const [loadingJobFit, setLoadingJobFit] = React.useState(false)
    const [loadingPositioning, setLoadingPositioning] = React.useState(false)
    const [loadingCoverLetter, setLoadingCoverLetter] = React.useState(false)
    const [loadingNetworking, setLoadingNetworking] = React.useState(false)

    // Messages
    const [errorMsg, setErrorMsg] = React.useState<string | null>(null)
    const [upgradeHint, setUpgradeHint] = React.useState<TabKey | null>(null)
    const [debug, setDebug] = React.useState<any>(null)

    // JobFit accordions
    const [jobfitWhyOpen, setJobfitWhyOpen] = React.useState(false)
    const [jobfitRisksOpen, setJobfitRisksOpen] = React.useState(false)

    // Positioning accordions
    const [posDriversOpen, setPosDriversOpen] = React.useState(true)
    const [posBulletsOpen, setPosBulletsOpen] = React.useState(false)
    const [posSummaryOpen, setPosSummaryOpen] = React.useState(false)

    // Networking open set
    // Hydration guard — state declared here with all other hooks
    const [mounted, setMounted] = React.useState(false)

    const [netOpenSet, setNetOpenSet] = React.useState<Set<number>>(
        () => new Set()
    )

    // Keep latest token
    const accessTokenRef = React.useRef<string | null>(null)
    React.useEffect(() => {
        accessTokenRef.current = accessToken
    }, [accessToken])

    const isLoggedIn = authStatus === "logged_in" && !!accessToken
    const hasJob = job.trim().length > 0

    const hasAnyResult =
        !!jobFitResult ||
        !!positioningResult ||
        !!coverLetterResult ||
        !!networkingResult
    React.useEffect(() => {
        if (typeof window === "undefined") return

        const path = window.location.pathname || ""
        const isJobFitIntake =
            path === "/" || path === "/signal" || path === "/signal/jobfit"

        if (!isJobFitIntake) return

        const existingSessionId = localStorage.getItem("jobfit_session_id")
        const sessionId = existingSessionId || crypto.randomUUID()

        if (!existingSessionId) {
            localStorage.setItem("jobfit_session_id", sessionId)
        }

        const url = new URL(window.location.href)

        // ── Step 2: Attribution capture ──────────────────────────────────
        // Read tracking params passed from the marketing page CTA click.
        // Save to localStorage so they persist if the user returns later.
        const mktSession = url.searchParams.get("mkt_session")
        const refSource = url.searchParams.get("ref_source")
        const refMedium = url.searchParams.get("ref_medium")
        const refCampaign = url.searchParams.get("ref_campaign")
        const clickedFrom = url.searchParams.get("clicked_from")

        if (mktSession) {
            localStorage.setItem("signal_mkt_session", mktSession)
            localStorage.setItem("signal_ref_source", refSource || "")
            localStorage.setItem("signal_ref_medium", refMedium || "")
            localStorage.setItem("signal_ref_campaign", refCampaign || "")
            localStorage.setItem("signal_clicked_from", clickedFrom || "")

            // Also save to sessionStorage so /api/track calls on
            // subsequent pages (intake form, etc.) can include UTM data
            // even when the URL params have been stripped.
            try {
                sessionStorage.setItem("signal_utm_source", refSource || "")
                sessionStorage.setItem("signal_utm_medium", refMedium || "")
                sessionStorage.setItem("signal_utm_campaign", refCampaign || "")
            } catch {}

            // Write attribution row to Supabase
            supabase
                .from("signal_attribution")
                .upsert(
                    [
                        {
                            mkt_session_id: mktSession,
                            app_session_id: sessionId,
                            ref_source: refSource || "direct",
                            ref_medium: refMedium || "",
                            ref_campaign: refCampaign || "",
                            clicked_from: clickedFrom || "",
                            intake_started_at: new Date().toISOString(),
                        },
                    ],
                    { onConflict: "mkt_session_id" }
                )
                .then(({ error }) => {
                    if (error)
                        console.error("signal_attribution insert failed", error)
                    else
                        console.log("[SIGNAL] Attribution captured", mktSession)
                })
        }

        // Also persist URL UTM params to sessionStorage (for non-attribution flows)
        const urlUtmSource = url.searchParams.get("utm_source")
        const urlUtmMedium = url.searchParams.get("utm_medium")
        const urlUtmCampaign = url.searchParams.get("utm_campaign")
        try {
            if (urlUtmSource)
                sessionStorage.setItem("signal_utm_source", urlUtmSource)
            if (urlUtmMedium)
                sessionStorage.setItem("signal_utm_medium", urlUtmMedium)
            if (urlUtmCampaign)
                sessionStorage.setItem("signal_utm_campaign", urlUtmCampaign)
        } catch {}
        // ─────────────────────────────────────────────────────────────────

        // Resolve UTM: prefer URL params, fall back to sessionStorage
        const resolvedUtmSource =
            urlUtmSource || sessionStorage.getItem("signal_utm_source") || null
        const resolvedUtmMedium =
            urlUtmMedium || sessionStorage.getItem("signal_utm_medium") || null
        const resolvedUtmCampaign =
            urlUtmCampaign ||
            sessionStorage.getItem("signal_utm_campaign") ||
            null

        // Track page view via /api/track (server-side insert)
        fetch(`${API_BASE}/api/track`, {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
                session_id: sessionId,
                page_path: path,
                page_name: "jobfit_trial_intake",
                referrer: document.referrer || null,
                utm_source: resolvedUtmSource,
                utm_medium: resolvedUtmMedium,
                utm_campaign: resolvedUtmCampaign,
                utm_content: url.searchParams.get("utm_content"),
                utm_term: url.searchParams.get("utm_term"),
            }),
        }).catch((err) => {
            console.error("track call failed", err)
        })
    }, [])

    const [trialEmail, setTrialEmail] = React.useState("")

    React.useEffect(() => {
        if (typeof window !== "undefined") {
            setTrialEmail(localStorage.getItem("jobfit_email") || "")
        }
    }, [])

    const displayEmail = authedEmail || trialEmail
    const isTrialUser = !isLoggedIn && !!trialEmail
    React.useEffect(() => {
        if (hasAnyResult) setJobInputOpen(false)
    }, [hasAnyResult])

    // On JobFit result, lock job context if available
    React.useEffect(() => {
        if (!jobFitResult) return
        const ctx = extractJobContextFromJobFit(jobFitResult)
        if (ctx.title || ctx.company) setJobContext(ctx)
    }, [jobFitResult])

    // REVIEW behavior: collapse both, then auto-open risks on REVIEW
    React.useEffect(() => {
        const decision = String(jobFitResult?.decision || "").toLowerCase()
        if (!decision) return
        const isPass = decision.includes("pass") || decision.includes("do not")
        setJobfitWhyOpen(false)
        setJobfitRisksOpen(false)

        if (decision.includes("review")) setJobfitRisksOpen(true)
    }, [jobFitResult])

    function clearErrorAndDebug() {
        setErrorMsg(null)
        setDebug(null)
        setUpgradeHint(null)
    }

    function computeAccessMode(sessionToken?: string | null) {
        if (sessionToken) return "full" as const
        if (getTrialUnlocked()) return "jobfit_only" as const
        return "full" as const
    }

    // On mount: exchange code if present, then check session, then subscribe
    React.useEffect(() => {
        let mounted = true

        async function initSession() {
            let timedOut = false
            const timer = setTimeout(() => {
                timedOut = true
                if (!mounted) return
                setAuthStatus("logged_out")
                setAccessMode(computeAccessMode(null))
            }, 10000)

            try {
                if (typeof window !== "undefined") {
                    const url = new URL(window.location.href)
                    const code = url.searchParams.get("code")
                    if (code) {
                        await supabase.auth.exchangeCodeForSession(code)
                        url.searchParams.delete("code")
                        window.history.replaceState({}, "", url.toString())
                    }

                    // Token handoff from dashboard: tokens are passed in the
                    // URL hash (not query params) to avoid Framer's router
                    // choking on the long JWT strings. Falls back to query
                    // params for backwards compatibility.
                    const hashParams = new URLSearchParams((window.location.hash || "").replace(/^#/, ""))
                    const accessToken = hashParams.get("access_token") || url.searchParams.get("access_token")
                    const refreshToken = hashParams.get("refresh_token") || url.searchParams.get("refresh_token")
                    if (accessToken && refreshToken) {
                        await supabase.auth.setSession({
                            access_token: accessToken,
                            refresh_token: refreshToken,
                        })
                        url.searchParams.delete("access_token")
                        url.searchParams.delete("refresh_token")
                        window.history.replaceState({}, "", url.pathname + url.search)
                    }
                }

                const { data, error } = await supabase.auth.getSession()

                clearTimeout(timer)
                if (timedOut || !mounted) return

                if (error) {
                    setAccessToken(null)
                    setAuthedEmail(null)
                    setAuthStatus("logged_out")
                    setAccessMode(computeAccessMode(null))
                    return
                }

                const session = data?.session
                if (session?.access_token) {
                    setAccessToken(session.access_token)
                    setAuthedEmail(session.user?.email || null)
                    setAuthStatus("logged_in")
                    setAccessMode(computeAccessMode(session.access_token))
                    loadPersonas(session.access_token)

                    // ── Deep link: hydrate from ?run=<uuid> ──────────────
                    // When the tracker links to /signal/jobfit?run=<id>,
                    // fetch the cached run and hydrate all four result tabs
                    // so the user sees their previous results immediately.
                    if (typeof window !== "undefined") {
                        const deepUrl = new URL(window.location.href)
                        const runId = deepUrl.searchParams.get("run")
                        if (runId && mounted) {
                            setLoadingJobFit(true)
                            fetch(`https://wrnsignal-api.vercel.app/api/runs/${encodeURIComponent(runId)}`, {
                                headers: { Authorization: `Bearer ${session.access_token}` },
                            })
                                .then((res) => {
                                    if (!res.ok) throw new Error(`${res.status}`)
                                    return res.json()
                                })
                                .then((data) => {
                                    if (!mounted) return
                                    if (data.jobfit) setJobFitResult(normalizeJobFitResponseForUI(data.jobfit))
                                    if (data.positioning) setPositioningResult(data.positioning)
                                    if (data.coverLetter) setCoverLetterResult(data.coverLetter)
                                    if (data.networking) setNetworkingResult(data.networking)
                                    if (data.jobDescription) setJob(data.jobDescription)
                                    if (data.jobTitle) setManualJobTitle(data.jobTitle)
                                    if (data.companyName) setManualCompanyName(data.companyName)
                                    setTab("jobfit")
                                    window.history.replaceState({}, "", window.location.pathname)
                                })
                                .catch((err) => {
                                    console.warn("[deep-link] Failed to load run:", err)
                                })
                                .finally(() => {
                                    if (mounted) setLoadingJobFit(false)
                                })
                        }
                    }
                    // ── End deep link ────────────────────────────────────
                } else {
                    setAccessToken(null)
                    setAuthedEmail(null)
                    setAuthStatus("logged_out")
                    setAccessMode(computeAccessMode(null))
                }
            } catch {
                clearTimeout(timer)
                if (timedOut || !mounted) return
                setAccessToken(null)
                setAuthedEmail(null)
                setAuthStatus("logged_out")
                setAccessMode(computeAccessMode(null))
            }
        }

        initSession()

        const { data: sub } = supabase.auth.onAuthStateChange(
            (event, session) => {
                if (!mounted) return

                if (session?.access_token) {
                    setAccessToken(session.access_token)
                    setAuthedEmail(session.user?.email || null)
                    setAuthStatus("logged_in")
                    // Load personas when user logs in
                    loadPersonas(session.access_token)
                    setAccessMode("full")

                    if (typeof window !== "undefined") {
                        const path = window.location.pathname || ""
                        if (path === "/signal/auth") {
                            window.top!.location.replace("https://wrnsignal-api.vercel.app/dashboard")
                            return
                        }
                    }
                } else if (event === "SIGNED_OUT") {
                    // Only log out on explicit sign-out, not on transient
                    // refresh failures. Supabase auto-retries token refresh;
                    // a single failed refresh should not kick the user out.
                    setAccessToken(null)
                    setAuthedEmail(null)
                    setAuthStatus("logged_out")
                    setAccessMode(computeAccessMode(null))
                }
            }
        )

        function onTrialUnlock() {
            if (!mounted) return
            if (!accessTokenRef.current) setAccessMode("jobfit_only")
        }

        if (typeof window !== "undefined") {
            window.addEventListener("jobfit_unlocked", onTrialUnlock)
        }

        return () => {
            mounted = false
            sub?.subscription?.unsubscribe?.()
            if (typeof window !== "undefined") {
                window.removeEventListener("jobfit_unlocked", onTrialUnlock)
            }
        }
    }, [])

    // Safety: prevent auth checking hang
    React.useEffect(() => {
        if (authStatus !== "checking") return
        const t = setTimeout(() => {
            setAuthStatus("logged_out")
            setAccessMode(computeAccessMode(null))
        }, 10000)
        return () => clearTimeout(t)
    }, [authStatus])

    // Hydration guard — effect and early return placed after ALL hooks
    // so hook call order is identical on every render.
    React.useEffect(() => { setMounted(true) }, [])
    if (!mounted) return null

    async function sendMagicLink() {
        clearErrorAndDebug()

        const e = email.trim().toLowerCase()
        if (!e) {
            setErrorMsg("Enter your email.")
            return
        }

        setAuthStatus("checking")

        try {
            const res = await fetch(`${API_BASE}/api/auth/send-link`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ email: e }),
            })
            const data = await res.json()

            if (res.ok && data.sent) {
                setAuthStatus("email_sent")
                setMagicLinkSent(true)
            } else if (res.status === 403 && data.error === "no_account") {
                setAuthStatus("logged_out")
                setAccessMode(computeAccessMode(null))
                setErrorMsg(
                    "No account found for that email. Purchase access or run a free job analysis below."
                )
            } else {
                throw new Error(data.message || data.error || "Unknown error")
            }
        } catch (err: any) {
            console.error("MAGIC LINK ERROR", err)
            setAuthStatus("logged_out")
            setAccessMode(computeAccessMode(null))
            setErrorMsg(
                "Something went wrong. Please try again or contact support."
            )
        }
    }

    async function loadPersonas(token: string) {
        setPersonasLoading(true)
        try {
            const res = await fetch(`${API_BASE}/api/personas`, {
                headers: { Authorization: `Bearer ${token}` },
            })
            if (!res.ok) return
            const data = await res.json()
            const list = Array.isArray(data) ? data : (data.personas ?? [])
            setPersonas(list)
            // Auto-select default persona
            const defaultPersona =
                list.find((p: any) => p.is_default) ?? list[0]
            if (defaultPersona) setSelectedPersonaId(defaultPersona.id)
        } catch (e) {
            console.warn("Failed to load personas", e)
        } finally {
            setPersonasLoading(false)
        }
    }

    async function continueTrial() {
        clearErrorAndDebug()

        const e = trialEmail.trim().toLowerCase()
        if (!e) {
            setErrorMsg("Enter your email.")
            return
        }

        setAuthStatus("checking")

        try {
            const data = await postJsonWithDebug(
                `${API_BASE}/api/jobfit-trial-lookup`,
                { email: e },
                setDebug,
                null
            )

            if (!data?.ok) {
                throw new Error(
                    "We couldn’t find a SIGNAL trial profile for that email. If you haven’t signed up yet, go to workforcereadynow.com/signal to get started."
                )
            }

            if (typeof window !== "undefined") {
                localStorage.setItem("jobfit_unlocked", "true")
                localStorage.setItem("jobfit_email", e)
                sessionStorage.setItem("jobfit_email", e)
                window.dispatchEvent(new Event("jobfit_unlocked"))
            }

            setTrialEmail(e)
            setAccessToken(null)
            setAuthedEmail(null)
            setAuthStatus("logged_out")
            setAccessMode("jobfit_only")
            runNewJob(true)

            if (typeof window !== "undefined") {
                window.top!.location.replace(
                    "https://wrnsignal.workforcereadynow.com/signal/jobfit"
                )
            }
        } catch (err: any) {
            setAuthStatus("logged_out")
            setAccessMode(computeAccessMode(null))
            setErrorMsg(
                err?.message ||
                    "We couldn’t find a SIGNAL trial profile for that email. If you haven’t signed up yet, go to workforcereadynow.com/signal to get started."
            )
        }
    }

    async function signInWithPassword() {
        clearErrorAndDebug()
        const e = email.trim().toLowerCase()
        if (!e || !password) {
            setErrorMsg("Enter email and password.")
            return
        }

        setAuthStatus("checking")

        try {
            const { error } = await supabase.auth.signInWithPassword({
                email: e,
                password,
            })
            if (error) throw error
        } catch (err: any) {
            setAuthStatus("logged_out")
            setErrorMsg(err?.message || "Password login failed.")
        }
    }

    async function logout() {
        // 1️⃣ Clear any error/debug state
        clearErrorAndDebug()

        try {
            // 2️⃣ Sign out from Supabase (full-access users)
            if (supabase) await supabase.auth.signOut()
        } catch (err) {
            console.warn("Supabase signOut error:", err)
        } finally {
            // 3️⃣ Reset React state
            setAccessToken(null)
            setAuthedEmail(null)
            setAuthStatus("logged_out")
            setAccessMode("full") // default for new session
            runNewJob(true)

            // 4️⃣ Clear trial info from localStorage
            if (typeof window !== "undefined") {
                localStorage.removeItem("jobfit_email")
                localStorage.removeItem("jobfit_unlocked")
            }

            // 5️⃣ Force UI refresh to clear SideNav
            if (typeof window !== "undefined") window.top!.location.replace("https://wrnsignal.workforcereadynow.com/signal/jobfit")
        }
    }

    function runNewJob(isSilent = false) {
        if (!isSilent) clearErrorAndDebug()
        setJob("")
        setManualCompanyName("")
        setManualJobTitle("")
        setManualPasteStep("paste")
        setManualPasteText("")
        setManualExtracting(false)
        setJobFitResult(null)
        setPositioningResult(null)
        setCoverLetterResult(null)
        setNetworkingResult(null)
        setTab("jobfit")
        setJobInputOpen(true)
        setJobfitWhyOpen(false)
        setJobfitRisksOpen(false)
        setPosDriversOpen(true)
        setPosBulletsOpen(false)
        setPosSummaryOpen(false)
        setNetOpenSet(new Set())
        setJobContext({ title: "", company: "" })
        const defaultPersona =
            personas.find((p: any) => p.is_default) ?? personas[0]
        if (defaultPersona) setSelectedPersonaId(defaultPersona.id)
    }

    async function handleFetchUrl() {
        if (!jobUrl.trim()) return
        setUrlLoading(true)
        setUrlError("")
        setShowLinkedInHelper(false)
        setShowBlockedPopup(false)
        try {
            const res = await fetch(`${API_BASE}/api/parse-job-url`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ url: jobUrl.trim() }),
            })
            const data = await res.json()
            if (data.code === "LINKEDIN" || data.code === "INDEED_BLOCKED" || data.code === "BLOCKED") {
                setShowBlockedPopup(true)
                setShowLinkedInHelper(true)
                return
            }
            if (data.error) {
                setUrlError(data.error)
                return
            }
            if (data.companyName) setManualCompanyName(data.companyName)
            if (data.jobTitle) setManualJobTitle(data.jobTitle)
            if (data.jobDescription) setJob(data.jobDescription)
            // Keep jobUrl — it gets sent to /api/jobfit as job_url
            // so the application dashboard auto-populates the link
            setUrlError("")
            setShowLinkedInHelper(false)
            setShowBlockedPopup(false)
        } catch {
            setUrlError("Something went wrong. Please paste the job description manually.")
        } finally {
            setUrlLoading(false)
        }
    }

    async function handleLinkedInPaste() {
        if (!linkedInPasteText.trim() || linkedInPasteText.length < 50) {
            setUrlError("Please paste more content — select the full page.")
            return
        }
        setLinkedInPasteLoading(true)
        try {
            const res = await fetch(`${API_BASE}/api/parse-job-text`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ text: linkedInPasteText.trim() }),
            })
            const data = await res.json()
            if (data.error) {
                setUrlError(data.error)
                return
            }
            if (data.companyName) setManualCompanyName(data.companyName)
            if (data.jobTitle) setManualJobTitle(data.jobTitle)
            if (data.jobDescription) setJob(data.jobDescription)
            setShowLinkedInHelper(false)
            setLinkedInPasteText("")
            // Keep jobUrl — it's the LinkedIn URL that gets sent as job_url
            setUrlError("")
        } catch {
            setUrlError("Extraction failed. Please paste the description manually below.")
        } finally {
            setLinkedInPasteLoading(false)
        }
    }

    async function handleManualExtract() {
        if (!manualPasteText.trim() || manualPasteText.length < 50) {
            setUrlError("Please paste more content — at least the full job posting.")
            return
        }
        setManualExtracting(true)
        setUrlError("")
        try {
            const res = await fetch(`${API_BASE}/api/parse-job-text`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify({ text: manualPasteText.trim() }),
            })
            const data = await res.json()
            if (data.error) {
                setUrlError(data.error)
                return
            }
            if (data.companyName) setManualCompanyName(data.companyName)
            if (data.jobTitle) setManualJobTitle(data.jobTitle)
            if (data.jobDescription) setJob(data.jobDescription)
            setManualPasteStep("confirm")
            setUrlError("")
        } catch {
            setUrlError("Extraction failed. Please try again.")
        } finally {
            setManualExtracting(false)
        }
    }

    function isLockedTab(key: TabKey) {
        if (accessMode !== "jobfit_only") return false
        return key !== "jobfit"
    }

    function showLockedMessage(which: TabKey) {
        setErrorMsg(null)
        setUpgradeHint(which)
    }

    function goTo(tabKey: TabKey) {
        const locked = isLockedTab(tabKey)
        if (locked) {
            setTab(tabKey)
            showLockedMessage(tabKey)
            return
        }
        setTab(tabKey)
    }

    function renderPrimaryRunButton(
        label: string,
        onClick: () => void,
        loading: boolean
    ) {
        return (
            <div style={{ marginTop: 10 }}>
                <button
                    style={{
                        ...primaryBtn,
                        width: "100%",
                        opacity: 1,
                        cursor: "pointer",
                        display: "inline-flex",
                        alignItems: "center",
                        justifyContent: "center",
                        gap: 10,
                    }}
                    disabled={loading}
                    onClick={onClick}
                    type="button"
                >
                    {loading ? <span style={spinner} aria-hidden /> : null}
                    {loading ? "Running…" : label}
                </button>
                {loading ? (
                    <div
                        style={{
                            marginTop: 12,
                            textAlign: "center" as const,
                            fontSize: 12,
                            color: "rgba(255,255,255,0.45)",
                            fontStyle: "italic" as const,
                            lineHeight: 1.5,
                        }}
                    >
                        This takes 20–40 seconds — we're reading your full
                        profile and the job in detail. Quality takes time.
                    </div>
                ) : null}
            </div>
        )
    }

    function renderUpgradePanel(variant: "inline" | "after_jobfit") {
        if (accessMode !== "jobfit_only") return null

        const decisionLower = String(jobFitResult?.decision || "").toLowerCase()

        const title =
            variant === "after_jobfit"
                ? decisionLower.includes("pass") ||
                  decisionLower.includes("do not")
                    ? "This job is not the move."
                    : decisionLower.includes("review")
                      ? "This one needs more caution."
                      : "This job may be worth pursuing. Now the goal is standing out."
                : "Want the next steps?"

        const body =
            variant === "after_jobfit"
                ? decisionLower.includes("pass") ||
                  decisionLower.includes("do not")
                    ? "Full SIGNAL helps you focus on stronger jobs and execute better when a role is actually worth your time."
                    : decisionLower.includes("review")
                      ? "Full SIGNAL helps you decide whether to proceed and how to position yourself if you do."
                      : "Full SIGNAL helps you position, message, and network so you do not blend in after you apply."
                : "JobFit only tells you the decision. Full SIGNAL shows you how to execute."

        const bullets = [
            "Positioning: align bullets and keywords to the job fast",
            "Cover Letter: generate job specific materials when it is worth it",
            "Networking: simple post apply plan plus copy paste messages",
        ]

        return (
            <div style={upgradePanel}>
                <div style={upgradeTitle}>{title}</div>
                <div style={upgradeBody}>{body}</div>

                <div style={{ height: 10 }} />

                <div style={upgradeBullets}>
                    {bullets.map((b, i) => (
                        <div key={i} style={{ marginBottom: 8 }}>
                            • {b}
                        </div>
                    ))}
                </div>

                <div style={{ height: 12 }} />

                <button
                    style={upgradeBtn}
                    onClick={openUpgradePage}
                    type="button"
                >
                    Unlock Full SIGNAL
                </button>
            </div>
        )
    }

    function renderLockedPreview(which: TabKey) {
        if (which === "positioning") {
            return (
                <>
                    <div style={outputText}>
                        <b>Locked.</b>
                        <div style={{ height: 10 }} />
                        Positioning rewrites your bullet angles so your resume
                        passes recruiter scan and keyword screens.
                    </div>
                    <div style={{ height: 14 }} />
                    {renderUpgradePanel("inline")}
                </>
            )
        }

        if (which === "coverletter") {
            return (
                <>
                    <div style={outputText}>
                        <b>Locked.</b>
                        <div style={{ height: 10 }} />
                        Cover Letter generates clean job aligned materials fast.
                    </div>
                    <div style={{ height: 14 }} />
                    {renderUpgradePanel("inline")}
                </>
            )
        }

        return (
            <>
                <div style={outputText}>
                    <b>Locked.</b>
                    <div style={{ height: 10 }} />
                    Networking gives you a post apply plan with exactly who to
                    target and what to say.
                </div>
                <div style={{ height: 14 }} />
                {renderUpgradePanel("inline")}
            </>
        )
    }

    function normalizeJobFitResponseForUI(data: any) {
        if (!data) return null
        if (data && typeof data === "object" && "result" in data && data.result)
            return data.result
        return data
    }

    async function runJobFit() {
        clearErrorAndDebug()
        setLoadingJobFit(true)

        // Pre-capture context from job text (fallback) but do not overwrite existing
        setJobContext((prev) => {
            if (prev.title || prev.company) return prev
            const fromText = extractJobContextFromJobText(job)
            if (fromText.title || fromText.company) return fromText
            return prev
        })

        try {
            if (!hasJob) throw new Error("Paste a job description first.")
            // Job title and company name are now REQUIRED by the scoring
            // engine — the deterministic title/company extractor had ~0%
            // accuracy on varied JD formats, so the API rejects requests
            // that omit them. Validate client-side so the user gets a
            // friendly message instead of a 400 from the API.
            if (!manualCompanyName.trim()) {
                throw new Error("Enter the company name before running JobFit.")
            }
            if (!manualJobTitle.trim()) {
                throw new Error("Enter the job title before running JobFit.")
            }

            if (accessMode === "jobfit_only") {
                const trialEmail = getTrialEmail().trim().toLowerCase()
                if (!trialEmail) {
                    throw new Error(
                        "Missing email. Complete the Job Fit intake first so we can save your profile."
                    )
                }

                const data = await postJsonWithDebug(
                    `${API_BASE}/api/jobfit-run-trial`,
                    {
                        email: trialEmail,
                        job_description: job.trim(),
                        // User-provided labels — now always included
                        // because client-side validation ensures they
                        // are non-empty before we get here.
                        company_name: manualCompanyName.trim(),
                        job_title: manualJobTitle.trim(),
                    },
                    setDebug,
                    null
                )

                const normalized = normalizeJobFitResponseForUI(data)
                setJobFitResult(normalized)

                const ctxFromApi = extractJobContextFromJobFit(normalized)
                const ctxFromText = extractJobContextFromJobText(job)

                setJobContext(() => {
                    if (ctxFromApi.title || ctxFromApi.company)
                        return ctxFromApi
                    if (ctxFromText.title || ctxFromText.company)
                        return ctxFromText
                    return { title: "", company: "" }
                })

                setTab("jobfit")
                return
            }

            if (!accessToken) throw new Error("Please log in again.")

            const data = await postJsonWithDebug(
                `${API_BASE}/api/jobfit`,
                {
                    job: job.trim(),
                    // User-provided labels are required — client-side
                    // validation above has already confirmed both are
                    // non-empty by this point.
                    company_name: manualCompanyName.trim(),
                    job_title: manualJobTitle.trim(),
                    ...(jobUrl.trim() ? { job_url: jobUrl.trim() } : {}),
                    ...(selectedPersonaId
                        ? { persona_id: selectedPersonaId }
                        : {}),
                },
                setDebug,
                accessToken
            )

            const normalized = normalizeJobFitResponseForUI(data)
            setJobFitResult(normalized)

            const ctxFromApi = extractJobContextFromJobFit(normalized)
            setJobContext((prev) => {
                if (ctxFromApi.title || ctxFromApi.company) return ctxFromApi
                return prev
            })

            setTab("jobfit")
        } catch (e: any) {
            setErrorMsg(e?.message || String(e))
        } finally {
            setLoadingJobFit(false)
        }
    }

    async function runPositioning() {
        if (accessMode === "jobfit_only") {
            showLockedMessage("positioning")
            return
        }

        clearErrorAndDebug()
        setLoadingPositioning(true)

        try {
            if (!hasJob) throw new Error("Paste a job description first.")
            if (!accessToken) throw new Error("Please log in again.")

            const data = await postJsonWithDebug(
                `${API_BASE}/api/positioning`,
                { job: job.trim() },
                setDebug,
                accessToken
            )

            setPositioningResult(data)
            setPosDriversOpen(true)
            setPosBulletsOpen(false)
            setPosSummaryOpen(false)
            setTab("positioning")
        } catch (e: any) {
            setErrorMsg(e?.message || String(e))
        } finally {
            setLoadingPositioning(false)
        }
    }

    async function runCoverLetter() {
        if (accessMode === "jobfit_only") {
            showLockedMessage("coverletter")
            return
        }

        clearErrorAndDebug()
        setLoadingCoverLetter(true)

        try {
            if (!hasJob) throw new Error("Paste a job description first.")
            if (!accessToken) throw new Error("Please log in again.")

            const data = await postJsonWithDebug(
                `${API_BASE}/api/coverletter`,
                { job: job.trim(), jobfit_result: jobFitResult ?? null },
                setDebug,
                accessToken
            )

            setCoverLetterResult(data)
            setTab("coverletter")
        } catch (e: any) {
            setErrorMsg(e?.message || String(e))
        } finally {
            setLoadingCoverLetter(false)
        }
    }

    async function runNetworking() {
        if (accessMode === "jobfit_only") {
            showLockedMessage("networking")
            return
        }

        clearErrorAndDebug()
        setLoadingNetworking(true)

        try {
            if (!hasJob) throw new Error("Paste a job description first.")
            if (!accessToken) throw new Error("Please log in again.")

            const data = await postJsonWithDebug(
                `${API_BASE}/api/networking`,
                { job: job.trim() },
                setDebug,
                accessToken
            )

            setNetworkingResult(data)
            setTab("networking")
        } catch (e: any) {
            setErrorMsg(e?.message || String(e))
        } finally {
            setLoadingNetworking(false)
        }
    }

    /* ---------- RENDER: JOBFIT ---------- */
    function renderJobFit() {
        if (!jobFitResult) {
            return accessMode === "jobfit_only" ? (
                <>
                    <div style={{ height: 10 }} />
                    {renderUpgradePanel("inline")}
                </>
            ) : null
        }

        // ── Data ─────────────────────────────────────────────────
        const decisionRaw = String(jobFitResult?.decision ?? "").trim()
        const decision = decisionRaw || "REVIEW"
        const lower = decision.toLowerCase()

        const score =
            typeof jobFitResult?.score === "number"
                ? jobFitResult.score
                : Number(jobFitResult?.score ?? 0)
        const hasScore = Number.isFinite(score) && score > 0

        const nextStep = String(jobFitResult?.next_step ?? "").trim()

        const whyCodes: any[] = Array.isArray(jobFitResult?.why_codes)
            ? jobFitResult.why_codes
            : []
        const riskCodes: any[] = Array.isArray(jobFitResult?.risk_codes)
            ? jobFitResult.risk_codes
            : []

        const renderedBullets: string[] = pickFirstArray(
            jobFitResult?.bullets,
            jobFitResult?.why
        )
            .map(toSafeLine)
            .map((s: string) => s.trim())
            .filter(Boolean)

        const renderedRisks: string[] = pickFirstArray(
            jobFitResult?.risk,
            jobFitResult?.risk_bullets,
            jobFitResult?.risk_flags,
            jobFitResult?.risks
        )
            .map(toSafeLine)
            .map((s: string) => s.trim())
            .filter(Boolean)

        const gate = jobFitResult?.gate_triggered ?? { type: "none" }
        const isForcePass = gate?.type === "force_pass"
        const gateDetail = String(gate?.detail ?? "").trim()

        const jobSignals = jobFitResult?.job_signals ?? {}
        const isInternship = jobSignals?.internship?.isInternship === true
        const locationMode = String(jobSignals?.location?.mode ?? "").trim()
        const locationCity = String(jobSignals?.location?.city ?? "").trim()
        const yearsRequired = jobSignals?.yearsRequired ?? null

        // Job context — API signals first, then state, then raw text extraction
        const ctxFromState = jobContext ?? { title: "", company: "" }
        const ctxFromText = extractJobContextFromJobText(job)
        const ctxTitle =
            String(jobSignals?.jobTitle ?? "").trim() ||
            ctxFromState.title ||
            ctxFromText.title ||
            ""
        const ctxCompany =
            String(jobSignals?.companyName ?? "").trim() ||
            ctxFromState.company ||
            ctxFromText.company ||
            ""

        const isPass = lower.includes("pass") || lower.includes("do not")
        const isReview = lower.includes("review")
        const isPriority = lower.includes("priority")

        // ── Decision palette ─────────────────────────────────────
        const decisionColor = isPass
            ? "#E87070"
            : isReview
              ? "#D4A444"
              : isPriority
                ? "#0FD668"
                : "#3DDC84"

        const stripeGradient = isPass
            ? "linear-gradient(90deg,#6B1E1E,#C23B3B)"
            : isReview
              ? "linear-gradient(90deg,#8A5A00,#C68A1E)"
              : isPriority
                ? "linear-gradient(90deg,#FEB06A,#0FD668)"
                : "linear-gradient(90deg,#18A957,#51ADE5)"

        const scoreBarGradient = isPass
            ? "#C23B3B"
            : isReview
              ? "linear-gradient(90deg,#C68A1E,#D4A444)"
              : isPriority
                ? "linear-gradient(90deg,#FEB06A,#0FD668)"
                : "linear-gradient(90deg,#18A957,#51ADE5)"

        const ctaBg = isPass
            ? undefined
            : isReview
              ? "linear-gradient(90deg,#C68A1E,#FEB06A)"
              : isPriority
                ? "linear-gradient(90deg,#FEB06A,#51ADE5)"
                : "linear-gradient(90deg,#18A957,#51ADE5)"

        const moveDotColor = isPass
            ? "#E87070"
            : isReview
              ? "#D4A444"
              : isPriority
                ? "#FEB06A"
                : "#3DDC84"

        const ctaLabel = isPass
            ? "Run New Job →"
            : isReview
              ? "Proceed Carefully →"
              : "Proceed to Positioning →"

        // ── Sort why_codes same as V4 renderer ───────────────────
        const WHY_PRIORITY: Record<string, number> = {
            WHY_INTEREST_ALIGN: 100,
            WHY_TOOL_PROOF: 80,
            WHY_DIRECT_PROOF: 70,
            WHY_ADJACENT_PROOF: 50,
            WHY_EXECUTION_PROOF: 40,
            WHY_MATCH: 20,
        }
        const sortedWhyCodes = whyCodes.slice().sort((a: any, b: any) => {
            const byWeight = (b?.weight ?? 0) - (a?.weight ?? 0)
            if (byWeight !== 0) return byWeight
            return (WHY_PRIORITY[b?.code] ?? 0) - (WHY_PRIORITY[a?.code] ?? 0)
        })

        // ── Helpers ──────────────────────────────────────────────
        function cleanFactQuote(s: string): string {
            return s
                .replace(
                    /^(posting (lists|says|indicates|suggests)|role (requires|expects)|job (requires|expects|says))[:\s]*/i,
                    ""
                )
                .replace(/\.$/, "")
                .trim()
        }

        function cleanRiskTitle(jobFact: string, fallback: string): string {
            const raw = cleanFactQuote(jobFact || fallback)
            return raw ? raw.charAt(0).toUpperCase() + raw.slice(1) : ""
        }

        function onPrimaryClick() {
            if (isPass) {
                runNewJob()
                return
            }
            goTo("positioning")
        }

        // ── Location label ───────────────────────────────────────
        const locLabel = locationCity
            ? locationCity
            : locationMode === "remote"
              ? "Remote"
              : locationMode === "hybrid"
                ? "Hybrid"
                : locationMode === "in_person"
                  ? "On-site"
                  : ""

        const hasCtx = !!(
            ctxTitle ||
            ctxCompany ||
            isInternship ||
            locLabel ||
            yearsRequired !== null
        )

        // ── Section header ───────────────────────────────────────
        function SectionHeader({
            label,
            count,
        }: {
            label: string
            count: number
        }) {
            return (
                <div style={jfSecHd}>
                    <div style={jfSecLabel}>{label}</div>
                    <div style={jfSecLine} />
                    <div style={jfSecCount}>
                        {count} {count === 1 ? "signal" : "signals"}
                    </div>
                </div>
            )
        }

        // ── Why cards ────────────────────────────────────────────
        function WhyCards() {
            // Map why codes to action instructions
            const whyActions: Record<string, string> = {
                WHY_DIRECT_PROOF:
                    "Lead with this in your cover letter first paragraph and mention it in your opener.",
                WHY_TOOL_PROOF:
                    "Name this tool explicitly in your application — they are scanning for it.",
                WHY_ADJACENT_PROOF:
                    "Frame this as directly relevant — connect the dots for the hiring manager.",
                WHY_EXECUTION_PROOF:
                    "Quantify this if you can — a number makes it concrete and memorable.",
                WHY_INTEREST_ALIGN:
                    "Mention your genuine interest in this area — it signals culture fit.",
                WHY_MATCH:
                    "This alignment is a strength — make sure it's visible in your resume.",
            }

            const defaultAction =
                "Highlight this clearly in your application and be ready to speak to it in interviews."

            if (renderedBullets.length > 0) {
                return (
                    <>
                        {renderedBullets.map((bulletText, i) => {
                            const code = sortedWhyCodes[i] as any | undefined
                            const profileFact = String(
                                code?.profile_fact ?? ""
                            ).trim()
                            const jobFact = String(code?.job_fact ?? "").trim()
                            const isDirect = code?.match_strength === "direct"
                            const jobFactClean = jobFact
                                ? cleanFactQuote(jobFact)
                                : ""
                            const proofColor = isDirect ? "#4ade80" : "#7BC8F0"
                            const proofLabel = isDirect
                                ? "Direct proof"
                                : "Your proof"
                            const actionText =
                                whyActions[code?.code] ?? defaultAction

                            return (
                                <div key={i} style={jfWhyCard}>
                                    <div style={jfWhyCardInner}>
                                        <div style={jfWhyCardAccent} />
                                        <div style={{ flex: 1 }}>
                                            <div style={jfWhyBody}>
                                                {bulletText}
                                            </div>
                                            {/* Action instruction */}
                                            <div style={jfWhyAction}>
                                                <div style={jfWhyActionLabel}>
                                                    → Do this
                                                </div>
                                                <div style={jfWhyActionText}>
                                                    {actionText}
                                                </div>
                                            </div>
                                            {/* Proof footer */}
                                            {jobFactClean || profileFact ? (
                                                <div style={jfWhyFoot}>
                                                    {jobFactClean ? (
                                                        <div
                                                            style={{
                                                                ...jfWhyCell,
                                                                borderRight:
                                                                    "1px solid rgba(74,222,128,0.08)",
                                                            }}
                                                        >
                                                            <div
                                                                style={
                                                                    jfWfNeedsLabel
                                                                }
                                                            >
                                                                Role needs
                                                            </div>
                                                            <div
                                                                style={jfWfText}
                                                            >
                                                                {jobFactClean}
                                                            </div>
                                                        </div>
                                                    ) : null}
                                                    {profileFact ? (
                                                        <div style={jfWhyCell}>
                                                            <div
                                                                style={{
                                                                    ...jfWfProofLabel,
                                                                    color: proofColor,
                                                                }}
                                                            >
                                                                {proofLabel}
                                                            </div>
                                                            <div
                                                                style={jfWfText}
                                                            >
                                                                {profileFact}
                                                            </div>
                                                        </div>
                                                    ) : null}
                                                </div>
                                            ) : null}
                                        </div>
                                    </div>
                                </div>
                            )
                        })}
                    </>
                )
            }

            // Fallback: why_codes directly
            if (sortedWhyCodes.length > 0) {
                return (
                    <>
                        {sortedWhyCodes
                            .map((w: any, i: number) => {
                                const body = String(w?.note ?? "").trim()
                                const profileFact = String(
                                    w?.profile_fact ?? ""
                                ).trim()
                                const isDirect = w?.match_strength === "direct"
                                const actionText =
                                    whyActions[w?.code] ?? defaultAction
                                if (!body) return null
                                return (
                                    <div key={i} style={jfWhyCard}>
                                        <div style={jfWhyCardInner}>
                                            <div style={jfWhyCardAccent} />
                                            <div style={{ flex: 1 }}>
                                                <div style={jfWhyBody}>
                                                    {body}
                                                </div>
                                                <div style={jfWhyAction}>
                                                    <div
                                                        style={jfWhyActionLabel}
                                                    >
                                                        → Do this
                                                    </div>
                                                    <div
                                                        style={jfWhyActionText}
                                                    >
                                                        {actionText}
                                                    </div>
                                                </div>
                                                {profileFact ? (
                                                    <div style={jfWhyFoot}>
                                                        <div style={jfWhyCell}>
                                                            <div
                                                                style={{
                                                                    ...jfWfProofLabel,
                                                                    color: isDirect
                                                                        ? "#4ade80"
                                                                        : "#7BC8F0",
                                                                }}
                                                            >
                                                                {isDirect
                                                                    ? "Direct proof"
                                                                    : "Your proof"}
                                                            </div>
                                                            <div
                                                                style={jfWfText}
                                                            >
                                                                {profileFact}
                                                            </div>
                                                        </div>
                                                    </div>
                                                ) : null}
                                            </div>
                                        </div>
                                    </div>
                                )
                            })
                            .filter(Boolean)}
                    </>
                )
            }

            return (
                <div style={{ padding: "14px 16px" }}>
                    <div
                        style={{
                            fontSize: 13,
                            color: "rgba(255,255,255,0.30)",
                        }}
                    >
                        No match signals were generated for this job.
                    </div>
                </div>
            )
        }

        // ── Risk cards ───────────────────────────────────────────
        function RiskCards() {
            // Action instructions by risk code
            const riskActions: Record<string, string> = {
                RISK_MISSING_PROOF:
                    "Address this gap directly in your cover letter. Name the adjacent experience you have and signal you are actively building this skill.",
                RISK_TOOL_GAP:
                    "List this tool in your skills section even if your exposure is limited. Mention any self-study or adjacent tools you know.",
                RISK_YEARS_GAP:
                    "Don't let this stop you. Apply and address the gap directly — explain what you accomplished in your shorter time.",
                RISK_OVERQUALIFIED:
                    "Keep your application focused on growth and learning, not on what you have already done.",
                RISK_LOCATION:
                    "Address location flexibility directly — remote, relocation, or hybrid preference should be stated clearly.",
                RISK_CONTRACT:
                    "This role type conflicts with your constraints. Consider whether this is a hard line or one you could reconsider.",
                RISK_SALES:
                    "This role has sales expectations that conflict with your profile. Evaluate whether the role description matches the actual day-to-day.",
                RISK_ANALYTICS_HEAVY:
                    "If analytics is a stretch, mention any data exposure you have and your willingness to develop this skill.",
            }

            const defaultRiskAction =
                "Address this gap proactively in your cover letter. Don't ignore it — hiring managers notice."

            if (renderedRisks.length > 0) {
                return (
                    <>
                        {renderedRisks.map((r, i) => {
                            const rawCode = riskCodes[i] as any | undefined
                            const severity = String(
                                rawCode?.severity ?? "medium"
                            ).toLowerCase()
                            const cardStyle =
                                severity === "high"
                                    ? jfRiskCardHi
                                    : severity === "low"
                                      ? jfRiskCardLo
                                      : jfRiskCardMd
                            const accentStyle =
                                severity === "high"
                                    ? jfRiskAccentHi
                                    : severity === "low"
                                      ? jfRiskAccentLo
                                      : jfRiskAccentMd
                            const sevStyle =
                                severity === "high"
                                    ? jfRiskSevHi
                                    : severity === "low"
                                      ? jfRiskSevLo
                                      : jfRiskSevMd
                            const actionStyle =
                                severity === "high"
                                    ? jfRiskActionHiTop
                                    : jfRiskAction
                            const actionLabelStyle =
                                severity === "high"
                                    ? jfRiskActionLabelHi
                                    : jfRiskActionLabel
                            const actionText =
                                riskActions[rawCode?.code] ?? defaultRiskAction

                            return (
                                <div key={i} style={cardStyle}>
                                    <div style={jfRiskCardInner}>
                                        <div style={accentStyle} />
                                        <div style={{ flex: 1 }}>
                                            <div style={jfRiskContent}>
                                                <div style={jfRiskTop}>
                                                    <div style={jfRiskTitle}>
                                                        {r}
                                                    </div>
                                                    <div style={sevStyle}>
                                                        {severity}
                                                    </div>
                                                </div>
                                            </div>
                                            <div style={actionStyle}>
                                                <div style={actionLabelStyle}>
                                                    → Fix this
                                                </div>
                                                <div style={jfRiskActionText}>
                                                    {actionText}
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            )
                        })}
                    </>
                )
            }

            // Fallback: raw risk codes
            if (riskCodes.length > 0) {
                return (
                    <>
                        {riskCodes
                            .map((r: any, i: number) => {
                                const body = String(r?.risk ?? "").trim()
                                const jobFact = String(r?.job_fact ?? "").trim()
                                const severity = String(
                                    r?.severity ?? "medium"
                                ).toLowerCase()
                                if (!body) return null

                                const title = cleanRiskTitle(jobFact, body)
                                const cardStyle =
                                    severity === "high"
                                        ? jfRiskCardHi
                                        : severity === "low"
                                          ? jfRiskCardLo
                                          : jfRiskCardMd
                                const accentStyle =
                                    severity === "high"
                                        ? jfRiskAccentHi
                                        : severity === "low"
                                          ? jfRiskAccentLo
                                          : jfRiskAccentMd
                                const sevStyle =
                                    severity === "high"
                                        ? jfRiskSevHi
                                        : severity === "low"
                                          ? jfRiskSevLo
                                          : jfRiskSevMd
                                const actionStyle =
                                    severity === "high"
                                        ? jfRiskActionHiTop
                                        : jfRiskAction
                                const actionLabelStyle =
                                    severity === "high"
                                        ? jfRiskActionLabelHi
                                        : jfRiskActionLabel
                                const actionText =
                                    riskActions[r?.code] ?? defaultRiskAction

                                return (
                                    <div key={i} style={cardStyle}>
                                        <div style={jfRiskCardInner}>
                                            <div style={accentStyle} />
                                            <div style={{ flex: 1 }}>
                                                <div style={jfRiskContent}>
                                                    <div style={jfRiskTop}>
                                                        <div
                                                            style={jfRiskTitle}
                                                        >
                                                            {title}
                                                        </div>
                                                        <div style={sevStyle}>
                                                            {severity}
                                                        </div>
                                                    </div>
                                                    <div style={jfRiskBody}>
                                                        {body}
                                                    </div>
                                                </div>
                                                <div style={actionStyle}>
                                                    <div
                                                        style={actionLabelStyle}
                                                    >
                                                        → Fix this
                                                    </div>
                                                    <div
                                                        style={jfRiskActionText}
                                                    >
                                                        {actionText}
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                )
                            })
                            .filter(Boolean)}
                    </>
                )
            }

            return null
        }

        const whyCount = renderedBullets.length || sortedWhyCodes.length
        const riskCount = renderedRisks.length || riskCodes.length

        // ── Full render ──────────────────────────────────────────
        return (
            <>
                {/* Job context bar */}
                {hasCtx ? (
                    <div style={jfCtxBar}>
                        <div style={jfCtxLeft}>
                            {ctxTitle ? (
                                <span style={jfCtxRole}>{ctxTitle}</span>
                            ) : null}
                            {ctxTitle && ctxCompany ? (
                                <span style={jfCtxSep}> at </span>
                            ) : null}
                            {ctxCompany ? (
                                <span style={jfCtxCo}>{ctxCompany}</span>
                            ) : null}
                        </div>
                        <div style={jfCtxPills}>
                            {isInternship ? (
                                <div
                                    style={{
                                        ...jfCtxPill,
                                        background: "rgba(220,254,255,0.10)",
                                        color: "#DCFEFF",
                                        border: "1px solid rgba(220,254,255,0.20)",
                                    }}
                                >
                                    Internship
                                </div>
                            ) : null}
                            {locLabel ? (
                                <div
                                    style={{
                                        ...jfCtxPill,
                                        background: "rgba(81,173,229,0.12)",
                                        color: "#51ADE5",
                                        border: "1px solid rgba(81,173,229,0.24)",
                                    }}
                                >
                                    {locLabel}
                                </div>
                            ) : null}
                            {yearsRequired !== null ? (
                                <div
                                    style={{
                                        ...jfCtxPill,
                                        background: "rgba(255,255,255,0.07)",
                                        color: "rgba(255,255,255,0.55)",
                                        border: "1px solid rgba(255,255,255,0.14)",
                                    }}
                                >
                                    {yearsRequired}+ yrs exp
                                </div>
                            ) : null}
                        </div>
                    </div>
                ) : null}
                {/* Hero verdict card */}
                <div style={jfHero}>
                    <div
                        style={{ ...jfHeroStripe, background: stripeGradient }}
                    />
                    <div style={jfHeroInner}>
                        {/* Decision + Score */}
                        <div style={jfHeroTop}>
                            <div>
                                <div style={jfHeroEyebrow}>Signal decision</div>
                                {isPriority ? (
                                    <div
                                        style={{
                                            ...jfHeroWord,
                                            background:
                                                "linear-gradient(118deg,#FEB06A 0%,#0FD668 100%)",
                                            WebkitBackgroundClip: "text",
                                            WebkitTextFillColor: "transparent",
                                            backgroundClip: "text",
                                        }}
                                    >
                                        <div>Priority</div>
                                        <div>Apply</div>
                                    </div>
                                ) : (
                                    <div
                                        style={{
                                            ...jfHeroWord,
                                            color: decisionColor,
                                        }}
                                    >
                                        {isPass
                                            ? "Pass"
                                            : isReview
                                              ? "Review"
                                              : "Apply"}
                                    </div>
                                )}
                            </div>
                            {hasScore ? (
                                <div style={jfScoreCol}>
                                    <div style={jfScoreEyebrow}>Fit Score</div>
                                    <div
                                        style={{
                                            display: "flex",
                                            alignItems: "baseline",
                                            gap: 2,
                                        }}
                                    >
                                        <div
                                            style={{
                                                ...jfScoreBig,
                                                color: decisionColor,
                                            }}
                                        >
                                            {score}
                                        </div>
                                        <div style={jfScoreOf}>/100</div>
                                    </div>
                                    <div style={jfScoreBarTrack}>
                                        <div
                                            style={{
                                                ...jfScoreBarFill,
                                                width: `${score}%`,
                                                background: scoreBarGradient,
                                            }}
                                        />
                                    </div>
                                </div>
                            ) : null}
                        </div>

                        <div style={jfRule} />

                        {/* Gate detail */}
                        {isForcePass && gateDetail ? (
                            <div style={{ marginBottom: 14 }}>
                                <div style={jfMoveLabel}>
                                    Why this is a pass
                                </div>
                                <div style={jfMoveText}>{gateDetail}</div>
                            </div>
                        ) : null}

                        {/* Next step */}
                        {nextStep ? (
                            <div
                                style={{
                                    borderLeft: `3px solid ${moveDotColor}`,
                                    background: isPass
                                        ? "rgba(239,68,68,0.08)"
                                        : isReview
                                          ? "rgba(254,176,106,0.08)"
                                          : "rgba(74,232,136,0.08)",
                                    borderRadius: "0 10px 10px 0",
                                    padding: "14px 18px",
                                    marginBottom: 18,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: "2px",
                                        textTransform: "uppercase" as const,
                                        color: moveDotColor,
                                        marginBottom: 6,
                                    }}
                                >
                                    Your move
                                </div>
                                <div
                                    style={{
                                        fontSize: 16,
                                        fontWeight: 600,
                                        lineHeight: 1.5,
                                        color: "rgba(255,255,255,0.92)",
                                    }}
                                >
                                    {nextStep}
                                </div>
                            </div>
                        ) : null}

                        {/* CTA */}
                        {accessMode === "jobfit_only" ? (
                            <div style={{ marginTop: 16 }}>
                                {renderUpgradePanel("after_jobfit")}
                            </div>
                        ) : isPass ? (
                            <button
                                style={jfCtaPass}
                                onClick={onPrimaryClick}
                                type="button"
                            >
                                {ctaLabel}
                            </button>
                        ) : (
                            <button
                                style={{
                                    ...jfCtaGo,
                                    background: ctaBg,
                                }}
                                onClick={onPrimaryClick}
                                type="button"
                            >
                                {ctaLabel}
                            </button>
                        )}
                    </div>
                </div>

                {/* ── ACTION BANNER ── */}
                {(whyCount > 0 || riskCount > 0) && (
                    <div
                        style={{
                            borderRadius: 14,
                            background:
                                "linear-gradient(135deg, rgba(254,176,106,0.10) 0%, rgba(81,173,229,0.08) 100%)",
                            border: "1px solid rgba(254,176,106,0.28)",
                            padding: "14px 18px",
                            marginBottom: 16,
                            display: "flex",
                            alignItems: "flex-start",
                            gap: 12,
                        }}
                    >
                        <div
                            style={{
                                fontSize: 18,
                                flexShrink: 0,
                                marginTop: 1,
                            }}
                        >
                            ⚡
                        </div>
                        <div>
                            <div
                                style={{
                                    fontSize: 14,
                                    fontWeight: 800,
                                    color: "rgba(255,255,255,0.95)",
                                    marginBottom: 3,
                                }}
                            >
                                Read this before you apply.
                            </div>
                            <div
                                style={{
                                    fontSize: 12,
                                    color: "rgba(255,255,255,0.45)",
                                    lineHeight: "18px",
                                }}
                            >
                                Your strengths tell you what to lead with. Your
                                risks tell you what to address. This is how you
                                stand out — most applicants never do this work.
                            </div>
                        </div>
                    </div>
                )}

                {/* ── TWO COLUMN CARDS ── */}
                {(whyCount > 0 || riskCount > 0) && (
                    <div
                        style={{
                            display: "grid",
                            gridTemplateColumns:
                                whyCount > 0 && riskCount > 0
                                    ? "1fr 1fr"
                                    : "1fr",
                            gap: 14,
                            alignItems: "start",
                            marginBottom: 12,
                        }}
                    >
                        {/* ── WHY CARD ── */}
                        {(whyCount > 0 || !isPass) && (
                            <div
                                style={{
                                    borderRadius: 18,
                                    border: "1px solid rgba(74,222,128,0.22)",
                                    background: "#0D1829",
                                    overflow: "hidden",
                                    boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                                }}
                            >
                                {/* gradient top bar */}
                                <div
                                    style={{
                                        height: 3,
                                        background:
                                            "linear-gradient(90deg, #4ade80, #22c55e, #51ADE5)",
                                    }}
                                />
                                {/* header */}
                                <div
                                    style={{
                                        padding: "16px 20px 14px",
                                        borderBottom:
                                            "1px solid rgba(74,222,128,0.12)",
                                        background: "rgba(74,222,128,0.06)",
                                        display: "flex",
                                        alignItems: "center",
                                        gap: 10,
                                    }}
                                >
                                    <div
                                        style={{
                                            width: 32,
                                            height: 32,
                                            borderRadius: 10,
                                            flexShrink: 0,
                                            background: "rgba(74,222,128,0.15)",
                                            border: "1px solid rgba(74,222,128,0.35)",
                                            display: "flex",
                                            alignItems: "center",
                                            justifyContent: "center",
                                            fontSize: 15,
                                            color: "#4ade80",
                                        }}
                                    >
                                        ✦
                                    </div>
                                    <div>
                                        <div
                                            style={{
                                                fontSize: 14,
                                                fontWeight: 800,
                                                color: "#4ade80",
                                            }}
                                        >
                                            {isPass
                                                ? "Strengths to Remember"
                                                : "Why You Are Competitive"}
                                        </div>
                                        <div
                                            style={{
                                                fontSize: 11,
                                                color: "rgba(255,255,255,0.35)",
                                                marginTop: 2,
                                            }}
                                        >
                                            Lead with these in your application
                                        </div>
                                    </div>
                                </div>
                                {/* bullets */}
                                <div
                                    style={{
                                        padding: "14px 18px 16px",
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 14,
                                    }}
                                >
                                    {renderedBullets.length === 0 ? (
                                        <div
                                            style={{
                                                fontSize: 14,
                                                lineHeight: "22px",
                                                color: "rgba(255,255,255,0.40)",
                                                fontStyle: "italic" as const,
                                            }}
                                        >
                                            No direct proof matches were found
                                            for this role's core requirements.
                                            Your transferable skills may still
                                            apply — review the risks carefully
                                            before deciding whether to apply.
                                        </div>
                                    ) : (
                                        renderedBullets.map(
                                            (bullet: string, i: number) => {
                                                // Extract full phrase between "this role relies on" and ", and you"
                                                const mWhy = bullet.match(
                                                    /this role relies on (.+?),\s+and you\b/i
                                                )
                                                const keyword = mWhy
                                                    ? mWhy[1]
                                                          .trim()
                                                          .toUpperCase()
                                                    : ""
                                                return (
                                                    <div
                                                        key={i}
                                                        style={{
                                                            display: "flex",
                                                            alignItems:
                                                                "flex-start",
                                                            gap: 12,
                                                        }}
                                                    >
                                                        <div
                                                            style={{
                                                                width: 22,
                                                                height: 22,
                                                                borderRadius:
                                                                    "50%",
                                                                background:
                                                                    "rgba(74,222,128,0.15)",
                                                                border: "1.5px solid rgba(74,222,128,0.50)",
                                                                display: "flex",
                                                                alignItems:
                                                                    "center",
                                                                justifyContent:
                                                                    "center",
                                                                flexShrink: 0,
                                                                marginTop: 2,
                                                                fontSize: 11,
                                                                color: "#4ade80",
                                                                fontWeight: 900,
                                                                animation:
                                                                    "whyPulse 2.5s ease-in-out infinite",
                                                                animationDelay: `${i * 0.3}s`,
                                                            }}
                                                        >
                                                            ✓
                                                        </div>
                                                        <div
                                                            style={{ flex: 1 }}
                                                        >
                                                            {keyword ? (
                                                                <div
                                                                    style={{
                                                                        fontSize: 10,
                                                                        fontWeight: 900,
                                                                        letterSpacing:
                                                                            "1.4px",
                                                                        textTransform:
                                                                            "uppercase" as const,
                                                                        color: "#4ade80",
                                                                        marginBottom: 4,
                                                                    }}
                                                                >
                                                                    {keyword} |
                                                                </div>
                                                            ) : null}
                                                            <div
                                                                style={{
                                                                    fontSize: 13,
                                                                    lineHeight:
                                                                        "19px",
                                                                    color: "rgba(255,255,255,0.82)",
                                                                    fontWeight: 400,
                                                                }}
                                                            >
                                                                {bullet}
                                                            </div>
                                                        </div>
                                                    </div>
                                                )
                                            }
                                        )
                                    )}
                                </div>
                            </div>
                        )}

                        {/* ── RISK CARD ── */}
                        {riskCount > 0 && (
                            <div
                                style={{
                                    borderRadius: 18,
                                    border: "1px solid rgba(248,113,113,0.22)",
                                    background: "#0D1829",
                                    overflow: "hidden",
                                    boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                                }}
                            >
                                {/* gradient top bar */}
                                <div
                                    style={{
                                        height: 3,
                                        background:
                                            "linear-gradient(90deg, #f87171, #ef4444, #FEB06A)",
                                    }}
                                />
                                {/* header */}
                                <div
                                    style={{
                                        padding: "16px 20px 14px",
                                        borderBottom:
                                            "1px solid rgba(248,113,113,0.12)",
                                        background: "rgba(248,113,113,0.06)",
                                        display: "flex",
                                        alignItems: "center",
                                        gap: 10,
                                    }}
                                >
                                    <div
                                        style={{
                                            width: 32,
                                            height: 32,
                                            borderRadius: 10,
                                            flexShrink: 0,
                                            background:
                                                "rgba(248,113,113,0.15)",
                                            border: "1px solid rgba(248,113,113,0.35)",
                                            display: "flex",
                                            alignItems: "center",
                                            justifyContent: "center",
                                            fontSize: 15,
                                            color: "#f87171",
                                        }}
                                    >
                                        ⚠
                                    </div>
                                    <div>
                                        <div
                                            style={{
                                                fontSize: 14,
                                                fontWeight: 800,
                                                color: "#f87171",
                                            }}
                                        >
                                            {isForcePass
                                                ? "Why This Is a Pass"
                                                : "Your Risks"}
                                        </div>
                                        <div
                                            style={{
                                                fontSize: 11,
                                                color: "rgba(255,255,255,0.35)",
                                                marginTop: 2,
                                            }}
                                        >
                                            Address these before you apply
                                        </div>
                                    </div>
                                </div>
                                {/* risk bullets — use riskCodes for specific tool names */}
                                <div
                                    style={{
                                        padding: "14px 18px 16px",
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 14,
                                    }}
                                >
                                    {(jobFitResult?.risk_structured?.length > 0
                                        ? jobFitResult.risk_structured.map(
                                              (r: any) => ({
                                                  risk: r.reframe,
                                                  job_fact: r.keyword,
                                                  code: r.keyword,
                                              })
                                          )
                                        : riskCodes.length > 0
                                          ? riskCodes
                                          : renderedRisks.map((r: string) => ({
                                                risk: r,
                                                job_fact: "",
                                                code: "",
                                            }))
                                    )
                                        .filter(
                                            (
                                                rc: any,
                                                idx: number,
                                                arr: any[]
                                            ) => {
                                                const fact = String(
                                                    rc?.job_fact ?? ""
                                                )
                                                    .toLowerCase()
                                                    .trim()
                                                    .replace(
                                                        /^posting (lists?|requires?|calls for|mentions|prefers?)[:\s]*/i,
                                                        ""
                                                    )
                                                    .replace(
                                                        /\s+as (required|preferred)\.?$/i,
                                                        ""
                                                    )
                                                    .replace(
                                                        /\s+tool usage$/i,
                                                        ""
                                                    )
                                                    .replace(/\.?$/, "")
                                                    .trim()
                                                if (!fact) return true
                                                return (
                                                    arr.findIndex((x: any) => {
                                                        const xFact = String(
                                                            x?.job_fact ?? ""
                                                        )
                                                            .toLowerCase()
                                                            .trim()
                                                            .replace(
                                                                /^posting (lists?|requires?|calls for|mentions|prefers?)[:\s]*/i,
                                                                ""
                                                            )
                                                            .replace(
                                                                /\s+as (required|preferred)\.?$/i,
                                                                ""
                                                            )
                                                            .replace(
                                                                /\s+tool usage$/i,
                                                                ""
                                                            )
                                                            .replace(/\.?$/, "")
                                                            .trim()
                                                        return xFact === fact
                                                    }) === idx
                                                )
                                            }
                                        )
                                        .map((rc: any, i: number) => {
                                            const jobFact = String(
                                                rc?.job_fact ?? ""
                                            ).trim()
                                            const riskText = String(
                                                rc?.risk ?? rc ?? ""
                                            ).trim()
                                            const riskCode = String(
                                                rc?.code ?? ""
                                            )

                                            // Code-based keyword fallbacks when job_fact is empty
                                            const codeFallbacks: Record<
                                                string,
                                                string
                                            > = {
                                                RISK_EXPERIENCE:
                                                    "EXPERIENCE REQUIREMENT",
                                                RISK_SENIORITY_MISMATCH:
                                                    "SENIORITY LEVEL",
                                                RISK_MISSING_PROOF:
                                                    "MISSING PROOF",
                                                RISK_LOCATION:
                                                    "LOCATION CONSTRAINT",
                                                RISK_CONTRACT: "CONTRACT ROLE",
                                                RISK_SALES:
                                                    "SALES EXPECTATIONS",
                                                RISK_ANALYTICS_HEAVY:
                                                    "ANALYTICS REQUIREMENT",
                                                RISK_OVERQUALIFIED:
                                                    "OVERQUALIFIED",
                                            }

                                            // Build keyword: job_fact first, then code fallback
                                            const keywordFromFact = jobFact
                                                ? jobFact
                                                      .replace(
                                                          /^posting (lists?|requires?|calls for|mentions|prefers?|suggests?)[:\s]*/i,
                                                          ""
                                                      )
                                                      .replace(
                                                          /\s+as (required|preferred|a requirement)\.?$/i,
                                                          ""
                                                      )
                                                      .replace(/\.?$/, "")
                                                      .trim()
                                                      .toUpperCase()
                                                : ""
                                            const keyword =
                                                keywordFromFact ||
                                                codeFallbacks[riskCode] ||
                                                ""
                                            return (
                                                <div
                                                    key={i}
                                                    style={{
                                                        display: "flex",
                                                        alignItems:
                                                            "flex-start",
                                                        gap: 12,
                                                    }}
                                                >
                                                    <div
                                                        style={{
                                                            width: 22,
                                                            height: 22,
                                                            borderRadius: "50%",
                                                            background:
                                                                "rgba(248,113,113,0.15)",
                                                            border: "1.5px solid rgba(248,113,113,0.50)",
                                                            display: "flex",
                                                            alignItems:
                                                                "center",
                                                            justifyContent:
                                                                "center",
                                                            flexShrink: 0,
                                                            marginTop: 2,
                                                            fontSize: 11,
                                                            color: "#f87171",
                                                            fontWeight: 900,
                                                            animation:
                                                                "riskPulse 2.5s ease-in-out infinite",
                                                            animationDelay: `${i * 0.3}s`,
                                                        }}
                                                    >
                                                        !
                                                    </div>
                                                    <div style={{ flex: 1 }}>
                                                        {keyword ? (
                                                            <div
                                                                style={{
                                                                    fontSize: 10,
                                                                    fontWeight: 900,
                                                                    letterSpacing:
                                                                        "1.4px",
                                                                    textTransform:
                                                                        "uppercase" as const,
                                                                    color: "#f87171",
                                                                    marginBottom: 4,
                                                                }}
                                                            >
                                                                {keyword} |
                                                            </div>
                                                        ) : null}
                                                        <div
                                                            style={{
                                                                fontSize: 13,
                                                                lineHeight:
                                                                    "19px",
                                                                color: "rgba(255,255,255,0.82)",
                                                                fontWeight: 400,
                                                            }}
                                                        >
                                                            {riskText}
                                                        </div>
                                                    </div>
                                                </div>
                                            )
                                        })}
                                </div>
                            </div>
                        )}
                    </div>
                )}
            </>
        )
    }

    /* ---------- RENDER: POSITIONING ---------- */
    /* ---------- RENDER: POSITIONING ---------- */
    function renderPositioning() {
        if (accessMode === "jobfit_only")
            return renderLockedPreview("positioning")

        const r = positioningResult?.result ?? positioningResult ?? null

        if (!r) {
            return (
                <>
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(81,173,229,0.20)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 16,
                            marginTop: 4,
                            position: "relative" as const,
                        }}
                    >
                        <div
                            style={{
                                position: "absolute" as const,
                                top: -40,
                                left: -40,
                                width: 220,
                                height: 220,
                                borderRadius: "50%",
                                background: "rgba(81,173,229,0.07)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                position: "absolute" as const,
                                bottom: -20,
                                right: -20,
                                width: 180,
                                height: 180,
                                borderRadius: "50%",
                                background: "rgba(254,176,106,0.06)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#FEB06A,#f97316,#51ADE5)",
                            }}
                        />
                        <div
                            style={{
                                padding: "28px 32px 32px",
                                position: "relative" as const,
                                zIndex: 1,
                                textAlign: "center" as const,
                            }}
                        >
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: "rgba(254,176,106,0.70)",
                                    marginBottom: 16,
                                }}
                            >
                                Step 2 of 4
                            </div>
                            <div
                                style={{
                                    fontSize: 38,
                                    fontWeight: 950,
                                    color: "rgba(255,255,255,0.97)",
                                    lineHeight: 1.1,
                                    letterSpacing: -1.2,
                                    marginBottom: 10,
                                }}
                            >
                                Your resume,{" "}
                                <span style={{ color: "#FEB06A" }}>
                                    rewritten
                                </span>
                                <br />
                                for this exact job.
                            </div>
                            <div
                                style={{
                                    fontSize: 22,
                                    fontWeight: 950,
                                    letterSpacing: -0.5,
                                    marginBottom: 18,
                                    lineHeight: 1.2,
                                }}
                            >
                                <span style={{ color: "#FEB06A" }}>
                                    One targeted story.
                                </span>
                                <span
                                    style={{ color: "rgba(255,255,255,0.90)" }}
                                >
                                    {" "}
                                    Not a template.
                                </span>
                            </div>
                            <div
                                style={{
                                    fontSize: 14,
                                    lineHeight: "22px",
                                    color: "rgba(255,255,255,0.45)",
                                    margin: "0 auto 28px",
                                    maxWidth: 500,
                                }}
                            >
                                Most resumes fail because they say the right
                                things in the wrong language. SIGNAL rewrites
                                your bullets to match this specific role —
                                before a recruiter decides in 7 seconds that
                                you're not a fit.
                            </div>
                            <div
                                style={{
                                    display: "flex",
                                    flexDirection: "column" as const,
                                    gap: 10,
                                    marginBottom: 22,
                                    textAlign: "left" as const,
                                }}
                            >
                                {[
                                    {
                                        n: "1",
                                        head: "Run JobFit",
                                        body: "Paste the job description and get a clear verdict — Apply, Review, or Pass — before you spend a minute on an application.",
                                    },
                                    {
                                        n: "2",
                                        head: "Run Positioning",
                                        body: "Rewrite your resume bullets to match this specific role. One targeted story, in the language the recruiter is scanning for.",
                                    },
                                    {
                                        n: "3",
                                        head: "Run Cover Letter",
                                        body: "Write a letter in your voice that connects your background to this company — not a template, a real case for why you fit.",
                                    },
                                    {
                                        n: "4",
                                        head: "Run Networking",
                                        body: "Build your outreach plan. Know who to contact, what to say, and how to follow up so your name is known before the interview.",
                                    },
                                ].map((s, idx) => {
                                    const isActive = idx === 1
                                    const isDone = idx < 1
                                    return (
                                        <div
                                            key={s.n}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 14,
                                                opacity: isActive
                                                    ? 1
                                                    : isDone
                                                      ? 0.55
                                                      : 0.3,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: isActive ? 36 : 28,
                                                    height: isActive ? 36 : 28,
                                                    borderRadius: "50%",
                                                    background: isActive
                                                        ? "rgba(254,176,106,0.25)"
                                                        : isDone
                                                          ? "rgba(74,222,128,0.15)"
                                                          : "rgba(255,255,255,0.06)",
                                                    border: isActive
                                                        ? "2px solid rgba(254,176,106,0.70)"
                                                        : isDone
                                                          ? "2px solid rgba(74,222,128,0.50)"
                                                          : "2px solid rgba(255,255,255,0.15)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: isActive
                                                        ? 15
                                                        : 12,
                                                    fontWeight: 900,
                                                    color: isActive
                                                        ? "#FEB06A"
                                                        : isDone
                                                          ? "#4ade80"
                                                          : "rgba(255,255,255,0.35)",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                {s.n}
                                            </div>
                                            <div>
                                                <div
                                                    style={{
                                                        fontSize: isActive
                                                            ? 16
                                                            : 13,
                                                        fontWeight: isActive
                                                            ? 900
                                                            : 700,
                                                        color: isActive
                                                            ? "#FEB06A"
                                                            : isDone
                                                              ? "rgba(255,255,255,0.75)"
                                                              : "rgba(255,255,255,0.40)",
                                                        marginBottom: 3,
                                                    }}
                                                >
                                                    {s.head}
                                                </div>
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: isActive
                                                            ? "rgba(255,255,255,0.55)"
                                                            : "rgba(255,255,255,0.22)",
                                                    }}
                                                >
                                                    {s.body}
                                                </div>
                                            </div>
                                        </div>
                                    )
                                })}
                            </div>
                            <div
                                style={{
                                    borderRadius: 14,
                                    border: "1px solid rgba(74,222,128,0.18)",
                                    background: "rgba(74,222,128,0.05)",
                                    padding: "14px 16px",
                                    textAlign: "left" as const,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 2,
                                        textTransform: "uppercase" as const,
                                        color: "rgba(74,222,128,0.65)",
                                        marginBottom: 10,
                                    }}
                                >
                                    What Positioning gives you
                                </div>
                                <div
                                    style={{
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 8,
                                    }}
                                >
                                    {[
                                        "Rewritten resume bullets matched to this specific job",
                                        "Language that mirrors what the job description actually says",
                                        "Proof-first bullets that lead with outcomes, not responsibilities",
                                        "A narrative that positions you as the obvious candidate",
                                    ].map((item, i) => (
                                        <div
                                            key={i}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 10,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: 18,
                                                    height: 18,
                                                    borderRadius: "50%",
                                                    background:
                                                        "rgba(74,222,128,0.15)",
                                                    border: "1.5px solid rgba(74,222,128,0.45)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: 10,
                                                    fontWeight: 900,
                                                    color: "#4ade80",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                ✓
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 12,
                                                    lineHeight: "18px",
                                                    color: "rgba(255,255,255,0.60)",
                                                }}
                                            >
                                                {item}
                                            </div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                    {renderPrimaryRunButton(
                        "Run Positioning →",
                        runPositioning,
                        loadingPositioning
                    )}
                </>
            )
        }

        const intro = String(r.student_intro ?? r.studentIntro ?? "").trim()

        const drivers: string[] = (() => {
            const d = pickFirstArray(
                r.primary_match_drivers,
                r.primaryMatchDrivers,
                r.role_angle?.evidence
            )
            return d
                .map(toSafeLine)
                .map((s) => s.trim())
                .filter(Boolean)
        })()

        const editsRaw: any[] = (() => {
            if (Array.isArray(r.resume_bullet_edits))
                return r.resume_bullet_edits
            if (Array.isArray(r.resumeBulletEdits)) return r.resumeBulletEdits
            if (Array.isArray(r.bullet_edits)) return r.bullet_edits
            if (Array.isArray(r.bulletEdits)) return r.bulletEdits
            return []
        })()

        const pickText = (obj: any, keys: string[]) => {
            for (const k of keys) {
                const v = obj?.[k]
                if (typeof v === "string" && v.trim()) return v.trim()
            }
            return ""
        }

        const normalizedEdits = editsRaw
            .map((e: any) => {
                if (typeof e === "string")
                    return { before: "", after: e.trim(), why: "" }
                const before = pickText(e, [
                    "before",
                    "before_bullet",
                    "beforeBullet",
                    "original",
                    "original_bullet",
                    "originalBullet",
                ])
                const after = pickText(e, [
                    "after",
                    "after_bullet",
                    "afterBullet",
                    "revised",
                    "revised_bullet",
                    "revisedBullet",
                    "updated",
                    "updated_bullet",
                    "updatedBullet",
                    "new",
                    "new_bullet",
                    "newBullet",
                ])
                const why = pickText(e, [
                    "why",
                    "rationale",
                    "reason",
                    "explanation",
                    "notes",
                ])
                return { before, after, why }
            })
            .filter((x) => (x.before || x.after) && x.before !== x.after)

        const s = r.summary_statement ?? r.summaryStatement ?? null

        // Section header helper
        function PosSection({
            label,
            count,
        }: {
            label: string
            count?: number
        }) {
            return (
                <div
                    style={{
                        display: "flex",
                        alignItems: "center",
                        gap: 10,
                        margin: "22px 0 12px",
                    }}
                >
                    <div
                        style={{
                            fontSize: 9,
                            fontWeight: 900,
                            letterSpacing: 2,
                            textTransform: "uppercase" as const,
                            color: "rgba(255,255,255,0.32)",
                            flexShrink: 0,
                        }}
                    >
                        {label}
                    </div>
                    <div
                        style={{
                            flex: 1,
                            height: 1,
                            background: "rgba(255,255,255,0.07)",
                        }}
                    />
                    {count !== undefined ? (
                        <div
                            style={{
                                fontSize: 9,
                                fontWeight: 900,
                                padding: "2px 8px",
                                borderRadius: 999,
                                background: "rgba(255,255,255,0.06)",
                                color: "rgba(255,255,255,0.25)",
                                flexShrink: 0,
                            }}
                        >
                            {count} {count === 1 ? "signal" : "signals"}
                        </div>
                    ) : null}
                </div>
            )
        }

        return (
            <>
                {/* ── POSITIONING ANALYSIS HEADER ── */}
                {intro ? (
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(81,173,229,0.20)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 20,
                            boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                        }}
                    >
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#51ADE5,#218C8C,#FEB06A)",
                            }}
                        />
                        <div style={{ padding: "20px 24px" }}>
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: "rgba(81,173,229,0.65)",
                                    marginBottom: 10,
                                }}
                            >
                                Positioning Analysis
                            </div>
                            <div
                                style={{
                                    fontSize: 15,
                                    lineHeight: "24px",
                                    color: "rgba(255,255,255,0.88)",
                                }}
                            >
                                {intro}
                            </div>
                        </div>
                    </div>
                ) : null}

                {/* ── STEP TRACK ── */}
                <div
                    style={{
                        display: "flex",
                        alignItems: "center",
                        gap: 10,
                        marginBottom: 20,
                        padding: "0 4px",
                    }}
                >
                    <div
                        style={{
                            display: "flex",
                            alignItems: "center",
                            gap: 8,
                        }}
                    >
                        <div
                            style={{
                                width: 10,
                                height: 10,
                                borderRadius: "50%",
                                background: "#FEB06A",
                                boxShadow: "0 0 8px #FEB06A",
                            }}
                        />
                        <div
                            style={{
                                fontSize: 11,
                                color: "rgba(255,255,255,0.40)",
                                whiteSpace: "nowrap" as const,
                            }}
                        >
                            Step 1 — Optimize Bullets
                        </div>
                    </div>
                    <div
                        style={{
                            flex: 1,
                            height: 1,
                            background: "rgba(255,255,255,0.08)",
                        }}
                    />
                    <div
                        style={{
                            display: "flex",
                            alignItems: "center",
                            gap: 8,
                        }}
                    >
                        <div
                            style={{
                                width: 10,
                                height: 10,
                                borderRadius: "50%",
                                background: "#51ADE5",
                                boxShadow: "0 0 8px #51ADE5",
                            }}
                        />
                        <div
                            style={{
                                fontSize: 11,
                                color: "rgba(255,255,255,0.40)",
                                whiteSpace: "nowrap" as const,
                            }}
                        >
                            Step 2 — Update Summary
                        </div>
                    </div>
                    <div
                        style={{
                            flex: 1,
                            height: 1,
                            background: "rgba(255,255,255,0.08)",
                        }}
                    />
                    <div
                        style={{
                            display: "flex",
                            alignItems: "center",
                            gap: 8,
                        }}
                    >
                        <div
                            style={{
                                width: 10,
                                height: 10,
                                borderRadius: "50%",
                                background: "#4ade80",
                                boxShadow: "0 0 8px #4ade80",
                            }}
                        />
                        <div
                            style={{
                                fontSize: 11,
                                color: "rgba(255,255,255,0.40)",
                                whiteSpace: "nowrap" as const,
                            }}
                        >
                            Why it works
                        </div>
                    </div>
                </div>

                {/* ── STEP 1: OPTIMIZE KEY BULLETS ── */}
                {normalizedEdits.length > 0 ? (
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(254,176,106,0.30)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 14,
                            boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                        }}
                    >
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#FEB06A,#f97316,#FEB06A)",
                            }}
                        />
                        <div
                            style={{
                                padding: "18px 22px 16px",
                                borderBottom:
                                    "1px solid rgba(254,176,106,0.12)",
                                background: "rgba(254,176,106,0.06)",
                                display: "flex",
                                alignItems: "center",
                                gap: 12,
                            }}
                        >
                            <div
                                style={{
                                    width: 36,
                                    height: 36,
                                    borderRadius: "50%",
                                    flexShrink: 0,
                                    background: "rgba(254,176,106,0.15)",
                                    border: "2px solid rgba(254,176,106,0.50)",
                                    display: "flex",
                                    alignItems: "center",
                                    justifyContent: "center",
                                    fontSize: 16,
                                    fontWeight: 900,
                                    color: "#FEB06A",
                                }}
                            >
                                1
                            </div>
                            <div style={{ flex: 1 }}>
                                <div
                                    style={{
                                        display: "flex",
                                        alignItems: "center",
                                        gap: 8,
                                        marginBottom: 3,
                                    }}
                                >
                                    <div
                                        style={{
                                            fontSize: 13,
                                            fontWeight: 900,
                                            color: "#FEB06A",
                                        }}
                                    >
                                        Do This First
                                    </div>
                                    <div
                                        style={{
                                            padding: "3px 10px",
                                            borderRadius: 999,
                                            background:
                                                "rgba(254,176,106,0.12)",
                                            border: "1px solid rgba(254,176,106,0.25)",
                                            fontSize: 10,
                                            fontWeight: 900,
                                            color: "#FEB06A",
                                        }}
                                    >
                                        Start here
                                    </div>
                                </div>
                                <div
                                    style={{
                                        fontSize: 16,
                                        fontWeight: 900,
                                        color: "rgba(255,255,255,0.95)",
                                    }}
                                >
                                    Optimize Key Bullets
                                </div>
                                <div
                                    style={{
                                        fontSize: 12,
                                        color: "rgba(255,255,255,0.40)",
                                        marginTop: 3,
                                    }}
                                >
                                    Copy these rewrites into your resume — they
                                    match the exact language this hiring manager
                                    is scanning for
                                </div>
                            </div>
                            <div
                                style={{
                                    fontSize: 10,
                                    fontWeight: 700,
                                    padding: "3px 10px",
                                    borderRadius: 999,
                                    background: "rgba(254,176,106,0.12)",
                                    color: "#FEB06A",
                                    border: "1px solid rgba(254,176,106,0.25)",
                                    flexShrink: 0,
                                }}
                            >
                                {normalizedEdits.length}{" "}
                                {normalizedEdits.length === 1
                                    ? "edit"
                                    : "edits"}
                            </div>
                        </div>

                        <div
                            style={{
                                padding: "14px 16px",
                                display: "flex",
                                flexDirection: "column" as const,
                                gap: 10,
                            }}
                        >
                            {normalizedEdits.map((b: any, i: number) => (
                                <div
                                    key={i}
                                    style={{
                                        borderRadius: 14,
                                        border: "1px solid rgba(255,255,255,0.08)",
                                        background: "rgba(255,255,255,0.03)",
                                        overflow: "hidden",
                                    }}
                                >
                                    <div
                                        style={{
                                            display: "grid",
                                            gridTemplateColumns: "1fr 1fr",
                                        }}
                                    >
                                        <div
                                            style={{
                                                padding: "14px 16px",
                                                borderRight:
                                                    "1px solid rgba(255,255,255,0.06)",
                                            }}
                                        >
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 1.4,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(255,255,255,0.25)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                Before
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 13,
                                                    lineHeight: "20px",
                                                    color: "rgba(255,255,255,0.40)",
                                                }}
                                            >
                                                {b.before || "—"}
                                            </div>
                                        </div>
                                        <div
                                            style={{
                                                padding: "14px 16px",
                                                background:
                                                    "rgba(74,222,128,0.06)",
                                            }}
                                        >
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 1.4,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "#4ade80",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                After
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 13,
                                                    lineHeight: "20px",
                                                    color: "rgba(255,255,255,0.88)",
                                                }}
                                            >
                                                {b.after
                                                    ? renderDiffTeal(
                                                          String(
                                                              b.before || ""
                                                          ),
                                                          String(b.after || "")
                                                      )
                                                    : "—"}
                                            </div>
                                            {b.after && (
                                                <button
                                                    onClick={() => copyText(String(b.after), `bullet-${i}`)}
                                                    type="button"
                                                    style={{
                                                        marginTop: 8,
                                                        padding: "5px 12px",
                                                        borderRadius: 8,
                                                        fontSize: 11,
                                                        fontWeight: 900,
                                                        cursor: "pointer",
                                                        border: wasCopied(`bullet-${i}`) ? "1px solid rgba(74,222,128,0.40)" : "1px solid rgba(255,255,255,0.12)",
                                                        background: wasCopied(`bullet-${i}`) ? "rgba(74,222,128,0.15)" : "rgba(255,255,255,0.07)",
                                                        color: wasCopied(`bullet-${i}`) ? "#4ade80" : "rgba(255,255,255,0.50)",
                                                    }}
                                                >
                                                    {wasCopied(`bullet-${i}`) ? "Copied ✓" : "⎘ Copy bullet"}
                                                </button>
                                            )}
                                        </div>
                                    </div>
                                    {b.why ? (
                                        <div
                                            style={{
                                                padding: "9px 16px",
                                                borderTop:
                                                    "1px solid rgba(254,176,106,0.10)",
                                                background:
                                                    "rgba(254,176,106,0.04)",
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 8,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 1,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(254,176,106,0.70)",
                                                    flexShrink: 0,
                                                    paddingTop: 2,
                                                }}
                                            >
                                                Why
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 12,
                                                    color: "rgba(255,255,255,0.50)",
                                                    lineHeight: "18px",
                                                }}
                                            >
                                                {b.why}
                                            </div>
                                        </div>
                                    ) : null}
                                </div>
                            ))}
                        </div>
                    </div>
                ) : (
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(74,222,128,0.25)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 14,
                        }}
                    >
                        <div style={{ height: 3, background: "linear-gradient(90deg,#4ade80,#22c55e,#4ade80)" }} />
                        <div style={{ padding: "18px 22px 16px", borderBottom: "1px solid rgba(74,222,128,0.12)", background: "rgba(74,222,128,0.06)", display: "flex", alignItems: "center", gap: 12 }}>
                            <div style={{ width: 36, height: 36, borderRadius: "50%", flexShrink: 0, background: "rgba(74,222,128,0.15)", border: "2px solid rgba(74,222,128,0.50)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 16, fontWeight: 900, color: "#4ade80" }}>✓</div>
                            <div style={{ flex: 1 }}>
                                <div style={{ fontSize: 16, fontWeight: 900, color: "rgba(255,255,255,0.95)" }}>Optimize Key Bullets</div>
                                <div style={{ fontSize: 12, color: "rgba(255,255,255,0.40)", marginTop: 3 }}>No changes needed</div>
                            </div>
                            <div style={{ fontSize: 10, fontWeight: 700, padding: "3px 10px", borderRadius: 999, background: "rgba(74,222,128,0.12)", color: "#4ade80", border: "1px solid rgba(74,222,128,0.25)", flexShrink: 0 }}>Aligned</div>
                        </div>
                        <div style={{ padding: "18px 22px" }}>
                            <div style={{ fontSize: 14, fontWeight: 700, color: "#4ade80", marginBottom: 6 }}>Your resume bullets are aligned well for this job.</div>
                            <div style={{ fontSize: 13, color: "rgba(255,255,255,0.45)", lineHeight: "20px" }}>SIGNAL didn't find any bullets that need rewriting. Your experience descriptions already match what this role is looking for.</div>
                        </div>
                    </div>
                )}

                {/* ── STEP 2: UPDATE YOUR SUMMARY ── */}
                <div
                    style={{
                        borderRadius: 18,
                        border: "1px solid rgba(81,173,229,0.25)",
                        background: "#0D1829",
                        overflow: "hidden",
                        marginBottom: 14,
                        boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                    }}
                >
                    <div
                        style={{
                            height: 3,
                            background:
                                "linear-gradient(90deg,#51ADE5,#218C8C,#51ADE5)",
                        }}
                    />
                    <div
                        style={{
                            padding: "18px 22px 16px",
                            borderBottom: "1px solid rgba(81,173,229,0.12)",
                            background: "rgba(81,173,229,0.06)",
                            display: "flex",
                            alignItems: "center",
                            gap: 12,
                        }}
                    >
                        <div
                            style={{
                                width: 36,
                                height: 36,
                                borderRadius: "50%",
                                flexShrink: 0,
                                background: "rgba(81,173,229,0.15)",
                                border: "2px solid rgba(81,173,229,0.40)",
                                display: "flex",
                                alignItems: "center",
                                justifyContent: "center",
                                fontSize: 16,
                                fontWeight: 900,
                                color: "#51ADE5",
                            }}
                        >
                            2
                        </div>
                        <div style={{ flex: 1 }}>
                            <div
                                style={{
                                    fontSize: 13,
                                    fontWeight: 900,
                                    color: "#51ADE5",
                                    marginBottom: 3,
                                }}
                            >
                                Then Do This
                            </div>
                            <div
                                style={{
                                    fontSize: 16,
                                    fontWeight: 900,
                                    color: "rgba(255,255,255,0.95)",
                                }}
                            >
                                Align Your Summary Statement
                            </div>
                            <div
                                style={{
                                    fontSize: 12,
                                    color: "rgba(255,255,255,0.40)",
                                    marginTop: 3,
                                    lineHeight: "17px",
                                }}
                            >
                                A summary is only suggested when your resume
                                isn't telling one clear story. It helps connect
                                your background to this specific role when that
                                connection isn't immediately obvious.
                            </div>
                        </div>
                    </div>

                    <div style={{ padding: "18px 22px" }}>
                        {typeof s === "string" && s.trim() ? (
                            <div
                                style={{
                                    fontSize: 14,
                                    lineHeight: "22px",
                                    color: "rgba(255,255,255,0.75)",
                                }}
                            >
                                {s.trim()}
                            </div>
                        ) : !s || typeof s !== "object" ? (
                            <div
                                style={{
                                    fontSize: 14,
                                    color: "rgba(255,255,255,0.35)",
                                }}
                            >
                                No summary guidance was generated for this job.
                            </div>
                        ) : (
                            <>
                                {/* Need summary pill */}
                                <div
                                    style={{
                                        display: "inline-flex",
                                        alignItems: "center",
                                        gap: 6,
                                        padding: "5px 14px",
                                        borderRadius: 999,
                                        marginBottom: 16,
                                        background:
                                            String(
                                                s.need_summary ??
                                                    s.needSummary ??
                                                    ""
                                            ).trim() === "NO"
                                                ? "rgba(74,222,128,0.10)"
                                                : "rgba(254,176,106,0.10)",
                                        border:
                                            String(
                                                s.need_summary ??
                                                    s.needSummary ??
                                                    ""
                                            ).trim() === "NO"
                                                ? "1px solid rgba(74,222,128,0.25)"
                                                : "1px solid rgba(254,176,106,0.25)",
                                        fontSize: 11,
                                        fontWeight: 900,
                                        color:
                                            String(
                                                s.need_summary ??
                                                    s.needSummary ??
                                                    ""
                                            ).trim() === "NO"
                                                ? "#4ade80"
                                                : "#FEB06A",
                                    }}
                                >
                                    {String(
                                        s.need_summary ?? s.needSummary ?? ""
                                    ).trim() === "NO"
                                        ? "✓ Your current summary works for this role"
                                        : "✎ Swap in this summary before you apply"}
                                </div>

                                {String(s.why ?? "").trim() ? (
                                    <div
                                        style={{
                                            fontSize: 13,
                                            lineHeight: "20px",
                                            color: "rgba(255,255,255,0.50)",
                                            marginBottom: 16,
                                        }}
                                    >
                                        {String(s.why).trim()}
                                    </div>
                                ) : null}

                                {(s.recommended_summary ??
                                s.recommendedSummary) ? (
                                    <div>
                                        <div
                                            style={{
                                                fontSize: 9,
                                                fontWeight: 900,
                                                letterSpacing: 1.5,
                                                textTransform:
                                                    "uppercase" as const,
                                                color: "#51ADE5",
                                                marginBottom: 8,
                                            }}
                                        >
                                            Resume Summary
                                        </div>
                                        <div
                                            style={{
                                                fontSize: 14,
                                                lineHeight: "22px",
                                                color: "rgba(255,255,255,0.88)",
                                                padding: "14px 16px",
                                                background:
                                                    "rgba(81,173,229,0.06)",
                                                borderRadius: 12,
                                                border: "1px solid rgba(81,173,229,0.18)",
                                            }}
                                        >
                                            {String(
                                                s.recommended_summary ??
                                                    s.recommendedSummary
                                            )}
                                        </div>
                                        <button
                                            onClick={() => copyText(String(s.recommended_summary ?? s.recommendedSummary ?? ""), "pos-summary")}
                                            type="button"
                                            style={{
                                                marginTop: 10,
                                                padding: "6px 14px",
                                                borderRadius: 8,
                                                fontSize: 11,
                                                fontWeight: 900,
                                                cursor: "pointer",
                                                border: wasCopied("pos-summary") ? "1px solid rgba(81,173,229,0.50)" : "1px solid rgba(255,255,255,0.12)",
                                                background: wasCopied("pos-summary") ? "rgba(81,173,229,0.15)" : "rgba(255,255,255,0.07)",
                                                color: wasCopied("pos-summary") ? "#51ADE5" : "rgba(255,255,255,0.50)",
                                            }}
                                        >
                                            {wasCopied("pos-summary") ? "Copied ✓" : "⎘ Copy summary"}
                                        </button>
                                    </div>
                                ) : null}

                                {Array.isArray(s.evidence) &&
                                s.evidence.length ? (
                                    <div
                                        style={{
                                            marginTop: 16,
                                            paddingTop: 14,
                                            borderTop:
                                                "1px solid rgba(255,255,255,0.07)",
                                        }}
                                    >
                                        <div
                                            style={{
                                                fontSize: 9,
                                                fontWeight: 900,
                                                letterSpacing: 1.5,
                                                textTransform:
                                                    "uppercase" as const,
                                                color: "rgba(255,255,255,0.28)",
                                                marginBottom: 10,
                                            }}
                                        >
                                            Supporting evidence
                                        </div>
                                        {s.evidence.map((e: any, i: number) => (
                                            <div
                                                key={i}
                                                style={{
                                                    display: "flex",
                                                    alignItems: "flex-start",
                                                    gap: 10,
                                                    marginBottom: 8,
                                                }}
                                            >
                                                <div
                                                    style={{
                                                        width: 5,
                                                        height: 5,
                                                        borderRadius: "50%",
                                                        background:
                                                            "rgba(81,173,229,0.50)",
                                                        flexShrink: 0,
                                                        marginTop: 7,
                                                    }}
                                                />
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: "rgba(255,255,255,0.50)",
                                                    }}
                                                >
                                                    {toSafeLine(e)}
                                                </div>
                                            </div>
                                        ))}
                                    </div>
                                ) : null}
                            </>
                        )}
                    </div>
                </div>

                {/* ── WHY IT WORKS — PRIMARY MATCH DRIVERS ── */}
                {drivers.length > 0 ? (
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(74,222,128,0.20)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 14,
                            boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                        }}
                    >
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#4ade80,#22c55e,#51ADE5)",
                            }}
                        />
                        <div
                            style={{
                                padding: "18px 22px 16px",
                                borderBottom: "1px solid rgba(74,222,128,0.10)",
                                background: "rgba(74,222,128,0.05)",
                                display: "flex",
                                alignItems: "center",
                                gap: 12,
                            }}
                        >
                            <div
                                style={{
                                    width: 36,
                                    height: 36,
                                    borderRadius: "50%",
                                    flexShrink: 0,
                                    background: "rgba(74,222,128,0.12)",
                                    border: "2px solid rgba(74,222,128,0.35)",
                                    display: "flex",
                                    alignItems: "center",
                                    justifyContent: "center",
                                    fontSize: 16,
                                    fontWeight: 900,
                                    color: "#4ade80",
                                }}
                            >
                                ✦
                            </div>
                            <div style={{ flex: 1 }}>
                                <div
                                    style={{
                                        fontSize: 13,
                                        fontWeight: 900,
                                        color: "#4ade80",
                                        marginBottom: 3,
                                    }}
                                >
                                    Why These Changes Work
                                </div>
                                <div
                                    style={{
                                        fontSize: 16,
                                        fontWeight: 900,
                                        color: "rgba(255,255,255,0.95)",
                                    }}
                                >
                                    Your Strongest Signals for This Role
                                </div>
                                <div
                                    style={{
                                        fontSize: 12,
                                        color: "rgba(255,255,255,0.40)",
                                        marginTop: 3,
                                    }}
                                >
                                    These are the exact reasons you are
                                    competitive here — mention them in your
                                    cover letter and interview
                                </div>
                            </div>
                            <div
                                style={{
                                    fontSize: 10,
                                    fontWeight: 700,
                                    padding: "3px 10px",
                                    borderRadius: 999,
                                    background: "rgba(74,222,128,0.10)",
                                    color: "#4ade80",
                                    border: "1px solid rgba(74,222,128,0.22)",
                                    flexShrink: 0,
                                }}
                            >
                                {drivers.length} signals
                            </div>
                        </div>
                        <div
                            style={{
                                padding: "14px 18px 16px",
                                display: "flex",
                                flexDirection: "column" as const,
                                gap: 10,
                            }}
                        >
                            {drivers.map((d, i) => (
                                <div
                                    key={i}
                                    style={{
                                        display: "flex",
                                        alignItems: "flex-start",
                                        gap: 12,
                                    }}
                                >
                                    <div
                                        style={{
                                            width: 22,
                                            height: 22,
                                            borderRadius: "50%",
                                            background: "rgba(74,222,128,0.12)",
                                            border: "1.5px solid rgba(74,222,128,0.40)",
                                            display: "flex",
                                            alignItems: "center",
                                            justifyContent: "center",
                                            flexShrink: 0,
                                            marginTop: 1,
                                            fontSize: 11,
                                            color: "#4ade80",
                                            fontWeight: 900,
                                        }}
                                    >
                                        ✓
                                    </div>
                                    <div
                                        style={{
                                            fontSize: 14,
                                            lineHeight: "22px",
                                            color: "rgba(255,255,255,0.85)",
                                        }}
                                    >
                                        {d}
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                ) : null}

                {/* ── CTA ── */}
                <div style={{ marginTop: 20 }}>
                    <button
                        style={{ ...jobfitHeroBtn, width: "100%" }}
                        onClick={() => goTo("coverletter")}
                        type="button"
                    >
                        Proceed to Cover Letter →
                    </button>
                </div>
            </>
        )
    }

    /* ---------- RENDER: COVER LETTER ---------- */
    function renderCoverLetter() {
        if (accessMode === "jobfit_only")
            return renderLockedPreview("coverletter")

        if (!coverLetterResult) {
            return (
                <>
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(81,173,229,0.20)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 16,
                            marginTop: 4,
                            position: "relative" as const,
                        }}
                    >
                        <div
                            style={{
                                position: "absolute" as const,
                                top: -40,
                                left: -40,
                                width: 220,
                                height: 220,
                                borderRadius: "50%",
                                background: "rgba(81,173,229,0.07)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                position: "absolute" as const,
                                bottom: -20,
                                right: -20,
                                width: 180,
                                height: 180,
                                borderRadius: "50%",
                                background: "rgba(254,176,106,0.06)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#FEB06A,#f97316,#51ADE5)",
                            }}
                        />
                        <div
                            style={{
                                padding: "28px 32px 32px",
                                position: "relative" as const,
                                zIndex: 1,
                                textAlign: "center" as const,
                            }}
                        >
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: "rgba(254,176,106,0.70)",
                                    marginBottom: 16,
                                }}
                            >
                                Step 3 of 4
                            </div>
                            <div
                                style={{
                                    fontSize: 38,
                                    fontWeight: 950,
                                    color: "rgba(255,255,255,0.97)",
                                    lineHeight: 1.1,
                                    letterSpacing: -1.2,
                                    marginBottom: 10,
                                }}
                            >
                                A cover letter written
                                <br />
                                in{" "}
                                <span style={{ color: "#FEB06A" }}>
                                    your voice.
                                </span>
                            </div>
                            <div
                                style={{
                                    fontSize: 22,
                                    fontWeight: 950,
                                    letterSpacing: -0.5,
                                    marginBottom: 18,
                                    lineHeight: 1.2,
                                }}
                            >
                                <span style={{ color: "#FEB06A" }}>
                                    Not a template.
                                </span>
                                <span
                                    style={{ color: "rgba(255,255,255,0.90)" }}
                                >
                                    {" "}
                                    Your story, this role.
                                </span>
                            </div>
                            <div
                                style={{
                                    fontSize: 14,
                                    lineHeight: "22px",
                                    color: "rgba(255,255,255,0.45)",
                                    margin: "0 auto 28px",
                                    maxWidth: 500,
                                }}
                            >
                                Generic cover letters get ignored. SIGNAL writes
                                one letter — built from your actual background,
                                matched to this specific company and role. No
                                filler. No resume repeat. Just a clear case for
                                why you belong here.
                            </div>
                            <div
                                style={{
                                    display: "flex",
                                    flexDirection: "column" as const,
                                    gap: 10,
                                    marginBottom: 22,
                                    textAlign: "left" as const,
                                }}
                            >
                                {[
                                    {
                                        n: "1",
                                        head: "Run JobFit",
                                        body: "Paste the job description and get a clear verdict — Apply, Review, or Pass — before you spend a minute on an application.",
                                    },
                                    {
                                        n: "2",
                                        head: "Run Positioning",
                                        body: "Rewrite your resume bullets to match this specific role. One targeted story, in the language the recruiter is scanning for.",
                                    },
                                    {
                                        n: "3",
                                        head: "Run Cover Letter",
                                        body: "Write a letter in your voice that connects your background to this company — not a template, a real case for why you fit.",
                                    },
                                    {
                                        n: "4",
                                        head: "Run Networking",
                                        body: "Build your outreach plan. Know who to contact, what to say, and how to follow up so your name is known before the interview.",
                                    },
                                ].map((s, idx) => {
                                    const isActive = idx === 2
                                    const isDone = idx < 2
                                    return (
                                        <div
                                            key={s.n}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 14,
                                                opacity: isActive
                                                    ? 1
                                                    : isDone
                                                      ? 0.55
                                                      : 0.3,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: isActive ? 36 : 28,
                                                    height: isActive ? 36 : 28,
                                                    borderRadius: "50%",
                                                    background: isActive
                                                        ? "rgba(254,176,106,0.25)"
                                                        : isDone
                                                          ? "rgba(74,222,128,0.15)"
                                                          : "rgba(255,255,255,0.06)",
                                                    border: isActive
                                                        ? "2px solid rgba(254,176,106,0.70)"
                                                        : isDone
                                                          ? "2px solid rgba(74,222,128,0.50)"
                                                          : "2px solid rgba(255,255,255,0.15)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: isActive
                                                        ? 15
                                                        : 12,
                                                    fontWeight: 900,
                                                    color: isActive
                                                        ? "#FEB06A"
                                                        : isDone
                                                          ? "#4ade80"
                                                          : "rgba(255,255,255,0.35)",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                {s.n}
                                            </div>
                                            <div>
                                                <div
                                                    style={{
                                                        fontSize: isActive
                                                            ? 16
                                                            : 13,
                                                        fontWeight: isActive
                                                            ? 900
                                                            : 700,
                                                        color: isActive
                                                            ? "#FEB06A"
                                                            : isDone
                                                              ? "rgba(255,255,255,0.75)"
                                                              : "rgba(255,255,255,0.40)",
                                                        marginBottom: 3,
                                                    }}
                                                >
                                                    {s.head}
                                                </div>
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: isActive
                                                            ? "rgba(255,255,255,0.55)"
                                                            : "rgba(255,255,255,0.22)",
                                                    }}
                                                >
                                                    {s.body}
                                                </div>
                                            </div>
                                        </div>
                                    )
                                })}
                            </div>
                            <div
                                style={{
                                    borderRadius: 14,
                                    border: "1px solid rgba(74,222,128,0.18)",
                                    background: "rgba(74,222,128,0.05)",
                                    padding: "14px 16px",
                                    textAlign: "left" as const,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 2,
                                        textTransform: "uppercase" as const,
                                        color: "rgba(74,222,128,0.65)",
                                        marginBottom: 10,
                                    }}
                                >
                                    What Cover Letter gives you
                                </div>
                                <div
                                    style={{
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 8,
                                    }}
                                >
                                    {[
                                        "A complete cover letter written from your actual background",
                                        "Opening that names the specific role and signals genuine fit",
                                        "Body that connects your experience to what this company needs",
                                        "Closing that is confident without being pushy",
                                    ].map((item, i) => (
                                        <div
                                            key={i}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 10,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: 18,
                                                    height: 18,
                                                    borderRadius: "50%",
                                                    background:
                                                        "rgba(74,222,128,0.15)",
                                                    border: "1.5px solid rgba(74,222,128,0.45)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: 10,
                                                    fontWeight: 900,
                                                    color: "#4ade80",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                ✓
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 12,
                                                    lineHeight: "18px",
                                                    color: "rgba(255,255,255,0.60)",
                                                }}
                                            >
                                                {item}
                                            </div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                    {renderPrimaryRunButton(
                        "Run Cover Letter →",
                        runCoverLetter,
                        loadingCoverLetter
                    )}
                </>
            )
        }

        const bodyRaw = String(coverLetterResult?.letter || "").trim()

        const rawName = String(
            coverLetterResult?.contact?.full_name ??
                coverLetterResult?.full_name ??
                coverLetterResult?.name ??
                coverLetterResult?.student_name ??
                coverLetterResult?.studentName ??
                ""
        ).trim()

        const looksLikeBadName =
            !rawName ||
            rawName.toLowerCase().includes("project") ||
            rawName.toLowerCase().includes("academic") ||
            rawName.toLowerCase().includes("selected") ||
            rawName.split(" ").length < 2

        const fullName = looksLikeBadName ? "" : rawName

        const phone =
            String(
                coverLetterResult?.contact?.phone ??
                    coverLetterResult?.phone ??
                    ""
            ).trim() || ""

        const emailFromProfile =
            String(
                coverLetterResult?.contact?.email ??
                    coverLetterResult?.email ??
                    authedEmail ??
                    ""
            ).trim() || ""

        console.log(
            "CL_DEBUG contact:",
            JSON.stringify(coverLetterResult?.contact)
        )
        console.log("CL_DEBUG keys:", Object.keys(coverLetterResult || {}))
        console.log("CL_DEBUG jobContext:", JSON.stringify(jobContext))
        console.log(
            "CL_DEBUG ctxFromText:",
            JSON.stringify(extractJobContextFromJobText(job))
        )

        const ctxFromText = extractJobContextFromJobText(job)
        const resolvedTitle = jobContext?.title || ctxFromText.title || ""
        const resolvedName = clName.trim() || fullName || ""
        const resolvedCompany =
            clCompany.trim() || jobContext?.company || ctxFromText.company || ""

        const finalLetter = buildFinalLetter({
            bodyRaw,
            jobTitle: resolvedTitle,
            companyName: resolvedCompany,
            fullName: resolvedName,
            phone,
            email: emailFromProfile,
            hiringManager: clHiringManager,
        })
        const copyLetter = () => {
            if (typeof navigator === "undefined" || !navigator.clipboard) return
            navigator.clipboard.writeText(finalLetter)
        }

        return (
            <>
                {/* Header row */}
                {/* Editable fields with DocuSign-style indicators */}
                <div
                    style={{
                        display: "grid",
                        ggridTemplateColumns: "1fr 1fr 1fr",
                        gap: 10,
                        marginBottom: 14,
                    }}
                >
                    {[
                        {
                            label: "Your name",
                            value: clName,
                            setter: setClName,
                            placeholder: "Your full name",
                        },
                        {
                            label: "Company name",
                            value: clCompany,
                            setter: setClCompany,
                            placeholder: "Company name",
                        },
                        {
                            label: "Hiring manager (optional)",
                            value: clHiringManager,
                            setter: setClHiringManager,
                            placeholder: "e.g. Sarah Johnson",
                        },
                    ].map((field) => (
                        <div key={field.label} style={{ position: "relative" }}>
                            {/* Orange arrow indicator */}
                            <div
                                style={{
                                    display: "flex",
                                    alignItems: "center",
                                    gap: 8,
                                    marginBottom: 6,
                                }}
                            >
                                <div
                                    style={{
                                        display: "flex",
                                        alignItems: "center",
                                        gap: 0,
                                    }}
                                >
                                    <div
                                        style={{
                                            width: 0,
                                            height: 0,
                                            borderTop: "10px solid transparent",
                                            borderBottom:
                                                "10px solid transparent",
                                            borderLeft: "14px solid #FEB06A",
                                        }}
                                    />
                                    <div
                                        style={{
                                            background: "#FEB06A",
                                            height: 6,
                                            width: 10,
                                            marginLeft: -1,
                                        }}
                                    />
                                </div>
                                <div
                                    style={{
                                        fontSize: 11,
                                        fontWeight: 900,
                                        letterSpacing: 0.8,
                                        color: "#FEB06A",
                                        textTransform: "uppercase" as const,
                                    }}
                                >
                                    {field.label}
                                </div>
                            </div>
                            <input
                                style={{
                                    ...input,
                                    fontSize: 14,
                                    height: 42,
                                    border: `2px solid rgba(254,176,106,0.50)`,
                                    borderRadius: 12,
                                }}
                                value={field.value}
                                onChange={(e) => field.setter(e.target.value)}
                                placeholder={field.placeholder}
                            />
                        </div>
                    ))}
                </div>

                {/* Header row */}
                <div
                    style={{
                        display: "flex",
                        alignItems: "center",
                        justifyContent: "space-between",
                        gap: 12,
                        marginBottom: 14,
                    }}
                >
                    <div
                        style={{
                            fontSize: 9,
                            fontWeight: 900,
                            letterSpacing: 2,
                            textTransform: "uppercase" as const,
                            color: "rgba(255,255,255,0.32)",
                        }}
                    >
                        Letter generated
                    </div>
                    <button
                        type="button"
                        onClick={copyLetter}
                        style={{
                            background: "transparent",
                            border: "none",
                            padding: 0,
                            fontSize: 12,
                            fontWeight: 900,
                            color: WRN_BLUE,
                            cursor: "pointer",
                            textDecoration: "underline",
                            textUnderlineOffset: 3,
                            letterSpacing: 0.3,
                        }}
                    >
                        Copy letter
                    </button>
                </div>

                {/* Document card */}
                <div
                    style={{
                        background: "#FFFFFF",
                        borderRadius: 18,
                        padding: "32px 36px",
                        fontFamily: "Georgia, 'Times New Roman', serif",
                        fontSize: 14,
                        lineHeight: "24px",
                        color: "#1a1a2e",
                        whiteSpace: "pre-wrap" as const,
                        marginBottom: 16,
                    }}
                >
                    {finalLetter}
                </div>

                {/* Copy CTA */}
                <button
                    style={{
                        width: "100%",
                        padding: "13px",
                        borderRadius: 13,
                        background: GLASS,
                        fontWeight: 900,
                        fontSize: 14,
                        border: `1px solid ${BORDER_SOFT}`,
                        cursor: "pointer",
                        color: TEXT,
                        marginBottom: 10,
                    }}
                    onClick={copyLetter}
                    type="button"
                >
                    Copy to Clipboard
                </button>

                <button
                    style={{ ...jobfitHeroBtn, width: "100%" }}
                    onClick={() => goTo("networking")}
                    type="button"
                >
                    Proceed to Networking →
                </button>
            </>
        )
    }

    /* ---------- RENDER: NETWORKING ---------- */
    function renderNetworking() {
        if (accessMode === "jobfit_only")
            return renderLockedPreview("networking")

        if (!networkingResult) {
            return (
                <>
                    <div
                        style={{
                            borderRadius: 18,
                            border: "1px solid rgba(81,173,229,0.20)",
                            background: "#0D1829",
                            overflow: "hidden",
                            marginBottom: 16,
                            marginTop: 4,
                            position: "relative" as const,
                        }}
                    >
                        <div
                            style={{
                                position: "absolute" as const,
                                top: -40,
                                left: -40,
                                width: 220,
                                height: 220,
                                borderRadius: "50%",
                                background: "rgba(81,173,229,0.07)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                position: "absolute" as const,
                                bottom: -20,
                                right: -20,
                                width: 180,
                                height: 180,
                                borderRadius: "50%",
                                background: "rgba(254,176,106,0.06)",
                                pointerEvents: "none" as const,
                            }}
                        />
                        <div
                            style={{
                                height: 3,
                                background:
                                    "linear-gradient(90deg,#FEB06A,#f97316,#51ADE5)",
                            }}
                        />
                        <div
                            style={{
                                padding: "28px 32px 32px",
                                position: "relative" as const,
                                zIndex: 1,
                                textAlign: "center" as const,
                            }}
                        >
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: "rgba(254,176,106,0.70)",
                                    marginBottom: 16,
                                }}
                            >
                                Step 4 of 4
                            </div>
                            <div
                                style={{
                                    fontSize: 38,
                                    fontWeight: 950,
                                    color: "rgba(255,255,255,0.97)",
                                    lineHeight: 1.1,
                                    letterSpacing: -1.2,
                                    marginBottom: 10,
                                }}
                            >
                                Make your name known
                                <br />
                                <span style={{ color: "#FEB06A" }}>
                                    before
                                </span>{" "}
                                the interview.
                            </div>
                            <div
                                style={{
                                    fontSize: 22,
                                    fontWeight: 950,
                                    letterSpacing: -0.5,
                                    marginBottom: 18,
                                    lineHeight: 1.2,
                                }}
                            >
                                <span style={{ color: "#FEB06A" }}>
                                    Don't just apply.
                                </span>
                                <span
                                    style={{ color: "rgba(255,255,255,0.90)" }}
                                >
                                    {" "}
                                    Be appropriately aggressive.
                                </span>
                            </div>
                            <div
                                style={{
                                    fontSize: 14,
                                    lineHeight: "22px",
                                    color: "rgba(255,255,255,0.45)",
                                    margin: "0 auto 28px",
                                    maxWidth: 500,
                                }}
                            >
                                Most candidates apply and wait. The ones who get
                                interviews are already known inside the org.
                                SIGNAL gives you exactly who to contact, what to
                                say, and how to follow up — without being
                                annoying about it.
                            </div>
                            <div
                                style={{
                                    display: "flex",
                                    flexDirection: "column" as const,
                                    gap: 10,
                                    marginBottom: 22,
                                    textAlign: "left" as const,
                                }}
                            >
                                {[
                                    {
                                        n: "1",
                                        head: "Run JobFit",
                                        body: "Paste the job description and get a clear verdict — Apply, Review, or Pass — before you spend a minute on an application.",
                                    },
                                    {
                                        n: "2",
                                        head: "Run Positioning",
                                        body: "Rewrite your resume bullets to match this specific role. One targeted story, in the language the recruiter is scanning for.",
                                    },
                                    {
                                        n: "3",
                                        head: "Run Cover Letter",
                                        body: "Write a letter in your voice that connects your background to this company — not a template, a real case for why you fit.",
                                    },
                                    {
                                        n: "4",
                                        head: "Run Networking",
                                        body: "Build your outreach plan. Know who to contact, what to say, and how to follow up so your name is known before the interview.",
                                    },
                                ].map((s, idx) => {
                                    const isActive = idx === 3
                                    const isDone = idx < 3
                                    return (
                                        <div
                                            key={s.n}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 14,
                                                opacity: isActive
                                                    ? 1
                                                    : isDone
                                                      ? 0.55
                                                      : 0.3,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: isActive ? 36 : 28,
                                                    height: isActive ? 36 : 28,
                                                    borderRadius: "50%",
                                                    background: isActive
                                                        ? "rgba(254,176,106,0.25)"
                                                        : isDone
                                                          ? "rgba(74,222,128,0.15)"
                                                          : "rgba(255,255,255,0.06)",
                                                    border: isActive
                                                        ? "2px solid rgba(254,176,106,0.70)"
                                                        : isDone
                                                          ? "2px solid rgba(74,222,128,0.50)"
                                                          : "2px solid rgba(255,255,255,0.15)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: isActive
                                                        ? 15
                                                        : 12,
                                                    fontWeight: 900,
                                                    color: isActive
                                                        ? "#FEB06A"
                                                        : isDone
                                                          ? "#4ade80"
                                                          : "rgba(255,255,255,0.35)",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                {s.n}
                                            </div>
                                            <div>
                                                <div
                                                    style={{
                                                        fontSize: isActive
                                                            ? 16
                                                            : 13,
                                                        fontWeight: isActive
                                                            ? 900
                                                            : 700,
                                                        color: isActive
                                                            ? "#FEB06A"
                                                            : isDone
                                                              ? "rgba(255,255,255,0.75)"
                                                              : "rgba(255,255,255,0.40)",
                                                        marginBottom: 3,
                                                    }}
                                                >
                                                    {s.head}
                                                </div>
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: isActive
                                                            ? "rgba(255,255,255,0.55)"
                                                            : "rgba(255,255,255,0.22)",
                                                    }}
                                                >
                                                    {s.body}
                                                </div>
                                            </div>
                                        </div>
                                    )
                                })}
                            </div>
                            <div
                                style={{
                                    borderRadius: 14,
                                    border: "1px solid rgba(74,222,128,0.18)",
                                    background: "rgba(74,222,128,0.05)",
                                    padding: "14px 16px",
                                    textAlign: "left" as const,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 2,
                                        textTransform: "uppercase" as const,
                                        color: "rgba(74,222,128,0.65)",
                                        marginBottom: 10,
                                    }}
                                >
                                    What Networking gives you
                                </div>
                                <div
                                    style={{
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 8,
                                    }}
                                >
                                    {[
                                        "A list of specific people to contact inside the company",
                                        "LinkedIn connection request and first message — ready to send",
                                        "A follow-up sequence if they don't respond",
                                        "Conversation starters and call prep for when they do",
                                    ].map((item, i) => (
                                        <div
                                            key={i}
                                            style={{
                                                display: "flex",
                                                alignItems: "flex-start",
                                                gap: 10,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    width: 18,
                                                    height: 18,
                                                    borderRadius: "50%",
                                                    background:
                                                        "rgba(74,222,128,0.15)",
                                                    border: "1.5px solid rgba(74,222,128,0.45)",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: 10,
                                                    fontWeight: 900,
                                                    color: "#4ade80",
                                                    flexShrink: 0,
                                                    marginTop: 1,
                                                }}
                                            >
                                                ✓
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 12,
                                                    lineHeight: "18px",
                                                    color: "rgba(255,255,255,0.60)",
                                                }}
                                            >
                                                {item}
                                            </div>
                                        </div>
                                    ))}
                                </div>
                            </div>
                        </div>
                    </div>
                    {renderPrimaryRunButton(
                        "Run Networking →",
                        runNetworking,
                        loadingNetworking
                    )}
                </>
            )
        }

        function normalizeUserVoice(text: string) {
            if (!text) return ""
            return text
                .replace(/\bReece\b/gi, "You")
                .replace(/\bhe\b/gi, "you")
                .replace(/\bhis\b/gi, "your")
                .replace(/\bhim\b/gi, "you")
                .replace(/Move\s*1/gi, "First outreach")
                .replace(/Move\s*2/gi, "Second outreach")
                .replace(/Move\s*3/gi, "Third outreach")
        }

        const framing = normalizeUserVoice(
            String(networkingResult?.framing || "").trim()
        )
        const strategy = normalizeUserVoice(
            String(networkingResult?.strategy || "").trim()
        )
        const sequence = Array.isArray(networkingResult?.sequence)
            ? networkingResult.sequence
            : []
        const moves = Array.isArray(networkingResult?.moves)
            ? networkingResult.moves
            : []

        // copyText and wasCopied are defined at component level

        const laneMeta = [
            {
                stage: "Start Here",
                title: "Someone Doing the Job",
                accent: WRN_ORANGE,
                tag: "Do this first",
            },
            {
                stage: "Second Reachout",
                title: "Shared Background Contact",
                accent: WRN_BLUE,
                tag: "",
            },
            {
                stage: "Third Reachout",
                title: "Hiring Team Contact",
                accent: ICE,
                tag: "",
            },
        ]

        // Shared sub-section label style
        function NetLabel({ text, color }: { text: string; color?: string }) {
            return (
                <div
                    style={{
                        fontSize: 9,
                        fontWeight: 900,
                        letterSpacing: 2,
                        textTransform: "uppercase" as const,
                        color: color || "rgba(255,255,255,0.30)",
                        marginBottom: 6,
                        marginTop: 14,
                    }}
                >
                    {text}
                </div>
            )
        }

        // Color config per step
        const stepColors = [
            {
                accent: "#FEB06A",
                border: "rgba(254,176,106,0.30)",
                headerBg: "rgba(254,176,106,0.06)",
                headerBorder: "rgba(254,176,106,0.12)",
                iconBg: "rgba(254,176,106,0.15)",
                iconBorder: "rgba(254,176,106,0.50)",
                msgBg: "rgba(254,176,106,0.07)",
                msgBorder: "rgba(254,176,106,0.20)",
                btnBg: "linear-gradient(135deg,#FEB06A,#f97316)",
                btnColor: "#0B1020",
                pillBg: "rgba(254,176,106,0.12)",
                pillBorder: "rgba(254,176,106,0.25)",
                gradient: "linear-gradient(90deg,#FEB06A,#f97316,#FEB06A)",
                detailBg: "rgba(254,176,106,0.05)",
                detailBorder: "rgba(254,176,106,0.15)",
                timelineDot: "#FEB06A",
                whenLabel: "Send Today",
            },
            {
                accent: "#51ADE5",
                border: "rgba(81,173,229,0.25)",
                headerBg: "rgba(81,173,229,0.06)",
                headerBorder: "rgba(81,173,229,0.12)",
                iconBg: "rgba(81,173,229,0.15)",
                iconBorder: "rgba(81,173,229,0.40)",
                msgBg: "rgba(81,173,229,0.07)",
                msgBorder: "rgba(81,173,229,0.18)",
                btnBg: "rgba(81,173,229,0.18)",
                btnColor: "#51ADE5",
                pillBg: "rgba(81,173,229,0.12)",
                pillBorder: "rgba(81,173,229,0.25)",
                gradient: "linear-gradient(90deg,#51ADE5,#218C8C,#51ADE5)",
                detailBg: "rgba(81,173,229,0.05)",
                detailBorder: "rgba(81,173,229,0.15)",
                timelineDot: "#51ADE5",
                whenLabel: "Send Day 3",
            },
            {
                accent: "#4ade80",
                border: "rgba(74,222,128,0.22)",
                headerBg: "rgba(74,222,128,0.06)",
                headerBorder: "rgba(74,222,128,0.12)",
                iconBg: "rgba(74,222,128,0.15)",
                iconBorder: "rgba(74,222,128,0.40)",
                msgBg: "rgba(74,222,128,0.06)",
                msgBorder: "rgba(74,222,128,0.18)",
                btnBg: "rgba(74,222,128,0.15)",
                btnColor: "#4ade80",
                pillBg: "rgba(74,222,128,0.12)",
                pillBorder: "rgba(74,222,128,0.25)",
                gradient: "linear-gradient(90deg,#4ade80,#22c55e,#51ADE5)",
                detailBg: "rgba(74,222,128,0.05)",
                detailBorder: "rgba(74,222,128,0.15)",
                timelineDot: "#4ade80",
                whenLabel: "Send Day 5",
            },
        ]

        // Simple human labels replacing jargon
        const stepTitles = [
            "Find someone in the company already doing this job",
            "Find someone in the company with a similar background to you",
            "Find a recruiter, HR member, or hiring manager at the company",
        ]

        return (
            <>
                {/* ── PLAN HEADER ── */}
                <div
                    style={{
                        borderRadius: 18,
                        border: "1px solid rgba(81,173,229,0.20)",
                        background: "#0D1829",
                        overflow: "hidden",
                        marginBottom: 20,
                        boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                    }}
                >
                    <div
                        style={{
                            height: 3,
                            background:
                                "linear-gradient(90deg,#51ADE5,#218C8C,#FEB06A)",
                        }}
                    />
                    <div style={{ padding: "20px 24px" }}>
                        <div
                            style={{
                                fontSize: 9,
                                fontWeight: 900,
                                letterSpacing: 2,
                                textTransform: "uppercase" as const,
                                color: "rgba(81,173,229,0.65)",
                                marginBottom: 10,
                            }}
                        >
                            Your Networking Plan
                        </div>
                        <div
                            style={{
                                fontSize: 18,
                                fontWeight: 900,
                                color: "rgba(255,255,255,0.95)",
                                lineHeight: "26px",
                                marginBottom: 8,
                            }}
                        >
                            Send 3 messages over the next 5 days.
                        </div>
                        <div
                            style={{
                                fontSize: 14,
                                color: "rgba(255,255,255,0.50)",
                                lineHeight: "22px",
                            }}
                        >
                            {framing ||
                                "Each message goes to a different person at or connected to the company. Start with Step 1 today — right after you apply."}
                        </div>
                    </div>
                </div>

                {/* ── TIMELINE TRACK ── */}
                <div
                    style={{
                        display: "flex",
                        alignItems: "center",
                        gap: 10,
                        marginBottom: 20,
                        padding: "0 4px",
                    }}
                >
                    {stepColors.map((c, i) => (
                        <React.Fragment key={i}>
                            <div
                                style={{
                                    display: "flex",
                                    alignItems: "center",
                                    gap: 8,
                                }}
                            >
                                <div
                                    style={{
                                        width: 10,
                                        height: 10,
                                        borderRadius: "50%",
                                        background: c.timelineDot,
                                        boxShadow: `0 0 8px ${c.timelineDot}`,
                                    }}
                                />
                                <div
                                    style={{
                                        fontSize: 11,
                                        color: "rgba(255,255,255,0.40)",
                                        whiteSpace: "nowrap" as const,
                                    }}
                                >
                                    Step {i + 1} —{" "}
                                    {c.whenLabel.replace("Send ", "")}
                                </div>
                            </div>
                            {i < 2 && (
                                <div
                                    style={{
                                        flex: 1,
                                        height: 1,
                                        background: "rgba(255,255,255,0.08)",
                                    }}
                                />
                            )}
                        </React.Fragment>
                    ))}
                </div>

                {/* ── STEP CARDS ── */}
                {moves.map((m: any, i: number) => {
                    const c = stepColors[i] || stepColors[2]
                    const isOpen = netOpenSet.has(i)

                    const targetTitle = normalizeUserVoice(
                        String(m?.target_title || m?.targetTitle || "").trim()
                    )
                    const goal = String(m?.goal || "").trim()
                    const why = String(m?.why_this_target || "").trim()
                    const timing = String(m?.timing || "").trim()
                    const channelPrimary = String(
                        m?.channel_plan?.primary || ""
                    ).trim()
                    const connectionRequest = String(
                        m?.linkedin_connection_request || ""
                    ).trim()
                    const linkedinMessage = String(
                        m?.linkedin_message || ""
                    ).trim()
                    const emailSubject = String(m?.email_subject || "").trim()
                    const emailBody = String(m?.email_body || "").trim()
                    const followUp = String(m?.follow_up_message || "").trim()
                    const searchQueries = Array.isArray(
                        m?.linkedin_search_queries
                    )
                        ? m.linkedin_search_queries
                        : []
                    const openers = Array.isArray(m?.conversation_openers)
                        ? m.conversation_openers
                        : []

                    const humanTitle = stepTitles[i] || targetTitle || "Contact"
                    const searchHint = searchQueries[0]
                        ? `Search LinkedIn for: ${String(searchQueries[0]).trim()}`
                        : targetTitle
                          ? `Search LinkedIn for: ${targetTitle}`
                          : ""

                    const primaryMessage =
                        linkedinMessage || connectionRequest || ""

                    const toggle = () =>
                        setNetOpenSet((prev: Set<number>) => {
                            const next = new Set(prev)
                            if (next.has(i)) next.delete(i)
                            else next.add(i)
                            return next
                        })

                    return (
                        <div
                            key={i}
                            style={{
                                borderRadius: 18,
                                border: `1px solid ${c.border}`,
                                background: "#0D1829",
                                overflow: "hidden",
                                marginBottom: 14,
                                boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                            }}
                        >
                            <div
                                style={{ height: 3, background: c.gradient }}
                            />

                            {/* Card header */}
                            <div
                                style={{
                                    padding: "18px 22px 20px",
                                    borderBottom: isOpen
                                        ? `1px solid ${c.headerBorder}`
                                        : "none",
                                }}
                            >
                                <div
                                    style={{
                                        display: "flex",
                                        alignItems: "flex-start",
                                        gap: 14,
                                        marginBottom: 14,
                                    }}
                                >
                                    {/* Step number */}
                                    <div
                                        style={{
                                            width: 36,
                                            height: 36,
                                            borderRadius: "50%",
                                            flexShrink: 0,
                                            background: c.iconBg,
                                            border: `2px solid ${c.iconBorder}`,
                                            display: "flex",
                                            alignItems: "center",
                                            justifyContent: "center",
                                            fontSize: 16,
                                            fontWeight: 900,
                                            color: c.accent,
                                        }}
                                    >
                                        {i + 1}
                                    </div>
                                    <div style={{ flex: 1 }}>
                                        <div
                                            style={{
                                                display: "flex",
                                                alignItems: "center",
                                                gap: 8,
                                                marginBottom: 4,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    fontSize: 13,
                                                    fontWeight: 900,
                                                    color: c.accent,
                                                }}
                                            >
                                                {c.whenLabel}
                                            </div>
                                            {i === 0 && (
                                                <div
                                                    style={{
                                                        padding: "3px 10px",
                                                        borderRadius: 999,
                                                        background: c.pillBg,
                                                        border: `1px solid ${c.pillBorder}`,
                                                        fontSize: 10,
                                                        fontWeight: 900,
                                                        color: c.accent,
                                                    }}
                                                >
                                                    Do this first
                                                </div>
                                            )}
                                        </div>
                                        <div
                                            style={{
                                                fontSize: 16,
                                                fontWeight: 900,
                                                color: "rgba(255,255,255,0.95)",
                                                lineHeight: "22px",
                                                marginBottom: 4,
                                            }}
                                        >
                                            {humanTitle}
                                        </div>
                                        {searchHint ? (
                                            <div
                                                style={{
                                                    fontSize: 12,
                                                    color: "rgba(255,255,255,0.38)",
                                                    lineHeight: "18px",
                                                }}
                                            >
                                                {searchHint}
                                            </div>
                                        ) : null}
                                    </div>
                                </div>

                                {/* Primary message — always visible */}
                                {primaryMessage ? (
                                    <>
                                        <div
                                            style={{
                                                fontSize: 9,
                                                fontWeight: 900,
                                                letterSpacing: 2,
                                                textTransform:
                                                    "uppercase" as const,
                                                color: `${c.accent}99`,
                                                marginBottom: 8,
                                            }}
                                        >
                                            Your message — copy and send on
                                            LinkedIn
                                        </div>
                                        <div
                                            style={{
                                                borderRadius: 12,
                                                padding: "16px 18px",
                                                background: c.msgBg,
                                                border: `1px solid ${c.msgBorder}`,
                                                fontSize: 14,
                                                lineHeight: "22px",
                                                color: "rgba(255,255,255,0.88)",
                                                marginBottom: 12,
                                                whiteSpace: "pre-wrap" as const,
                                            }}
                                        >
                                            {primaryMessage}
                                        </div>
                                        <button
                                            onClick={() =>
                                                copyText(
                                                    primaryMessage,
                                                    `primary-${i}`
                                                )
                                            }
                                            type="button"
                                            style={{
                                                width: "100%",
                                                padding: "13px",
                                                borderRadius: 12,
                                                border: "none",
                                                cursor: "pointer",
                                                fontSize: 14,
                                                fontWeight: 900,
                                                letterSpacing: 0.2,
                                                background: wasCopied(
                                                    `primary-${i}`
                                                )
                                                    ? "rgba(74,222,128,0.18)"
                                                    : c.btnBg,
                                                color: wasCopied(`primary-${i}`)
                                                    ? "#4ade80"
                                                    : c.btnColor,
                                                display: "flex",
                                                alignItems: "center",
                                                justifyContent: "center",
                                                gap: 8,
                                                transition:
                                                    "background 0.2s, color 0.2s",
                                            }}
                                        >
                                            {wasCopied(`primary-${i}`)
                                                ? "✓ Copied!"
                                                : "⎘   Copy this message"}
                                        </button>
                                    </>
                                ) : null}

                                {/* More detail toggle */}
                                {connectionRequest ||
                                emailSubject ||
                                emailBody ||
                                followUp ||
                                openers.length > 0 ||
                                why ? (
                                    <button
                                        onClick={toggle}
                                        type="button"
                                        style={{
                                            marginTop: 12,
                                            background: "none",
                                            border: "none",
                                            padding: 0,
                                            fontSize: 12,
                                            fontWeight: 900,
                                            color: "rgba(255,255,255,0.35)",
                                            cursor: "pointer",
                                            letterSpacing: 0.3,
                                            display: "flex",
                                            alignItems: "center",
                                            gap: 4,
                                        }}
                                    >
                                        {isOpen
                                            ? "▾ Hide details"
                                            : "▸ More — connection request, email version, follow-up"}
                                    </button>
                                ) : null}
                            </div>

                            {/* Expanded details */}
                            {isOpen ? (
                                <div
                                    style={{
                                        padding: "18px 22px 20px",
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 16,
                                    }}
                                >
                                    {/* Connection request */}
                                    {connectionRequest &&
                                    connectionRequest !== primaryMessage ? (
                                        <div>
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 2,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(255,255,255,0.28)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                Short connection request (300
                                                chars)
                                            </div>
                                            <div
                                                style={{
                                                    borderRadius: 12,
                                                    padding: "14px 16px",
                                                    background:
                                                        "rgba(255,255,255,0.04)",
                                                    border: "1px solid rgba(255,255,255,0.09)",
                                                    fontSize: 13,
                                                    lineHeight: "21px",
                                                    color: "rgba(255,255,255,0.80)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                {connectionRequest}
                                            </div>
                                            <button
                                                onClick={() =>
                                                    copyText(
                                                        connectionRequest,
                                                        `conn-${i}`
                                                    )
                                                }
                                                type="button"
                                                style={{
                                                    background: wasCopied(
                                                        `conn-${i}`
                                                    )
                                                        ? "rgba(74,222,128,0.15)"
                                                        : "rgba(255,255,255,0.07)",
                                                    border: wasCopied(
                                                        `conn-${i}`
                                                    )
                                                        ? "1px solid rgba(74,222,128,0.40)"
                                                        : "1px solid rgba(255,255,255,0.12)",
                                                    borderRadius: 8,
                                                    padding: "7px 14px",
                                                    fontSize: 11,
                                                    fontWeight: 900,
                                                    color: wasCopied(
                                                        `conn-${i}`
                                                    )
                                                        ? "#4ade80"
                                                        : "rgba(255,255,255,0.55)",
                                                    cursor: "pointer",
                                                    transition: "all 0.2s",
                                                }}
                                            >
                                                {wasCopied(`conn-${i}`)
                                                    ? "✓ Copied!"
                                                    : "⎘ Copy connection request"}
                                            </button>
                                        </div>
                                    ) : null}

                                    {/* Email version */}
                                    {emailSubject || emailBody ? (
                                        <div>
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 2,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(255,255,255,0.28)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                Email version
                                            </div>
                                            {emailSubject ? (
                                                <div
                                                    style={{
                                                        fontSize: 13,
                                                        fontWeight: 700,
                                                        color: "rgba(255,255,255,0.70)",
                                                        marginBottom: 8,
                                                    }}
                                                >
                                                    Subject: {emailSubject}
                                                </div>
                                            ) : null}
                                            {emailBody ? (
                                                <div
                                                    style={{
                                                        borderRadius: 12,
                                                        padding: "14px 16px",
                                                        background:
                                                            "rgba(255,255,255,0.04)",
                                                        border: "1px solid rgba(255,255,255,0.09)",
                                                        fontSize: 13,
                                                        lineHeight: "21px",
                                                        color: "rgba(255,255,255,0.80)",
                                                        whiteSpace:
                                                            "pre-wrap" as const,
                                                        marginBottom: 8,
                                                    }}
                                                >
                                                    {emailBody}
                                                </div>
                                            ) : null}
                                            <div
                                                style={{
                                                    display: "flex",
                                                    gap: 8,
                                                }}
                                            >
                                                {emailSubject ? (
                                                    <button
                                                        onClick={() =>
                                                            copyText(
                                                                emailSubject,
                                                                `subj-${i}`
                                                            )
                                                        }
                                                        type="button"
                                                        style={{
                                                            background:
                                                                wasCopied(
                                                                    `subj-${i}`
                                                                )
                                                                    ? "rgba(74,222,128,0.15)"
                                                                    : "rgba(255,255,255,0.07)",
                                                            border: wasCopied(
                                                                `subj-${i}`
                                                            )
                                                                ? "1px solid rgba(74,222,128,0.40)"
                                                                : "1px solid rgba(255,255,255,0.12)",
                                                            borderRadius: 8,
                                                            padding: "7px 14px",
                                                            fontSize: 11,
                                                            fontWeight: 900,
                                                            color: wasCopied(
                                                                `subj-${i}`
                                                            )
                                                                ? "#4ade80"
                                                                : "rgba(255,255,255,0.55)",
                                                            cursor: "pointer",
                                                            transition:
                                                                "all 0.2s",
                                                        }}
                                                    >
                                                        {wasCopied(`subj-${i}`)
                                                            ? "✓ Copied!"
                                                            : "⎘ Copy subject"}
                                                    </button>
                                                ) : null}
                                                {emailBody ? (
                                                    <button
                                                        onClick={() =>
                                                            copyText(
                                                                emailBody,
                                                                `email-${i}`
                                                            )
                                                        }
                                                        type="button"
                                                        style={{
                                                            background:
                                                                wasCopied(
                                                                    `email-${i}`
                                                                )
                                                                    ? "rgba(74,222,128,0.15)"
                                                                    : "rgba(255,255,255,0.07)",
                                                            border: wasCopied(
                                                                `email-${i}`
                                                            )
                                                                ? "1px solid rgba(74,222,128,0.40)"
                                                                : "1px solid rgba(255,255,255,0.12)",
                                                            borderRadius: 8,
                                                            padding: "7px 14px",
                                                            fontSize: 11,
                                                            fontWeight: 900,
                                                            color: wasCopied(
                                                                `email-${i}`
                                                            )
                                                                ? "#4ade80"
                                                                : "rgba(255,255,255,0.55)",
                                                            cursor: "pointer",
                                                            transition:
                                                                "all 0.2s",
                                                        }}
                                                    >
                                                        {wasCopied(`email-${i}`)
                                                            ? "✓ Copied!"
                                                            : "⎘ Copy email"}
                                                    </button>
                                                ) : null}
                                            </div>
                                        </div>
                                    ) : null}

                                    {/* Follow-up */}
                                    {followUp ? (
                                        <div>
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 2,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(255,255,255,0.28)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                If they don't reply in 5 days
                                            </div>
                                            <div
                                                style={{
                                                    borderRadius: 12,
                                                    padding: "14px 16px",
                                                    background:
                                                        "rgba(255,255,255,0.04)",
                                                    border: "1px solid rgba(255,255,255,0.09)",
                                                    fontSize: 13,
                                                    lineHeight: "21px",
                                                    color: "rgba(255,255,255,0.80)",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                {followUp}
                                            </div>
                                            <button
                                                onClick={() =>
                                                    copyText(
                                                        followUp,
                                                        `fu-${i}`
                                                    )
                                                }
                                                type="button"
                                                style={{
                                                    background: wasCopied(
                                                        `fu-${i}`
                                                    )
                                                        ? "rgba(74,222,128,0.15)"
                                                        : "rgba(255,255,255,0.07)",
                                                    border: wasCopied(`fu-${i}`)
                                                        ? "1px solid rgba(74,222,128,0.40)"
                                                        : "1px solid rgba(255,255,255,0.12)",
                                                    borderRadius: 8,
                                                    padding: "7px 14px",
                                                    fontSize: 11,
                                                    fontWeight: 900,
                                                    color: wasCopied(`fu-${i}`)
                                                        ? "#4ade80"
                                                        : "rgba(255,255,255,0.55)",
                                                    cursor: "pointer",
                                                    transition: "all 0.2s",
                                                }}
                                            >
                                                {wasCopied(`fu-${i}`)
                                                    ? "✓ Copied!"
                                                    : "⎘ Copy follow-up"}
                                            </button>
                                        </div>
                                    ) : null}

                                    {/* Conversation openers */}
                                    {openers.length > 0 ? (
                                        <div>
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    fontWeight: 900,
                                                    letterSpacing: 2,
                                                    textTransform:
                                                        "uppercase" as const,
                                                    color: "rgba(255,255,255,0.28)",
                                                    marginBottom: 10,
                                                }}
                                            >
                                                If they reply — things to ask
                                            </div>
                                            <div
                                                style={{
                                                    display: "flex",
                                                    flexDirection:
                                                        "column" as const,
                                                    gap: 8,
                                                }}
                                            >
                                                {openers.map(
                                                    (o: any, oi: number) => (
                                                        <div
                                                            key={oi}
                                                            style={{
                                                                display: "flex",
                                                                alignItems:
                                                                    "flex-start",
                                                                gap: 10,
                                                            }}
                                                        >
                                                            <div
                                                                style={{
                                                                    width: 5,
                                                                    height: 5,
                                                                    borderRadius:
                                                                        "50%",
                                                                    background:
                                                                        c.accent,
                                                                    flexShrink: 0,
                                                                    marginTop: 7,
                                                                    opacity: 0.6,
                                                                }}
                                                            />
                                                            <div
                                                                style={{
                                                                    fontSize: 13,
                                                                    lineHeight:
                                                                        "20px",
                                                                    color: "rgba(255,255,255,0.55)",
                                                                }}
                                                            >
                                                                {String(
                                                                    o
                                                                ).trim()}
                                                            </div>
                                                        </div>
                                                    )
                                                )}
                                            </div>
                                        </div>
                                    ) : null}
                                </div>
                            ) : null}
                        </div>
                    )
                })}

                {/* ── SEQUENCE (if API returns it) ── */}
                {sequence.length > 0 ? (
                    <div
                        style={{
                            borderRadius: 14,
                            border: "1px solid rgba(255,255,255,0.08)",
                            background: "rgba(255,255,255,0.03)",
                            padding: "16px 20px",
                            marginBottom: 14,
                        }}
                    >
                        <div
                            style={{
                                fontSize: 9,
                                fontWeight: 900,
                                letterSpacing: 2,
                                textTransform: "uppercase" as const,
                                color: "rgba(255,255,255,0.28)",
                                marginBottom: 12,
                            }}
                        >
                            Full outreach sequence
                        </div>
                        <div
                            style={{
                                display: "flex",
                                flexDirection: "column" as const,
                                gap: 10,
                            }}
                        >
                            {sequence.map((step: any, si: number) => {
                                const stepText =
                                    typeof step === "string"
                                        ? step
                                        : String(
                                              step?.step ||
                                                  step?.message ||
                                                  step?.text ||
                                                  step?.content ||
                                                  JSON.stringify(step)
                                          )
                                return (
                                    <div
                                        key={si}
                                        style={{
                                            display: "flex",
                                            alignItems: "flex-start",
                                            gap: 12,
                                        }}
                                    >
                                        <div
                                            style={{
                                                width: 22,
                                                height: 22,
                                                borderRadius: "50%",
                                                background:
                                                    "rgba(254,176,106,0.15)",
                                                border: "1px solid rgba(254,176,106,0.35)",
                                                display: "flex",
                                                alignItems: "center",
                                                justifyContent: "center",
                                                fontSize: 11,
                                                fontWeight: 900,
                                                color: "#FEB06A",
                                                flexShrink: 0,
                                                marginTop: 1,
                                            }}
                                        >
                                            {si + 1}
                                        </div>
                                        <div style={{ flex: 1 }}>
                                            <div
                                                style={{
                                                    fontSize: 13,
                                                    lineHeight: "20px",
                                                    color: "rgba(255,255,255,0.70)",
                                                    marginBottom: 6,
                                                }}
                                            >
                                                {stepText}
                                            </div>
                                            <button
                                                onClick={() =>
                                                    copyText(
                                                        stepText,
                                                        `seq-${si}`
                                                    )
                                                }
                                                type="button"
                                                style={{
                                                    background: wasCopied(
                                                        `seq-${si}`
                                                    )
                                                        ? "rgba(74,222,128,0.15)"
                                                        : "rgba(255,255,255,0.07)",
                                                    border: wasCopied(
                                                        `seq-${si}`
                                                    )
                                                        ? "1px solid rgba(74,222,128,0.40)"
                                                        : "1px solid rgba(255,255,255,0.12)",
                                                    borderRadius: 8,
                                                    padding: "5px 12px",
                                                    fontSize: 11,
                                                    fontWeight: 900,
                                                    color: wasCopied(
                                                        `seq-${si}`
                                                    )
                                                        ? "#4ade80"
                                                        : "rgba(255,255,255,0.50)",
                                                    cursor: "pointer",
                                                    transition: "all 0.2s",
                                                }}
                                            >
                                                {wasCopied(`seq-${si}`)
                                                    ? "✓ Copied!"
                                                    : "⎘ Copy"}
                                            </button>
                                        </div>
                                    </div>
                                )
                            })}
                        </div>
                    </div>
                ) : null}

                {/* ── LINKEDIN SEARCH TIP ── */}
                <div
                    style={{
                        borderRadius: 14,
                        border: "1px solid rgba(255,255,255,0.08)",
                        background: "rgba(255,255,255,0.03)",
                        overflow: "hidden",
                        marginBottom: 20,
                    }}
                >
                    <div
                        onClick={() =>
                            setNetOpenSet((prev: Set<number>) => {
                                const next = new Set(prev)
                                if (next.has(99)) next.delete(99)
                                else next.add(99)
                                return next
                            })
                        }
                        style={{
                            padding: "14px 18px",
                            display: "flex",
                            alignItems: "center",
                            justifyContent: "space-between",
                            cursor: "pointer",
                        }}
                    >
                        <div
                            style={{
                                display: "flex",
                                alignItems: "center",
                                gap: 12,
                            }}
                        >
                            <div style={{ fontSize: 18 }}>🔍</div>
                            <div>
                                <div
                                    style={{
                                        fontSize: 13,
                                        fontWeight: 800,
                                        color: "rgba(255,255,255,0.70)",
                                    }}
                                >
                                    Need help finding these people on LinkedIn?
                                </div>
                                <div
                                    style={{
                                        fontSize: 11,
                                        color: "rgba(255,255,255,0.35)",
                                        marginTop: 2,
                                    }}
                                >
                                    Tap to see search tips and exact queries to
                                    use
                                </div>
                            </div>
                        </div>
                        <div
                            style={{
                                fontSize: 12,
                                color: "rgba(255,255,255,0.35)",
                            }}
                        >
                            {netOpenSet.has(99) ? "▾" : "▸"}
                        </div>
                    </div>
                    {netOpenSet.has(99) ? (
                        <div
                            style={{
                                padding: "0 18px 16px",
                                borderTop: "1px solid rgba(255,255,255,0.06)",
                            }}
                        >
                            <div
                                style={{
                                    paddingTop: 14,
                                    display: "flex",
                                    flexDirection: "column" as const,
                                    gap: 10,
                                }}
                            >
                                {moves.map((m: any, i: number) => {
                                    const c = stepColors[i] || stepColors[2]
                                    const queries = Array.isArray(
                                        m?.linkedin_search_queries
                                    )
                                        ? m.linkedin_search_queries
                                        : []
                                    if (!queries.length) return null
                                    return (
                                        <div key={i}>
                                            <div
                                                style={{
                                                    fontSize: 10,
                                                    fontWeight: 900,
                                                    color: c.accent,
                                                    marginBottom: 6,
                                                    letterSpacing: 0.5,
                                                }}
                                            >
                                                Step {i + 1}
                                            </div>
                                            {queries.map(
                                                (q: any, qi: number) => {
                                                    const qStr =
                                                        String(q).trim()
                                                    const liUrl = `https://www.linkedin.com/search/results/people/?keywords=${encodeURIComponent(qStr)}`
                                                    return (
                                                        <div
                                                            key={qi}
                                                            style={{
                                                                display: "flex",
                                                                alignItems:
                                                                    "center",
                                                                gap: 8,
                                                                marginBottom: 6,
                                                            }}
                                                        >
                                                            <div
                                                                style={{
                                                                    flex: 1,
                                                                    fontSize: 13,
                                                                    lineHeight:
                                                                        "19px",
                                                                    color: "rgba(255,255,255,0.60)",
                                                                    padding:
                                                                        "6px 10px",
                                                                    background:
                                                                        "rgba(255,255,255,0.04)",
                                                                    borderRadius: 8,
                                                                    fontFamily:
                                                                        "monospace",
                                                                }}
                                                            >
                                                                {qStr}
                                                            </div>
                                                            <button
                                                                onClick={() =>
                                                                    copyText(
                                                                        qStr,
                                                                        `q-${i}-${qi}`
                                                                    )
                                                                }
                                                                type="button"
                                                                title="Copy search query"
                                                                style={{
                                                                    background:
                                                                        wasCopied(
                                                                            `q-${i}-${qi}`
                                                                        )
                                                                            ? "rgba(74,222,128,0.15)"
                                                                            : "rgba(255,255,255,0.07)",
                                                                    border: wasCopied(
                                                                        `q-${i}-${qi}`
                                                                    )
                                                                        ? "1px solid rgba(74,222,128,0.40)"
                                                                        : "1px solid rgba(255,255,255,0.12)",
                                                                    borderRadius: 8,
                                                                    padding:
                                                                        "6px 10px",
                                                                    fontSize: 11,
                                                                    fontWeight: 900,
                                                                    color: wasCopied(
                                                                        `q-${i}-${qi}`
                                                                    )
                                                                        ? "#4ade80"
                                                                        : "rgba(255,255,255,0.50)",
                                                                    cursor: "pointer",
                                                                    flexShrink: 0,
                                                                    transition:
                                                                        "all 0.2s",
                                                                }}
                                                            >
                                                                {wasCopied(
                                                                    `q-${i}-${qi}`
                                                                )
                                                                    ? "✓"
                                                                    : "⎘"}
                                                            </button>
                                                            <a
                                                                href={liUrl}
                                                                target="_blank"
                                                                rel="noopener noreferrer"
                                                                title="Search on LinkedIn"
                                                                style={{
                                                                    background:
                                                                        "rgba(10,102,194,0.20)",
                                                                    border: "1px solid rgba(10,102,194,0.45)",
                                                                    borderRadius: 8,
                                                                    padding:
                                                                        "6px 10px",
                                                                    fontSize: 11,
                                                                    fontWeight: 900,
                                                                    color: "#5ba4cf",
                                                                    cursor: "pointer",
                                                                    textDecoration:
                                                                        "none",
                                                                    flexShrink: 0,
                                                                }}
                                                            >
                                                                Search →
                                                            </a>
                                                        </div>
                                                    )
                                                }
                                            )}
                                        </div>
                                    )
                                })}
                            </div>
                        </div>
                    ) : null}
                </div>
            </>
        )
    }
    /* ---------- AUTH GATE ---------- */
    const shouldShowAuthGate = !isLoggedIn && accessMode !== "jobfit_only"

    if (authStatus === "checking" && accessMode !== "jobfit_only") {
        return (
            <div style={outerFrame}>
                <div
                    style={{
                        width: "100%",
                        height: "100%",
                        display: "flex",
                        alignItems: "center",
                        justifyContent: "center",
                        flexDirection: "column" as const,
                        gap: 16,
                        background: BG,
                    }}
                >
                    <div
                        style={{
                            fontSize: 28,
                            fontWeight: 950,
                            color: WRN_ORANGE,
                            letterSpacing: -0.5,
                        }}
                    >
                        SIGNAL
                    </div>
                    <div
                        style={{
                            fontSize: 13,
                            color: MUTED,
                            letterSpacing: 0.3,
                        }}
                    >
                        Loading your session…
                    </div>
                </div>
            </div>
        )
    }

    if (shouldShowAuthGate) {
        return (
            <div style={outerFrame}>
                <div
                    style={{
                        ...shell,
                        // Block layout with vertical padding instead of flex
                        // center. Flex center with alignItems: center cuts
                        // off content that exceeds the container height
                        // (classic flexbox gotcha) — and the login screen
                        // can exceed viewport on small windows. Block
                        // layout lets the inner div center horizontally
                        // via margin: 0 auto while the natural document
                        // flow handles vertical overflow gracefully.
                        display: "block",
                        paddingTop: 48,
                        paddingBottom: 48,
                    }}
                >
                    <div
                        style={{
                            width: "100%",
                            maxWidth: 780,
                            padding: "16px 24px 40px",
                            margin: "0 auto",
                        }}
                    >
                        {/* Logo */}
                        <div style={{ textAlign: "center", marginBottom: 32 }}>
                            <div
                                style={{
                                    display: "inline-flex",
                                    alignItems: "center",
                                    marginBottom: 6,
                                }}
                            >
                                <span
                                    style={{
                                        fontSize: 36,
                                        fontWeight: 950,
                                        fontStyle: "italic",
                                        letterSpacing: -1,
                                        color: "#fff",
                                        lineHeight: 1,
                                    }}
                                >
                                    SIGNAL
                                </span>
                                <div
                                    style={{
                                        width: 20,
                                        height: 5,
                                        background: WRN_ORANGE,
                                        borderRadius: 1,
                                        marginLeft: 5,
                                        marginBottom: 2,
                                    }}
                                />
                            </div>
                            <div
                                style={{
                                    display: "flex",
                                    alignItems: "flex-start",
                                    justifyContent: "flex-start",
                                    gap: 5,
                                }}
                            >
                                <span
                                    style={{
                                        fontSize: 10,
                                        fontWeight: 700,
                                        color: "rgba(255,255,255,0.30)",
                                    }}
                                >
                                    by
                                </span>
                                <span
                                    style={{
                                        fontSize: 10,
                                        fontWeight: 900,
                                        letterSpacing: 0.8,
                                        color: WRN_ORANGE,
                                        textTransform: "uppercase" as const,
                                    }}
                                >
                                    Workforce
                                </span>
                                <span
                                    style={{
                                        fontSize: 10,
                                        fontWeight: 900,
                                        letterSpacing: 0.8,
                                        color: WRN_BLUE,
                                        textTransform: "uppercase" as const,
                                    }}
                                >
                                    Ready Now
                                </span>
                            </div>
                        </div>

                        {/* Headline */}
                        <div
                            style={{
                                fontSize: 32,
                                fontWeight: 950,
                                color: "#fff",
                                textAlign: "center",
                                letterSpacing: -0.5,
                                lineHeight: 1.1,
                                marginBottom: 8,
                            }}
                        >
                            The decision engine for{" "}
                            <span style={{ color: WRN_ORANGE }}>
                                smarter applications
                            </span>
                        </div>

                        {/* Subhead */}
                        <div
                            style={{
                                fontSize: 15,
                                color: MUTED,
                                textAlign: "center",
                                lineHeight: 1.6,
                                marginBottom: 32,
                                maxWidth: 520,
                                marginLeft: "auto",
                                marginRight: "auto",
                            }}
                        >
                            Choose the access type that matches your account and
                            enter your email to get started.
                        </div>

                        {/* Two cards */}
                        <div
                            style={{
                                display: "grid",
                                gridTemplateColumns:
                                    "repeat(2, minmax(0, 1fr))",
                                gap: 16,
                            }}
                        >
                            {/* Full Access */}
                            <div
                                style={{
                                    borderRadius: 20,
                                    padding: 24,
                                    border: `1px solid ${BORDER_SOFT}`,
                                    background: CARD,
                                    borderTop: `3px solid ${WRN_ORANGE}`,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 1.8,
                                        textTransform: "uppercase" as const,
                                        color: WRN_ORANGE,
                                        marginBottom: 8,
                                    }}
                                >
                                    Full Access
                                </div>
                                <div
                                    style={{
                                        fontSize: 20,
                                        fontWeight: 900,
                                        color: TEXT,
                                        marginBottom: 6,
                                    }}
                                >
                                    Paid account
                                </div>
                                <div
                                    style={{
                                        fontSize: 13,
                                        color: MUTED,
                                        lineHeight: "20px",
                                        marginBottom: 20,
                                        minHeight: 42,
                                    }}
                                >
                                    Use the email tied to your paid SIGNAL
                                    account. We will send you a magic link to
                                    sign in.
                                </div>
                                <label
                                    style={{
                                        fontSize: 11,
                                        fontWeight: 900,
                                        letterSpacing: 0.5,
                                        color: DIM,
                                        marginBottom: 6,
                                        display: "block",
                                    }}
                                >
                                    Full access account email
                                </label>
                                <input
                                    style={input}
                                    value={email}
                                    onChange={(e) => setEmail(e.target.value)}
                                    placeholder="you@example.com"
                                    type="email"
                                    id="signal-full-access-email"
                                    name="full-access-email"
                                    autoComplete="off"
                                    inputMode="email"
                                />
                                <div style={{ height: 14 }} />
                                <button
                                    style={{
                                        width: "100%",
                                        padding: "13px",
                                        borderRadius: 13,
                                        background:
                                            "linear-gradient(90deg,#FEB06A,#51ADE5)",
                                        fontWeight: 900,
                                        fontSize: 14,
                                        border: "none",
                                        cursor:
                                            authStatus === "email_sent"
                                                ? "default"
                                                : "pointer",
                                        color: "#04060F",
                                        opacity:
                                            authStatus === "email_sent"
                                                ? 0.4
                                                : 1,
                                    }}
                                    onClick={sendMagicLink}
                                    disabled={magicLinkSent}
                                    type="button"
                                >
                                    Send Magic Link
                                </button>
                                {magicLinkSent ? (
                                    <div
                                        style={{
                                            marginTop: 14,
                                            padding: "16px 18px",
                                            borderRadius: 14,
                                            background: "rgba(74,222,128,0.10)",
                                            border: "1.5px solid rgba(74,222,128,0.40)",
                                        }}
                                    >
                                        <div
                                            style={{
                                                fontSize: 15,
                                                fontWeight: 900,
                                                color: "#4ade80",
                                                marginBottom: 5,
                                            }}
                                        >
                                            ✓ Link sent — check your email
                                        </div>
                                        <div
                                            style={{
                                                fontSize: 13,
                                                color: "rgba(255,255,255,0.60)",
                                                lineHeight: "19px",
                                            }}
                                        >
                                            Click the link in your inbox to sign
                                            in. Check spam if you don't see it.
                                            Don't click Send again — it won't
                                            work for 60 seconds.
                                        </div>
                                    </div>
                                ) : null}
                            </div>

                            {/* Trial */}
                            <div
                                style={{
                                    borderRadius: 20,
                                    padding: 24,
                                    border: `1px solid ${BORDER_SOFT}`,
                                    background: CARD,
                                    borderTop: `3px solid ${WRN_BLUE}`,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 1.8,
                                        textTransform: "uppercase" as const,
                                        color: WRN_BLUE,
                                        marginBottom: 8,
                                    }}
                                >
                                    Free Trial
                                </div>
                                <div
                                    style={{
                                        fontSize: 20,
                                        fontWeight: 900,
                                        color: TEXT,
                                        marginBottom: 6,
                                    }}
                                >
                                    Trial user
                                </div>
                                <div
                                    style={{
                                        fontSize: 13,
                                        color: MUTED,
                                        lineHeight: "20px",
                                        marginBottom: 20,
                                        minHeight: 42,
                                    }}
                                >
                                    Use the email tied to your saved trial
                                    profile. Only for users who already started
                                    a SIGNAL trial.
                                </div>
                                <label
                                    style={{
                                        fontSize: 11,
                                        fontWeight: 900,
                                        letterSpacing: 0.5,
                                        color: DIM,
                                        marginBottom: 6,
                                        display: "block",
                                    }}
                                >
                                    Trial user email
                                </label>
                                <input
                                    style={input}
                                    value={trialEmail}
                                    onChange={(e) =>
                                        setTrialEmail(e.target.value)
                                    }
                                    placeholder="you@example.com"
                                    type="email"
                                    id="signal-trial-email"
                                    name="trial-user-email"
                                    autoComplete="off"
                                    inputMode="email"
                                />
                                <div style={{ height: 14 }} />
                                <button
                                    style={{
                                        width: "100%",
                                        padding: "13px",
                                        borderRadius: 13,
                                        background:
                                            "linear-gradient(90deg,#51ADE5,#218C8C)",
                                        fontWeight: 900,
                                        fontSize: 14,
                                        border: "none",
                                        cursor: "pointer",
                                        color: "#04060F",
                                    }}
                                    onClick={continueTrial}
                                    type="button"
                                >
                                    Continue Trial Access
                                </button>
                            </div>
                        </div>

                        {/* Trust row */}
                        <div
                            style={{
                                display: "flex",
                                alignItems: "center",
                                justifyContent: "center",
                                gap: 16,
                                marginTop: 24,
                                flexWrap: "wrap" as const,
                            }}
                        >
                            {[
                                "No password required",
                                "Magic link login",
                                "Secure session",
                            ].map((t, i, arr) => (
                                <React.Fragment key={t}>
                                    <div
                                        style={{
                                            fontSize: 11,
                                            fontWeight: 700,
                                            color: "rgba(255,255,255,0.28)",
                                        }}
                                    >
                                        {t}
                                    </div>
                                    {i < arr.length - 1 ? (
                                        <div
                                            style={{
                                                width: 4,
                                                height: 4,
                                                borderRadius: "50%",
                                                background:
                                                    "rgba(255,255,255,0.18)",
                                            }}
                                        />
                                    ) : null}
                                </React.Fragment>
                            ))}
                        </div>

                        {errorMsg ? (
                            <div
                                style={{
                                    ...errorBox,
                                    marginTop: 14,
                                    textAlign: "center",
                                }}
                            >
                                {errorMsg}
                            </div>
                        ) : null}
                    </div>
                </div>
            </div>
        )
    }

    /* ---------- MAIN APP ---------- */

    const renderMainOutput = () => {
        if (tab === "jobfit") return renderJobFit()
        if (tab === "positioning") return renderPositioning()
        if (tab === "coverletter") return renderCoverLetter()
        return renderNetworking()
    }

    const showJobFitLandingCTA =
        tab === "jobfit" && !jobFitResult && !isLockedTab("jobfit")

    return (
        <div style={{ ...props.style, ...outerFrame }}>
            <style>{`
@keyframes wrnspin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes whyPulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(74,222,128,0); border-color: rgba(74,222,128,0.45); }
  50%       { box-shadow: 0 0 0 4px rgba(74,222,128,0.15); border-color: rgba(74,222,128,0.80); }
}
@keyframes riskPulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(248,113,113,0); border-color: rgba(248,113,113,0.45); }
  50%       { box-shadow: 0 0 0 4px rgba(248,113,113,0.15); border-color: rgba(248,113,113,0.80); }
}
`}</style>

            <div style={shell}>
                <SideNav
                    tab={tab}
                    accessMode={accessMode}
                    isLoggedIn={isLoggedIn}
                    authedEmail={authedEmail}
                    trialEmail={trialEmail} // <--- ADD THIS
                    onSelect={(t) => goTo(t)}
                    onRunNewJob={() => runNewJob()}
                    onLogout={logout}
                    onUpgrade={openUpgradePage}
                />

                <div style={main}>
                    <Header />

                    {/* HOME only: how it works */}
                    {!hasAnyResult ? (
                        <div
                            style={{
                                background: "#0d1b2e",
                                borderRadius: 14,
                                padding: "28px 32px 32px",
                                marginBottom: 26,
                            }}
                        >
                            {/* Eyebrow */}
                            <div
                                style={{
                                    fontSize: 10,
                                    letterSpacing: "0.14em",
                                    textTransform: "uppercase" as const,
                                    color: "#3d5878",
                                    fontWeight: 600,
                                    marginBottom: 24,
                                }}
                            >
                                How Signal works
                            </div>

                            {/* Track bar */}
                            <div
                                style={{
                                    display: "flex",
                                    alignItems: "center",
                                    marginBottom: 24,
                                }}
                            >
                                {[
                                    { n: 1, key: "jobfit" },
                                    { n: 2, key: "positioning" },
                                    { n: 3, key: "coverletter" },
                                    { n: 4, key: "networking" },
                                ].map((s, i) => {
                                    const tabIndex = [
                                        "jobfit",
                                        "positioning",
                                        "coverletter",
                                        "networking",
                                    ].indexOf(tab)
                                    const isActive = i === tabIndex
                                    const isDone = i < tabIndex
                                    return (
                                        <React.Fragment key={s.n}>
                                            <button
                                                onClick={() =>
                                                    goTo(s.key as TabKey)
                                                }
                                                type="button"
                                                style={{
                                                    width: 44,
                                                    height: 44,
                                                    borderRadius: "50%",
                                                    display: "flex",
                                                    alignItems: "center",
                                                    justifyContent: "center",
                                                    fontSize: 14,
                                                    fontWeight: 700,
                                                    flexShrink: 0,
                                                    cursor: isLockedTab(
                                                        s.key as TabKey
                                                    )
                                                        ? "default"
                                                        : "pointer",
                                                    border: isActive
                                                        ? "1.5px solid #f97316"
                                                        : isDone
                                                          ? "1.5px solid rgba(249,115,22,0.5)"
                                                          : "1.5px solid rgba(255,255,255,0.08)",
                                                    background: isActive
                                                        ? "#f97316"
                                                        : isDone
                                                          ? "rgba(249,115,22,0.15)"
                                                          : "rgba(255,255,255,0.03)",
                                                    color: isActive
                                                        ? "#fff"
                                                        : isDone
                                                          ? "#f97316"
                                                          : "#3d5878",
                                                    zIndex: 2,
                                                    padding: 0,
                                                }}
                                            >
                                                {s.n}
                                            </button>
                                            {i < 3 && (
                                                <div
                                                    style={{
                                                        flex: 1,
                                                        height: 1,
                                                        background:
                                                            i < tabIndex
                                                                ? "rgba(249,115,22,0.45)"
                                                                : "rgba(255,255,255,0.07)",
                                                        zIndex: 1,
                                                    }}
                                                />
                                            )}
                                        </React.Fragment>
                                    )
                                })}
                            </div>

                            {/* Step cards */}
                            <div
                                style={{
                                    display: "grid",
                                    gridTemplateColumns:
                                        "repeat(4, minmax(0, 1fr))",
                                    gap: 10,
                                }}
                            >
                                {(accessMode === "jobfit_only"
                                    ? [
                                          {
                                              n: 1,
                                              key: "jobfit",
                                              tag: "Free",
                                              head: "JobFit",
                                              desc: "Tells you your competitive signal so you stop wasting applications.",
                                              locked: false,
                                          },
                                          {
                                              n: 2,
                                              key: "positioning",
                                              tag: "Upgrade",
                                              head: "Positioning",
                                              desc: "One targeted story written to get through ATS and the recruiter's 7-second scan.",
                                              locked: true,
                                          },
                                          {
                                              n: 3,
                                              key: "coverletter",
                                              tag: "Upgrade",
                                              head: "Cover Letter",
                                              desc: "Written in your voice, tied to your real background and this specific job.",
                                              locked: true,
                                          },
                                          {
                                              n: 4,
                                              key: "networking",
                                              tag: "Upgrade",
                                              head: "Networking",
                                              desc: "Who to contact, what to say, and how to be appropriately aggressive.",
                                              locked: true,
                                          },
                                      ]
                                    : [
                                          {
                                              n: 1,
                                              key: "jobfit",
                                              tag: "Decision",
                                              head: "JobFit",
                                              desc: "Know your competitive signal before spending hours on the wrong role.",
                                              locked: false,
                                          },
                                          {
                                              n: 2,
                                              key: "positioning",
                                              tag: "Resume",
                                              head: "Positioning",
                                              desc: "Restructures your bullets to match the job and pass the ATS scan.",
                                              locked: false,
                                          },
                                          {
                                              n: 3,
                                              key: "coverletter",
                                              tag: "Letter",
                                              head: "Cover Letter",
                                              desc: "Generates a letter only when it is actually worth writing one.",
                                              locked: false,
                                          },
                                          {
                                              n: 4,
                                              key: "networking",
                                              tag: "Outreach",
                                              head: "Networking",
                                              desc: "Post-apply plan with copy-paste outreach ready to send.",
                                              locked: false,
                                          },
                                      ]
                                ).map((s) => {
                                    const tabIndex = [
                                        "jobfit",
                                        "positioning",
                                        "coverletter",
                                        "networking",
                                    ].indexOf(tab)
                                    const stepIndex = [
                                        "jobfit",
                                        "positioning",
                                        "coverletter",
                                        "networking",
                                    ].indexOf(s.key)
                                    const isActive = stepIndex === tabIndex
                                    const isDone = stepIndex < tabIndex
                                    return (
                                        <button
                                            key={s.n}
                                            onClick={() =>
                                                !s.locked &&
                                                goTo(s.key as TabKey)
                                            }
                                            type="button"
                                            style={{
                                                padding: "16px 14px 14px",
                                                borderRadius: 8,
                                                borderTop: isActive
                                                    ? "2px solid #f97316"
                                                    : isDone
                                                      ? "2px solid rgba(249,115,22,0.3)"
                                                      : "2px solid rgba(255,255,255,0.06)",
                                                border: "none",
                                                background: isActive
                                                    ? "rgba(249,115,22,0.1)"
                                                    : isDone
                                                      ? "rgba(249,115,22,0.04)"
                                                      : "rgba(255,255,255,0.025)",
                                                cursor: s.locked
                                                    ? "default"
                                                    : "pointer",
                                                textAlign: "left" as const,
                                                opacity: s.locked ? 0.55 : 1,
                                            }}
                                        >
                                            <div
                                                style={{
                                                    fontSize: 9,
                                                    letterSpacing: "0.1em",
                                                    textTransform:
                                                        "uppercase" as const,
                                                    fontWeight: 600,
                                                    color:
                                                        isActive || isDone
                                                            ? "#f97316"
                                                            : "#3d5878",
                                                    marginBottom: 8,
                                                }}
                                            >
                                                {s.tag}
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 14,
                                                    fontWeight: 600,
                                                    color: isActive
                                                        ? "#f0f4ff"
                                                        : isDone
                                                          ? "#8fa5be"
                                                          : "#4a5f78",
                                                    marginBottom: 6,
                                                }}
                                            >
                                                {s.head}
                                            </div>
                                            <div
                                                style={{
                                                    fontSize: 11,
                                                    color: isActive
                                                        ? "#7a9ab8"
                                                        : isDone
                                                          ? "#3d5878"
                                                          : "#2e4259",
                                                    lineHeight: "1.65",
                                                }}
                                            >
                                                {s.desc}
                                            </div>
                                        </button>
                                    )
                                })}
                            </div>

                            {/* Upgrade footer — jobfit_only mode only */}
                            {accessMode === "jobfit_only" && (
                                <div
                                    style={{
                                        marginTop: 16,
                                        paddingTop: 16,
                                        borderTop:
                                            "1px solid rgba(255,255,255,0.06)",
                                        display: "flex",
                                        alignItems: "center",
                                        justifyContent: "space-between",
                                        flexWrap: "wrap" as const,
                                        gap: 10,
                                    }}
                                >
                                    <div
                                        style={{
                                            fontSize: 12,
                                            color: "#3d5878",
                                        }}
                                    >
                                        Steps 2-4 require Full SIGNAL access.
                                    </div>
                                    <button
                                        onClick={openUpgradePage}
                                        type="button"
                                        style={{
                                            padding: "8px 16px",
                                            borderRadius: 10,
                                            background:
                                                "linear-gradient(135deg,#FEB06A,#51ADE5)",
                                            border: "none",
                                            fontWeight: 900,
                                            fontSize: 12,
                                            color: "#0B1020",
                                            cursor: "pointer",
                                        }}
                                    >
                                        Unlock Full SIGNAL →
                                    </button>
                                </div>
                            )}
                        </div>
                    ) : null}

                    {/* JobFit landing CTA only, and only below the general step notes */}

                    {/* Trial instruction block */}
                    {!hasAnyResult && accessMode === "jobfit_only" ? (
                        // ─────────────────────────────────────────────────────────────────────────────
                        // JOBFIT LANDING REDESIGN — matches intake form dark style
                        // There are TWO identical blocks to replace in the component:
                        //
                        // BLOCK 1 — Trial/free access version (around line 5910)
                        //   Find: background: "#FFFFFF", borderRadius: 18, padding: "24px 26px"  (first occurrence)
                        //   Replace from the opening <div style={{ background: "#FFFFFF" ... to the closing </div>
                        //   that ends with ) : null} (the trial block conditional close)
                        //
                        // BLOCK 2 — Full access version (around line 6150)
                        //   Find: background: "#FFFFFF", borderRadius: 18, padding: "24px 26px" (second occurrence)
                        //   Replace from the opening <div style={{ background: "#FFFFFF" ... to its closing </div>
                        //   that ends with ) : null} (the full access block conditional close)
                        //
                        // USE THE SAME JSX BELOW FOR BOTH BLOCKS
                        // ─────────────────────────────────────────────────────────────────────────────

                        <div
                            style={{
                                borderRadius: 18,
                                border: "1px solid rgba(81,173,229,0.20)",
                                background: "#0D1829",
                                overflow: "hidden",
                                marginBottom: 16,
                                marginTop: 4,
                                boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                                position: "relative" as const,
                            }}
                        >
                            {/* ambient orbs */}
                            <div
                                style={{
                                    position: "absolute" as const,
                                    top: -40,
                                    left: -40,
                                    width: 220,
                                    height: 220,
                                    borderRadius: "50%",
                                    background: "rgba(81,173,229,0.07)",
                                    animation:
                                        "orbFloat1 8s ease-in-out infinite",
                                    pointerEvents: "none" as const,
                                }}
                            />
                            <div
                                style={{
                                    position: "absolute" as const,
                                    bottom: -20,
                                    right: -20,
                                    width: 180,
                                    height: 180,
                                    borderRadius: "50%",
                                    background: "rgba(254,176,106,0.06)",
                                    animation:
                                        "orbFloat2 9s ease-in-out infinite",
                                    pointerEvents: "none" as const,
                                }}
                            />
                            {/* gradient top bar */}
                            <div
                                style={{
                                    height: 3,
                                    background:
                                        "linear-gradient(90deg,#FEB06A,#f97316,#51ADE5)",
                                }}
                            />
                            <div
                                style={{
                                    padding: "22px 24px 24px",
                                    position: "relative" as const,
                                    zIndex: 1,
                                    textAlign: "center" as const,
                                }}
                            >
                                {/* eyebrow */}
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 2,
                                        textTransform: "uppercase" as const,
                                        color: "rgba(254,176,106,0.70)",
                                        marginBottom: 16,
                                    }}
                                >
                                    How to use Job Fit
                                </div>
                                {/* headline */}
                                <div
                                    style={{
                                        fontSize: 38,
                                        fontWeight: 950,
                                        color: "rgba(255,255,255,0.97)",
                                        lineHeight: 1.1,
                                        letterSpacing: -1.2,
                                        marginBottom: 10,
                                    }}
                                >
                                    Found a job you're thinking
                                    <br />
                                    about{" "}
                                    <span style={{ color: "#FEB06A" }}>
                                        applying
                                    </span>{" "}
                                    to?
                                </div>
                                <div
                                    style={{
                                        fontSize: 22,
                                        fontWeight: 950,
                                        letterSpacing: -0.5,
                                        marginBottom: 18,
                                        lineHeight: 1.2,
                                    }}
                                >
                                    <span style={{ color: "#FEB06A" }}>
                                        Start here
                                    </span>
                                    <span
                                        style={{
                                            color: "rgba(255,255,255,0.90)",
                                        }}
                                    >
                                        {" "}
                                        — before you apply.
                                    </span>
                                </div>
                                {/* subhead */}
                                <div
                                    style={{
                                        fontSize: 14,
                                        lineHeight: "22px",
                                        color: "rgba(255,255,255,0.45)",
                                        marginBottom: 28,
                                        maxWidth: 500,
                                        margin: "0 auto 28px",
                                    }}
                                >
                                    Most students apply to jobs that were never
                                    a real fit. SIGNAL evaluates the match
                                    between your background and a specific role
                                    — before you spend hours on an application
                                    going nowhere.
                                </div>
                                {/* steps */}
                                <div
                                    style={{
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 10,
                                        marginBottom: 22,
                                        textAlign: "left" as const,
                                    }}
                                >
                                    {[
                                        {
                                            n: "1",
                                            head: "Run JobFit",
                                            body: "Paste the job description and get a clear verdict — Apply, Review, or Pass — before you spend a minute on an application.",
                                        },
                                        {
                                            n: "2",
                                            head: "Run Positioning",
                                            body: "Rewrite your resume bullets to match this specific role. One targeted story, in the language the recruiter is scanning for.",
                                        },
                                        {
                                            n: "3",
                                            head: "Run Cover Letter",
                                            body: "Write a letter in your voice that connects your background to this company — not a template, a real case for why you fit.",
                                        },
                                        {
                                            n: "4",
                                            head: "Run Networking",
                                            body: "Build your outreach plan. Know who to contact, what to say, and how to follow up so your name is known before the interview.",
                                        },
                                    ].map((s, idx) => {
                                        const isActive = idx === 0
                                        const isDone = idx < 0
                                        return (
                                            <div
                                                key={s.n}
                                                style={{
                                                    display: "flex",
                                                    alignItems: "flex-start",
                                                    gap: 14,
                                                    opacity: isActive
                                                        ? 1
                                                        : isDone
                                                          ? 0.55
                                                          : 0.3,
                                                }}
                                            >
                                                <div
                                                    style={{
                                                        width: isActive
                                                            ? 36
                                                            : 28,
                                                        height: isActive
                                                            ? 36
                                                            : 28,
                                                        borderRadius: "50%",
                                                        background: isActive
                                                            ? "rgba(254,176,106,0.25)"
                                                            : isDone
                                                              ? "rgba(74,222,128,0.15)"
                                                              : "rgba(255,255,255,0.06)",
                                                        border: isActive
                                                            ? "2px solid rgba(254,176,106,0.70)"
                                                            : isDone
                                                              ? "2px solid rgba(74,222,128,0.50)"
                                                              : "2px solid rgba(255,255,255,0.15)",
                                                        display: "flex",
                                                        alignItems: "center",
                                                        justifyContent:
                                                            "center",
                                                        fontSize: isActive
                                                            ? 15
                                                            : 12,
                                                        fontWeight: 900,
                                                        color: isActive
                                                            ? "#FEB06A"
                                                            : isDone
                                                              ? "#4ade80"
                                                              : "rgba(255,255,255,0.35)",
                                                        flexShrink: 0,
                                                        marginTop: 1,
                                                    }}
                                                >
                                                    {s.n}
                                                </div>
                                                <div>
                                                    <div
                                                        style={{
                                                            fontSize: isActive
                                                                ? 16
                                                                : 13,
                                                            fontWeight: isActive
                                                                ? 900
                                                                : 700,
                                                            color: isActive
                                                                ? "#FEB06A"
                                                                : isDone
                                                                  ? "rgba(255,255,255,0.75)"
                                                                  : "rgba(255,255,255,0.40)",
                                                            marginBottom: 3,
                                                        }}
                                                    >
                                                        {s.head}
                                                    </div>
                                                    <div
                                                        style={{
                                                            fontSize: 12,
                                                            lineHeight: "18px",
                                                            color: isActive
                                                                ? "rgba(255,255,255,0.55)"
                                                                : "rgba(255,255,255,0.22)",
                                                        }}
                                                    >
                                                        {s.body}
                                                    </div>
                                                </div>
                                            </div>
                                        )
                                    })}
                                </div>
                                {/* what you get */}
                                <div
                                    style={{
                                        borderRadius: 14,
                                        border: "1px solid rgba(74,222,128,0.18)",
                                        background: "rgba(74,222,128,0.05)",
                                        padding: "14px 16px",
                                    }}
                                >
                                    <div
                                        style={{
                                            fontSize: 9,
                                            fontWeight: 900,
                                            letterSpacing: 2,
                                            textTransform: "uppercase" as const,
                                            color: "rgba(74,222,128,0.65)",
                                            marginBottom: 10,
                                        }}
                                    >
                                        What Job Fit tells you
                                    </div>
                                    <div
                                        style={{
                                            display: "flex",
                                            flexDirection: "column" as const,
                                            gap: 8,
                                        }}
                                    >
                                        {[
                                            "Apply, Review, or Pass — a clear decision on whether this role is worth your time",
                                            "Your fit score out of 100 based on your actual background",
                                            "Why you are competitive — the specific signals that work in your favor",
                                            "Where you are exposed — the gaps a hiring manager will notice",
                                            "Your exact next move based on the verdict",
                                        ].map((item, i) => (
                                            <div
                                                key={i}
                                                style={{
                                                    display: "flex",
                                                    alignItems: "flex-start",
                                                    gap: 10,
                                                }}
                                            >
                                                <div
                                                    style={{
                                                        width: 18,
                                                        height: 18,
                                                        borderRadius: "50%",
                                                        background:
                                                            "rgba(74,222,128,0.15)",
                                                        border: "1.5px solid rgba(74,222,128,0.45)",
                                                        display: "flex",
                                                        alignItems: "center",
                                                        justifyContent:
                                                            "center",
                                                        fontSize: 10,
                                                        fontWeight: 900,
                                                        color: "#4ade80",
                                                        flexShrink: 0,
                                                        marginTop: 1,
                                                        animation:
                                                            "whyPulse 2.5s ease-in-out infinite",
                                                        animationDelay: `${i * 0.25}s`,
                                                    }}
                                                >
                                                    ✓
                                                </div>
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: "rgba(255,255,255,0.65)",
                                                    }}
                                                >
                                                    {item}
                                                </div>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            </div>
                        </div>
                    ) : null}

                    {/* Full access instruction block */}
                    {!hasAnyResult &&
                    accessMode !== "jobfit_only" &&
                    isLoggedIn ? (
                        // ─────────────────────────────────────────────────────────────────────────────
                        // JOBFIT LANDING REDESIGN — matches intake form dark style
                        // There are TWO identical blocks to replace in the component:
                        //
                        // BLOCK 1 — Trial/free access version (around line 5910)
                        //   Find: background: "#FFFFFF", borderRadius: 18, padding: "24px 26px"  (first occurrence)
                        //   Replace from the opening <div style={{ background: "#FFFFFF" ... to the closing </div>
                        //   that ends with ) : null} (the trial block conditional close)
                        //
                        // BLOCK 2 — Full access version (around line 6150)
                        //   Find: background: "#FFFFFF", borderRadius: 18, padding: "24px 26px" (second occurrence)
                        //   Replace from the opening <div style={{ background: "#FFFFFF" ... to its closing </div>
                        //   that ends with ) : null} (the full access block conditional close)
                        //
                        // USE THE SAME JSX BELOW FOR BOTH BLOCKS
                        // ─────────────────────────────────────────────────────────────────────────────

                        <div
                            style={{
                                borderRadius: 18,
                                border: "1px solid rgba(81,173,229,0.20)",
                                background: "#0D1829",
                                overflow: "hidden",
                                marginBottom: 16,
                                marginTop: 4,
                                boxShadow: "0 20px 60px rgba(0,0,0,0.5)",
                                position: "relative" as const,
                            }}
                        >
                            {/* ambient orbs */}
                            <div
                                style={{
                                    position: "absolute" as const,
                                    top: -40,
                                    left: -40,
                                    width: 220,
                                    height: 220,
                                    borderRadius: "50%",
                                    background: "rgba(81,173,229,0.07)",
                                    animation:
                                        "orbFloat1 8s ease-in-out infinite",
                                    pointerEvents: "none" as const,
                                }}
                            />
                            <div
                                style={{
                                    position: "absolute" as const,
                                    bottom: -20,
                                    right: -20,
                                    width: 180,
                                    height: 180,
                                    borderRadius: "50%",
                                    background: "rgba(254,176,106,0.06)",
                                    animation:
                                        "orbFloat2 9s ease-in-out infinite",
                                    pointerEvents: "none" as const,
                                }}
                            />
                            {/* gradient top bar */}
                            <div
                                style={{
                                    height: 3,
                                    background:
                                        "linear-gradient(90deg,#FEB06A,#f97316,#51ADE5)",
                                }}
                            />
                            <div
                                style={{
                                    padding: "22px 24px 24px",
                                    position: "relative" as const,
                                    zIndex: 1,
                                    textAlign: "center" as const,
                                }}
                            >
                                {/* eyebrow */}
                                <div
                                    style={{
                                        fontSize: 9,
                                        fontWeight: 900,
                                        letterSpacing: 2,
                                        textTransform: "uppercase" as const,
                                        color: "rgba(254,176,106,0.70)",
                                        marginBottom: 16,
                                    }}
                                >
                                    How to use Job Fit
                                </div>
                                {/* headline */}
                                <div
                                    style={{
                                        fontSize: 38,
                                        fontWeight: 950,
                                        color: "rgba(255,255,255,0.97)",
                                        lineHeight: 1.1,
                                        letterSpacing: -1.2,
                                        marginBottom: 10,
                                    }}
                                >
                                    Found a job you're thinking
                                    <br />
                                    about{" "}
                                    <span style={{ color: "#FEB06A" }}>
                                        applying
                                    </span>{" "}
                                    to?
                                </div>
                                <div
                                    style={{
                                        fontSize: 22,
                                        fontWeight: 950,
                                        letterSpacing: -0.5,
                                        marginBottom: 18,
                                        lineHeight: 1.2,
                                    }}
                                >
                                    <span style={{ color: "#FEB06A" }}>
                                        Start here
                                    </span>
                                    <span
                                        style={{
                                            color: "rgba(255,255,255,0.90)",
                                        }}
                                    >
                                        {" "}
                                        — before you apply.
                                    </span>
                                </div>
                                {/* subhead */}
                                <div
                                    style={{
                                        fontSize: 14,
                                        lineHeight: "22px",
                                        color: "rgba(255,255,255,0.45)",
                                        marginBottom: 28,
                                        maxWidth: 500,
                                        margin: "0 auto 28px",
                                    }}
                                >
                                    Most students apply to jobs that were never
                                    a real fit. SIGNAL evaluates the match
                                    between your background and a specific role
                                    — before you spend hours on an application
                                    going nowhere.
                                </div>
                                {/* steps */}
                                <div
                                    style={{
                                        display: "flex",
                                        flexDirection: "column" as const,
                                        gap: 10,
                                        marginBottom: 22,
                                        textAlign: "left" as const,
                                    }}
                                >
                                    {[
                                        {
                                            n: "1",
                                            head: "Run JobFit",
                                            body: "Paste the job description and get a clear verdict — Apply, Review, or Pass — before you spend a minute on an application.",
                                        },
                                        {
                                            n: "2",
                                            head: "Run Positioning",
                                            body: "Rewrite your resume bullets to match this specific role. One targeted story, in the language the recruiter is scanning for.",
                                        },
                                        {
                                            n: "3",
                                            head: "Run Cover Letter",
                                            body: "Write a letter in your voice that connects your background to this company — not a template, a real case for why you fit.",
                                        },
                                        {
                                            n: "4",
                                            head: "Run Networking",
                                            body: "Build your outreach plan. Know who to contact, what to say, and how to follow up so your name is known before the interview.",
                                        },
                                    ].map((s, idx) => {
                                        const isActive = idx === 0
                                        const isDone = idx < 0
                                        return (
                                            <div
                                                key={s.n}
                                                style={{
                                                    display: "flex",
                                                    alignItems: "flex-start",
                                                    gap: 14,
                                                    opacity: isActive
                                                        ? 1
                                                        : isDone
                                                          ? 0.55
                                                          : 0.3,
                                                }}
                                            >
                                                <div
                                                    style={{
                                                        width: isActive
                                                            ? 36
                                                            : 28,
                                                        height: isActive
                                                            ? 36
                                                            : 28,
                                                        borderRadius: "50%",
                                                        background: isActive
                                                            ? "rgba(254,176,106,0.25)"
                                                            : isDone
                                                              ? "rgba(74,222,128,0.15)"
                                                              : "rgba(255,255,255,0.06)",
                                                        border: isActive
                                                            ? "2px solid rgba(254,176,106,0.70)"
                                                            : isDone
                                                              ? "2px solid rgba(74,222,128,0.50)"
                                                              : "2px solid rgba(255,255,255,0.15)",
                                                        display: "flex",
                                                        alignItems: "center",
                                                        justifyContent:
                                                            "center",
                                                        fontSize: isActive
                                                            ? 15
                                                            : 12,
                                                        fontWeight: 900,
                                                        color: isActive
                                                            ? "#FEB06A"
                                                            : isDone
                                                              ? "#4ade80"
                                                              : "rgba(255,255,255,0.35)",
                                                        flexShrink: 0,
                                                        marginTop: 1,
                                                    }}
                                                >
                                                    {s.n}
                                                </div>
                                                <div>
                                                    <div
                                                        style={{
                                                            fontSize: isActive
                                                                ? 16
                                                                : 13,
                                                            fontWeight: isActive
                                                                ? 900
                                                                : 700,
                                                            color: isActive
                                                                ? "#FEB06A"
                                                                : isDone
                                                                  ? "rgba(255,255,255,0.75)"
                                                                  : "rgba(255,255,255,0.40)",
                                                            marginBottom: 3,
                                                        }}
                                                    >
                                                        {s.head}
                                                    </div>
                                                    <div
                                                        style={{
                                                            fontSize: 12,
                                                            lineHeight: "18px",
                                                            color: isActive
                                                                ? "rgba(255,255,255,0.55)"
                                                                : "rgba(255,255,255,0.22)",
                                                        }}
                                                    >
                                                        {s.body}
                                                    </div>
                                                </div>
                                            </div>
                                        )
                                    })}
                                </div>
                                {/* what you get */}
                                <div
                                    style={{
                                        borderRadius: 14,
                                        border: "1px solid rgba(74,222,128,0.18)",
                                        background: "rgba(74,222,128,0.05)",
                                        padding: "14px 16px",
                                    }}
                                >
                                    <div
                                        style={{
                                            fontSize: 9,
                                            fontWeight: 900,
                                            letterSpacing: 2,
                                            textTransform: "uppercase" as const,
                                            color: "rgba(74,222,128,0.65)",
                                            marginBottom: 10,
                                        }}
                                    >
                                        What Job Fit tells you
                                    </div>
                                    <div
                                        style={{
                                            display: "flex",
                                            flexDirection: "column" as const,
                                            gap: 8,
                                        }}
                                    >
                                        {[
                                            "Apply, Review, or Pass — a clear decision on whether this role is worth your time",
                                            "Your fit score out of 100 based on your actual background",
                                            "Why you are competitive — the specific signals that work in your favor",
                                            "Where you are exposed — the gaps a hiring manager will notice",
                                            "Your exact next move based on the verdict",
                                        ].map((item, i) => (
                                            <div
                                                key={i}
                                                style={{
                                                    display: "flex",
                                                    alignItems: "flex-start",
                                                    gap: 10,
                                                }}
                                            >
                                                <div
                                                    style={{
                                                        width: 18,
                                                        height: 18,
                                                        borderRadius: "50%",
                                                        background:
                                                            "rgba(74,222,128,0.15)",
                                                        border: "1.5px solid rgba(74,222,128,0.45)",
                                                        display: "flex",
                                                        alignItems: "center",
                                                        justifyContent:
                                                            "center",
                                                        fontSize: 10,
                                                        fontWeight: 900,
                                                        color: "#4ade80",
                                                        flexShrink: 0,
                                                        marginTop: 1,
                                                        animation:
                                                            "whyPulse 2.5s ease-in-out infinite",
                                                        animationDelay: `${i * 0.25}s`,
                                                    }}
                                                >
                                                    ✓
                                                </div>
                                                <div
                                                    style={{
                                                        fontSize: 12,
                                                        lineHeight: "18px",
                                                        color: "rgba(255,255,255,0.65)",
                                                    }}
                                                >
                                                    {item}
                                                </div>
                                            </div>
                                        ))}
                                    </div>
                                </div>
                            </div>
                        </div>
                    ) : null}

                    {/* Persona selector */}
                    {isLoggedIn && personas.length > 0 && (
                        <div
                            style={{
                                marginBottom: 16,
                                padding: "16px 18px",
                                borderRadius: 14,
                                border: "1px solid rgba(254,176,106,0.25)",
                                background: "rgba(254,176,106,0.06)",
                            }}
                        >
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: "rgba(254,176,106,0.70)",
                                    marginBottom: 10,
                                }}
                            >
                                Evaluating as
                            </div>
                            <div
                                style={{
                                    display: "flex",
                                    gap: 10,
                                    flexWrap: "wrap" as const,
                                }}
                            >
                                {personas.map((p: any) => {
                                    const isSelected =
                                        selectedPersonaId === p.id
                                    return (
                                        <button
                                            key={p.id}
                                            type="button"
                                            onClick={() =>
                                                setSelectedPersonaId(p.id)
                                            }
                                            style={{
                                                padding: "10px 18px",
                                                borderRadius: 10,
                                                border: isSelected
                                                    ? "1.5px solid rgba(254,176,106,0.70)"
                                                    : "1px solid rgba(255,255,255,0.10)",
                                                background: isSelected
                                                    ? "rgba(254,176,106,0.15)"
                                                    : "rgba(255,255,255,0.04)",
                                                color: isSelected
                                                    ? "#FEB06A"
                                                    : "rgba(255,255,255,0.55)",
                                                fontWeight: 900,
                                                fontSize: 13,
                                                cursor: "pointer",
                                                display: "flex",
                                                alignItems: "center",
                                                gap: 8,
                                                transition: "all 0.15s",
                                            }}
                                        >
                                            {isSelected && (
                                                <div
                                                    style={{
                                                        width: 7,
                                                        height: 7,
                                                        borderRadius: "50%",
                                                        background: "#FEB06A",
                                                        flexShrink: 0,
                                                    }}
                                                />
                                            )}
                                            {p.name}
                                            {p.is_default && (
                                                <span
                                                    style={{
                                                        fontSize: 9,
                                                        fontWeight: 900,
                                                        letterSpacing: 0.8,
                                                        textTransform:
                                                            "uppercase" as const,
                                                        color: isSelected
                                                            ? "rgba(254,176,106,0.60)"
                                                            : "rgba(255,255,255,0.25)",
                                                    }}
                                                >
                                                    default
                                                </span>
                                            )}
                                        </button>
                                    )
                                })}
                            </div>
                            {selectedPersonaId && (
                                <div
                                    style={{
                                        marginTop: 10,
                                        fontSize: 11,
                                        color: "rgba(255,255,255,0.35)",
                                        lineHeight: "16px",
                                    }}
                                >
                                    {personas
                                        .find(
                                            (p: any) =>
                                                p.id === selectedPersonaId
                                        )
                                        ?.resume_text?.slice(0, 80)
                                        ?.trim()}
                                    …
                                </div>
                            )}
                        </div>
                    )}

                    {/* Job paste area */}
                    <div style={{ marginTop: 10 }}>
                        <div
                            style={jobHeaderRow}
                            role="button"
                            onClick={() => setJobInputOpen((v) => !v)}
                        >
                            <div style={jobHeaderLink}>Active Job</div>
                            <div style={jobHeaderChevron}>
                                {jobInputOpen ? "▾" : "▸"}
                            </div>
                        </div>

                        {jobInputOpen ? (
                            <div style={{ marginTop: 10 }}>
                                {/* URL Fetch */}
                                <div style={{
                                    background: "#0D1F35",
                                    border: "1px solid rgba(255,149,0,0.2)",
                                    borderRadius: 12,
                                    padding: 16,
                                    marginBottom: 12,
                                }}>
                                    <div style={{ fontSize: 10, fontWeight: 900, letterSpacing: 1.5, textTransform: "uppercase" as const, color: "#FF9500", marginBottom: 4 }}>
                                        PASTE JOB URL
                                    </div>
                                    <div style={{ fontSize: 12, color: "rgba(255,255,255,0.4)", marginBottom: 10 }}>
                                        LinkedIn, Indeed, Greenhouse, Lever, Handshake — paste any job URL
                                    </div>
                                    <div style={{ display: "flex", gap: 8 }}>
                                        <input
                                            type="text"
                                            value={jobUrl}
                                            onChange={(e) => setJobUrl(e.target.value)}
                                            onKeyDown={(e) => { if (e.key === "Enter") handleFetchUrl() }}
                                            placeholder="https://linkedin.com/jobs/view/..."
                                            style={{
                                                flex: 1,
                                                padding: "10px 12px",
                                                background: "rgba(255,255,255,0.06)",
                                                border: "1px solid rgba(255,255,255,0.12)",
                                                borderRadius: 8,
                                                color: "#fff",
                                                fontSize: 13,
                                                outline: "none",
                                                fontFamily: "inherit",
                                            }}
                                        />
                                        <button
                                            onClick={handleFetchUrl}
                                            disabled={urlLoading}
                                            style={{
                                                padding: "10px 16px",
                                                background: urlLoading ? "#333" : "#FF9500",
                                                border: "none",
                                                borderRadius: 8,
                                                color: urlLoading ? "#999" : "#0B0F1A",
                                                fontSize: 12,
                                                fontWeight: 900,
                                                cursor: urlLoading ? "not-allowed" : "pointer",
                                                whiteSpace: "nowrap" as const,
                                                fontFamily: "inherit",
                                            }}
                                        >
                                            {urlLoading ? "Fetching..." : "FETCH →"}
                                        </button>
                                    </div>
                                    {urlError && <div style={{ fontSize: 12, color: "#FF3B5C", marginTop: 8 }}>{urlError}</div>}

                                    {/* Blocked site popup overlay */}
                                    {showBlockedPopup && (
                                        <div style={{
                                            position: "fixed", inset: 0, zIndex: 9999,
                                            background: "rgba(0,0,0,0.75)",
                                            display: "flex", alignItems: "center", justifyContent: "center",
                                        }}>
                                            <div style={{
                                                background: "#0D1F35",
                                                border: "0.5px solid #1E3A5F",
                                                borderRadius: 16,
                                                padding: 36,
                                                maxWidth: 460,
                                                width: "90%",
                                                textAlign: "center" as const,
                                            }}>
                                                <div style={{ fontSize: 22, fontWeight: 900, fontStyle: "italic", color: "#FFD60A", marginBottom: 14 }}>
                                                    LinkedIn / Indeed Detected
                                                </div>
                                                <div style={{ fontSize: 14, color: "rgba(255,255,255,0.6)", lineHeight: "1.7", marginBottom: 8 }}>
                                                    This site blocks automated access. No worries — just:
                                                </div>
                                                <div style={{ fontSize: 13, color: "rgba(255,255,255,0.5)", lineHeight: "1.8", textAlign: "left" as const, margin: "0 auto", maxWidth: 340, marginBottom: 24 }}>
                                                    1. Open the job posting (button below)<br />
                                                    2. Select all text — <strong style={{ color: "#fff" }}>Ctrl+A</strong> (Windows) or <strong style={{ color: "#fff" }}>Cmd+A</strong> (Mac)<br />
                                                    3. Copy — <strong style={{ color: "#fff" }}>Ctrl+C</strong> / <strong style={{ color: "#fff" }}>Cmd+C</strong><br />
                                                    4. Come back here and paste it below
                                                </div>
                                                <div style={{ display: "flex", gap: 10 }}>
                                                    <button
                                                        onClick={() => { window.open(jobUrl.trim(), "_blank"); setShowBlockedPopup(false) }}
                                                        style={{
                                                            flex: 1, padding: 14,
                                                            background: "linear-gradient(90deg, #FF6B00, #FF9A3C)",
                                                            color: "#fff", fontWeight: 900, fontStyle: "italic",
                                                            fontSize: 14, borderRadius: 10, border: "none",
                                                            cursor: "pointer", fontFamily: "inherit",
                                                        }}
                                                    >
                                                        Open Job Posting
                                                    </button>
                                                    <button
                                                        onClick={() => setShowBlockedPopup(false)}
                                                        style={{
                                                            flex: 1, padding: 14,
                                                            background: "rgba(255,255,255,0.06)",
                                                            color: "rgba(255,255,255,0.5)", fontWeight: 700,
                                                            fontSize: 14, borderRadius: 10,
                                                            border: "1px solid rgba(255,255,255,0.12)",
                                                            cursor: "pointer", fontFamily: "inherit",
                                                        }}
                                                    >
                                                        Close
                                                    </button>
                                                </div>
                                            </div>
                                        </div>
                                    )}

                                    {/* Paste helper (visible after popup closes) */}
                                    {showLinkedInHelper && !showBlockedPopup && (
                                        <div style={{
                                            background: "rgba(255,214,10,0.04)",
                                            border: "1px solid rgba(255,214,10,0.25)",
                                            borderRadius: 10,
                                            padding: 14,
                                            marginTop: 10,
                                        }}>
                                            <div style={{ fontSize: 12, fontWeight: 900, color: "#FFD60A", marginBottom: 8 }}>
                                                LINKEDIN / INDEED DETECTED
                                            </div>
                                            <textarea
                                                value={linkedInPasteText}
                                                onChange={(e) => setLinkedInPasteText(e.target.value)}
                                                placeholder="Paste the full job page here..."
                                                style={{
                                                    width: "100%",
                                                    minHeight: 100,
                                                    padding: "10px 12px",
                                                    background: "rgba(255,255,255,0.06)",
                                                    border: "1px solid rgba(255,255,255,0.12)",
                                                    borderRadius: 8,
                                                    color: "#fff",
                                                    fontSize: 13,
                                                    resize: "vertical" as const,
                                                    outline: "none",
                                                    fontFamily: "inherit",
                                                    boxSizing: "border-box" as const,
                                                }}
                                            />
                                            <button
                                                onClick={handleLinkedInPaste}
                                                disabled={linkedInPasteLoading}
                                                style={{
                                                    marginTop: 8,
                                                    padding: "10px 16px",
                                                    background: linkedInPasteLoading ? "#333" : "#FFD60A",
                                                    border: "none",
                                                    borderRadius: 8,
                                                    color: linkedInPasteLoading ? "#999" : "#0B0F1A",
                                                    fontSize: 12,
                                                    fontWeight: 900,
                                                    cursor: linkedInPasteLoading ? "not-allowed" : "pointer",
                                                    whiteSpace: "nowrap" as const,
                                                    fontFamily: "inherit",
                                                    width: "100%",
                                                }}
                                            >
                                                {linkedInPasteLoading ? "Extracting..." : "EXTRACT FROM PASTE →"}
                                            </button>
                                        </div>
                                    )}

                                </div>

                                {/* OR divider */}
                                <div style={{ display: "flex", alignItems: "center", gap: 14, margin: "16px 0" }}>
                                    <div style={{ flex: 1, height: 1, background: "rgba(255,255,255,0.08)" }} />
                                    <div style={{ fontSize: 13, fontWeight: 900, color: "rgba(255,255,255,0.25)", letterSpacing: 2 }}>OR</div>
                                    <div style={{ flex: 1, height: 1, background: "rgba(255,255,255,0.08)" }} />
                                </div>

                                {manualPasteStep === "paste" && (
                                    <div>
                                        <div style={{ fontSize: 16, fontWeight: 900, color: TEXT, marginBottom: 6 }}>
                                            Paste the full job posting
                                        </div>
                                        <div style={{ fontSize: 13, color: MUTED, lineHeight: "20px", marginBottom: 14 }}>
                                            {"Can't grab the URL? No problem. Go to the job posting in your browser, select all the text, copy it, and paste it here — SIGNAL will extract the company, title, and description automatically."}
                                        </div>
                                        <div style={{ fontSize: 12, color: DIM, lineHeight: "20px", marginBottom: 14 }}>
                                            {typeof window !== "undefined" && /Mobi|Android/i.test(navigator.userAgent)
                                                ? "Tap and hold anywhere on the job page → Select All → Copy → come back and paste below"
                                                : (<>
                                                    1. Press <strong style={{ color: TEXT }}>Ctrl+A</strong> to select everything on the job page<br />
                                                    2. Press <strong style={{ color: TEXT }}>Ctrl+C</strong> to copy<br />
                                                    3. Click back here and press <strong style={{ color: TEXT }}>Ctrl+V</strong> to paste below
                                                  </>)
                                            }
                                        </div>
                                        <div style={{ position: "relative" as const }}>
                                            <textarea
                                                style={{ ...textarea, minHeight: 200, maxHeight: 400 }}
                                                value={manualPasteText}
                                                onChange={(e) => setManualPasteText(e.target.value)}
                                                placeholder="Paste the entire job posting here..."
                                            />
                                            {manualPasteText && (
                                                <button
                                                    onClick={() => setManualPasteText("")}
                                                    style={{
                                                        position: "absolute" as const,
                                                        top: 8, right: 8,
                                                        background: "rgba(255,255,255,0.10)",
                                                        border: "none",
                                                        borderRadius: 12,
                                                        width: 24, height: 24,
                                                        color: MUTED,
                                                        fontSize: 14,
                                                        cursor: "pointer",
                                                        display: "flex",
                                                        alignItems: "center",
                                                        justifyContent: "center",
                                                    }}
                                                >
                                                    ×
                                                </button>
                                            )}
                                        </div>
                                        {urlError && <div style={{ fontSize: 12, color: "#FF3B5C", marginTop: 8 }}>{urlError}</div>}
                                        <button
                                            onClick={handleManualExtract}
                                            disabled={manualExtracting}
                                            style={{
                                                width: "100%",
                                                marginTop: 10,
                                                padding: "12px 16px",
                                                background: manualExtracting ? "#333" : WRN_ORANGE,
                                                border: "none",
                                                borderRadius: 12,
                                                color: manualExtracting ? "#999" : "#0B0F1A",
                                                fontSize: 14,
                                                fontWeight: 900,
                                                cursor: manualExtracting ? "not-allowed" : "pointer",
                                            }}
                                        >
                                            {manualExtracting ? "Extracting job details..." : "Extract & Continue →"}
                                        </button>
                                    </div>
                                )}

                                {manualPasteStep === "confirm" && (
                                    <div>
                                        <div style={{ fontSize: 10, fontWeight: 700, letterSpacing: 1.5, textTransform: "uppercase" as const, color: WRN_ORANGE, marginBottom: 10 }}>
                                            CONFIRM DETAILS
                                        </div>
                                        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10, marginBottom: 10 }}>
                                            <div>
                                                <div style={{ fontSize: 11, color: DIM, fontWeight: 700, marginBottom: 4 }}>Company</div>
                                                <input
                                                    type="text"
                                                    style={input}
                                                    value={manualCompanyName}
                                                    onChange={(e) => setManualCompanyName(e.target.value)}
                                                    placeholder="Company name"
                                                    maxLength={200}
                                                />
                                            </div>
                                            <div>
                                                <div style={{ fontSize: 11, color: DIM, fontWeight: 700, marginBottom: 4 }}>Title</div>
                                                <input
                                                    type="text"
                                                    style={input}
                                                    value={manualJobTitle}
                                                    onChange={(e) => setManualJobTitle(e.target.value)}
                                                    placeholder="Job title"
                                                    maxLength={200}
                                                />
                                            </div>
                                        </div>
                                        {/* Extracted job description preview */}
                                        {job && (
                                            <div style={{ marginBottom: 12 }}>
                                                <div style={{ fontSize: 11, color: DIM, fontWeight: 700, marginBottom: 4 }}>Job Description (extracted)</div>
                                                <div style={{
                                                    maxHeight: 120,
                                                    overflowY: "auto" as const,
                                                    padding: "10px 14px",
                                                    background: GLASS,
                                                    borderRadius: 12,
                                                    border: `1px solid ${BORDER}`,
                                                    fontSize: 13,
                                                    lineHeight: "20px",
                                                    color: MUTED,
                                                    whiteSpace: "pre-wrap" as const,
                                                }}>
                                                    {job.length > 600 ? job.slice(0, 600) + "…" : job}
                                                </div>
                                            </div>
                                        )}
                                        <div style={{ fontSize: 12, color: DIM, marginBottom: 12 }}>
                                            Look right? You can edit before continuing.
                                        </div>
                                        <button
                                            onClick={() => {
                                                setManualPasteStep("paste")
                                                setManualPasteText("")
                                                setManualCompanyName("")
                                                setManualJobTitle("")
                                                setJob("")
                                            }}
                                            style={{
                                                background: "none",
                                                border: "none",
                                                color: MUTED,
                                                fontSize: 12,
                                                fontWeight: 700,
                                                cursor: "pointer",
                                                padding: 0,
                                                marginBottom: 4,
                                            }}
                                        >
                                            ← Paste again
                                        </button>
                                    </div>
                                )}
                            </div>
                        ) : null}
                    </div>
                    {showJobFitLandingCTA
                        ? renderPrimaryRunButton(
                              "Run JobFit →",
                              runJobFit,
                              loadingJobFit
                          )
                        : null}
                    {upgradeHint ? (
                        <div
                            style={{
                                margin: "14px 0",
                                padding: "24px 24px 20px",
                                borderRadius: 18,
                                background:
                                    "linear-gradient(145deg, rgba(254,176,106,0.08), rgba(81,173,229,0.06))",
                                border: "1px solid rgba(254,176,106,0.25)",
                            }}
                        >
                            {/* Header */}
                            <div
                                style={{
                                    fontSize: 9,
                                    fontWeight: 900,
                                    letterSpacing: 2,
                                    textTransform: "uppercase" as const,
                                    color: WRN_ORANGE,
                                    marginBottom: 10,
                                }}
                            >
                                Full SIGNAL —{" "}
                                {upgradeHint === "positioning"
                                    ? "Step 2"
                                    : upgradeHint === "coverletter"
                                      ? "Step 3"
                                      : "Step 4"}
                            </div>

                            {/* Title */}
                            <div
                                style={{
                                    fontSize: 20,
                                    fontWeight: 900,
                                    color: ICE,
                                    marginBottom: 8,
                                    letterSpacing: -0.3,
                                    lineHeight: 1.2,
                                }}
                            >
                                {upgradeHint === "positioning"
                                    ? "Fix Your Positioning — Tell One Story, Not Ten"
                                    : upgradeHint === "coverletter"
                                      ? "Your Cover Letter, In Your Voice"
                                      : "Build Your Outreach Plan — Be Appropriately Aggressive"}
                            </div>

                            {/* Description */}
                            <div
                                style={{
                                    fontSize: 14,
                                    lineHeight: "22px",
                                    color: MUTED,
                                    marginBottom: 18,
                                }}
                            >
                                {upgradeHint === "positioning"
                                    ? "Most students apply like generalists — showing everything they've done and hoping something lands. Positioning shows you how to tell one targeted story for this specific role. It identifies which parts of your background actually matter here, helps you reframe your resume bullets to match what the hiring manager is looking for, and gets you through the ATS screening and the recruiter's 7-second scan."
                                    : upgradeHint === "coverletter"
                                      ? "SIGNAL doesn't write a generic cover letter — it helps you find your story and tell it in your own voice. Pulling from your real experience, your real background, and what this specific company is actually hiring for. Not a template. A narrative that sounds like you and speaks directly to this job."
                                      : "Applying alone has never been a real strategy. You have to make your name known inside the organization before the interview even happens. SIGNAL builds you a complete outreach plan — who to contact, what to say, when to follow up, and how to be appropriately aggressive without being annoying."}
                            </div>

                            {/* What's included */}
                            <div
                                style={{
                                    padding: "14px 16px",
                                    borderRadius: 12,
                                    background: "rgba(255,255,255,0.04)",
                                    border: "1px solid rgba(255,255,255,0.08)",
                                    marginBottom: 18,
                                }}
                            >
                                <div
                                    style={{
                                        fontSize: 10,
                                        fontWeight: 900,
                                        letterSpacing: 1.4,
                                        textTransform: "uppercase" as const,
                                        color: DIM,
                                        marginBottom: 10,
                                    }}
                                >
                                    Full SIGNAL includes
                                </div>
                                {[
                                    "Job Fit — know if a role is worth your time before you apply",
                                    "Positioning — one targeted story that gets through ATS and the 7-second scan",
                                    "Cover Letter — written in your voice, tied to the specific job",
                                    "Outreach Plan — who to contact and exactly what to say",
                                ].map((item, i) => (
                                    <div
                                        key={i}
                                        style={{
                                            display: "flex",
                                            alignItems: "flex-start",
                                            gap: 10,
                                            marginBottom: i < 3 ? 8 : 0,
                                            fontSize: 13,
                                            color: MUTED,
                                            lineHeight: "19px",
                                        }}
                                    >
                                        <span
                                            style={{
                                                color: WRN_ORANGE,
                                                fontWeight: 900,
                                                flexShrink: 0,
                                                fontSize: 13,
                                            }}
                                        >
                                            ✓
                                        </span>
                                        {item}
                                    </div>
                                ))}
                            </div>

                            {/* Price + CTA */}
                            <div
                                style={{
                                    display: "flex",
                                    alignItems: "center",
                                    gap: 14,
                                    flexWrap: "wrap" as const,
                                }}
                            >
                                <a
                                    href="https://workforcereadynow.com/signal"
                                    style={{
                                        display: "inline-block",
                                        padding: "13px 28px",
                                        borderRadius: 14,
                                        background:
                                            "linear-gradient(135deg, #FEB06A, #51ADE5)",
                                        fontWeight: 900,
                                        fontSize: 15,
                                        color: "#0B1020",
                                        textDecoration: "none",
                                        boxShadow:
                                            "0 4px 20px rgba(254,176,106,0.28)",
                                    }}
                                >
                                    Unlock Full SIGNAL — $99
                                </a>
                                <div
                                    style={{
                                        fontSize: 12,
                                        color: DIM,
                                        lineHeight: "17px",
                                    }}
                                >
                                    3 months access · 30-day money-back
                                    guarantee
                                </div>
                            </div>

                            {/* Dismiss */}
                            <button
                                style={{
                                    background: "transparent",
                                    border: "none",
                                    padding: 0,
                                    marginTop: 14,
                                    fontSize: 12,
                                    color: DIM,
                                    cursor: "pointer",
                                    textDecoration: "underline",
                                    textUnderlineOffset: 3,
                                }}
                                onClick={() => setUpgradeHint(null)}
                                type="button"
                            >
                                Dismiss
                            </button>
                        </div>
                    ) : null}
                    {errorMsg ? <div style={errorBox}>{errorMsg}</div> : null}

                    {/* Output area */}
                    {hasAnyResult || tab !== "jobfit" ? (
                        <div style={{ marginTop: 16 }}>
                            {renderMainOutput()}
                        </div>
                    ) : null}
                </div>
            </div>
        </div>
    )
}

/* ---------- SIDE NAV ---------- */
function SideNav({
    tab,
    accessMode,
    isLoggedIn,
    authedEmail,
    trialEmail,
    onSelect,
    onRunNewJob,
    onLogout,
    onUpgrade,
}: {
    tab: TabKey
    accessMode: AccessMode
    isLoggedIn: boolean
    authedEmail: string | null
    trialEmail: string
    onSelect: (t: TabKey) => void
    onRunNewJob: () => void
    onLogout: () => void
    onUpgrade: () => void
}) {
    const locked = (k: TabKey) => accessMode === "jobfit_only" && k !== "jobfit"

    return (
        <div style={sideNav}>
            <div
                style={{
                    paddingBottom: 16,
                    marginBottom: 12,
                    borderBottom: `1px solid ${BORDER_SOFT}`,
                }}
            >
                <div style={{ display: "flex", alignItems: "center" }}>
                    <span
                        style={{
                            fontSize: 28,
                            fontWeight: 950,
                            fontStyle: "italic",
                            letterSpacing: -0.5,
                            color: "#ffffff",
                            lineHeight: 1,
                        }}
                    >
                        SIGNAL
                    </span>
                    <div
                        style={{
                            width: 18,
                            height: 5,
                            background: WRN_ORANGE,
                            borderRadius: 1,
                            marginLeft: 3,
                            marginBottom: 1,
                            flexShrink: 0,
                        }}
                    />
                </div>
                <div
                    style={{
                        display: "flex",
                        alignItems: "center",
                        gap: 3,
                        marginTop: 3,
                    }}
                >
                    <span
                        style={{
                            fontSize: 7,
                            fontWeight: 700,
                            color: "rgba(255,255,255,0.28)",
                        }}
                    >
                        by
                    </span>
                    <span
                        style={{
                            fontSize: 7,
                            fontWeight: 900,
                            letterSpacing: 0.8,
                            color: WRN_ORANGE,
                            textTransform: "uppercase",
                        }}
                    >
                        Workforce
                    </span>
                    <span
                        style={{
                            fontSize: 7,
                            fontWeight: 900,
                            letterSpacing: 0.8,
                            color: WRN_BLUE,
                            textTransform: "uppercase",
                        }}
                    >
                        Ready Now
                    </span>
                </div>
            </div>

            <div style={sideSectionTitle}>SYSTEM</div>

            <SideNavItem
                label="JobFit"
                active={tab === "jobfit"}
                locked={false}
                onClick={() => onSelect("jobfit")}
            />
            <SideNavItem
                label="Positioning"
                active={tab === "positioning"}
                locked={locked("positioning")}
                onClick={() => onSelect("positioning")}
            />
            <SideNavItem
                label="Cover Letter"
                active={tab === "coverletter"}
                locked={locked("coverletter")}
                onClick={() => onSelect("coverletter")}
            />
            <SideNavItem
                label="Networking"
                active={tab === "networking"}
                locked={locked("networking")}
                onClick={() => onSelect("networking")}
            />

            <div style={{ height: 14 }} />

            <div style={sideSectionTitle}>ACTIONS</div>

            <button style={sideActionBtn} onClick={onRunNewJob} type="button">
                Run New Job
            </button>

            <button
                style={sideActionBtn}
                onClick={async () => {
                    const { data } = await supabase.auth.getSession()
                    const token = data?.session?.access_token
                    const base = "https://wrnsignal-api.vercel.app/dashboard"
                    const url = token ? `${base}?token=${token}` : base
                    window.top!.location.replace(url)
                }}
                type="button"
            >
                My Dashboard ↗
            </button>

            {accessMode === "jobfit_only" ? (
                <button
                    style={sideActionBtnSecondary}
                    onClick={onUpgrade}
                    type="button"
                >
                    Unlock Full SIGNAL
                </button>
            ) : null}

            <div style={{ height: 14 }} />

            <div style={sideSectionTitle}>ACCOUNT</div>

            <div style={sideAccountRow}>
                <div style={sideAvatar}>
                    {(authedEmail || trialEmail || "?").charAt(0).toUpperCase()}
                </div>
                <div style={{ overflow: "hidden" }}>
                    <div style={sideAccountName}>
                        {isLoggedIn
                            ? "Signed in"
                            : trialEmail
                              ? "Job Fit trial"
                              : "Guest"}
                    </div>
                    <div style={sideAccountEmail}>
                        {authedEmail || trialEmail || "Trial mode"}
                    </div>
                </div>
            </div>
            <button style={sideLogoutBtn} onClick={onLogout} type="button">
                Log Out
            </button>

        </div>
    )
}

function SideNavItem({
    label,
    active,
    locked,
    onClick,
}: {
    label: string
    active: boolean
    locked: boolean
    onClick: () => void
}) {
    return (
        <button
            style={{
                ...sideNavItem,
                ...(active ? sideNavItemActive : null),
                opacity: locked ? 0.55 : 1,
                cursor: "pointer",
            }}
            onClick={onClick}
            disabled={false}
            type="button"
        >
            <span>{label}</span>
            <span style={{ opacity: 0.8 }}>{locked ? "🔒" : "›"}</span>
        </button>
    )
}

/* ---------- STYLES ---------- */

/* ---------- APP SHELL ---------- */

// Fix: outer padding prevents left-edge clipping inside Framer canvas
// Root frame for the full app. Sets viewport-based dimensions so the
// component always fills the browser window regardless of how Framer
// sizes its parent frame. Uses 100dvh (dynamic viewport height) instead
// of 100vh so the mobile Safari collapsing toolbar doesn't push content
// below the visible area — the classic "login appears below the fold"
// bug was caused by 100vh being larger than the visible viewport on
// first paint. overflowX is hidden to prevent accidental horizontal
// scrollbars from fixed-width children, but overflowY is auto so long
// content (login, results pages, paginated lists) can actually scroll.
const outerFrame: React.CSSProperties = {
    width: "100%",
    height: "100%",
    background: BG,
    boxSizing: "border-box",
    overflowX: "hidden",
    overflowY: "auto",
    position: "relative",
}

// Inner shell container — the flex row holding nav + main content.
// minHeight 100dvh so the side nav always fills the visible area even
// when main content is short. Does not set overflow — outerFrame
// already handles scrolling for the whole app.
const shell: React.CSSProperties = {
    background: BG,
    color: TEXT,
    width: "100%",
    minHeight: "100dvh",
    display: "flex",
    boxSizing: "border-box",
}

const main: React.CSSProperties = {
    flex: 1,
    padding: "32px 40px 60px 36px",
    boxSizing: "border-box",
    width: "100%",
    minWidth: 0,
}
const headerTop: React.CSSProperties = {
    display: "flex",
    alignItems: "baseline",
    justifyContent: "space-between",
}

const ENGINE_BRAND_HEIGHT = 56

const engineBrandWrap: React.CSSProperties = {
    position: "absolute",
    top: 20,
    left: 24,
    zIndex: 9999,
    pointerEvents: "none",
    userSelect: "none",
}
const engineBrandSignal: React.CSSProperties = {
    fontSize: 30,
    fontWeight: 800,
    lineHeight: 1,
    letterSpacing: -0.6,
    color: WRN_ORANGE,
    margin: 0,
    padding: 0,
}

const engineBrandByline: React.CSSProperties = {
    fontSize: 11,
    fontWeight: 700,
    lineHeight: 1.1,
    letterSpacing: 0.8,
    color: WRN_BLUE,
    textTransform: "uppercase",
    marginTop: 4,
    paddingLeft: 2,
}

/* ---------- SIDE NAV ---------- */

const sideNav: React.CSSProperties = {
    width: 220,
    flex: "0 0 220px",
    padding: "24px 14px",
    background: NAV_BG,
    borderRight: "1px solid rgba(255,255,255,0.08)",
    boxSizing: "border-box",
    height: "100%",
    overflowY: "auto",
}

const sideNavTopSpacer: React.CSSProperties = {
    height: 6,
}

const sideSectionTitle: React.CSSProperties = {
    fontSize: 11,
    letterSpacing: 1.2,
    fontWeight: 900,
    color: "rgba(255,255,255,0.42)",
    margin: "12px 0 8px",
    textTransform: "uppercase",
}

const sideNavItem: React.CSSProperties = {
    width: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "11px 12px",
    borderRadius: 10,
    background: "rgba(255,255,255,0.04)",
    border: `1px solid ${BORDER_SOFT}`,
    color: TEXT,
    fontWeight: 900,
    cursor: "pointer",
    marginBottom: 8,
    fontSize: 13,
}

const sideNavItemActive: React.CSSProperties = {
    border: "1px solid rgba(254,176,106,0.35)",
    background: "rgba(254,176,106,0.08)",
    color: WRN_ORANGE,
}
const sideActionBtn: React.CSSProperties = {
    width: "100%",
    padding: "11px 12px",
    borderRadius: 10,
    background: "rgba(255,255,255,0.04)",
    border: `1px solid ${BORDER_SOFT}`,
    color: TEXT,
    fontWeight: 900,
    cursor: "pointer",
    marginBottom: 8,
    fontSize: 13,
}

const sideActionBtnSecondary: React.CSSProperties = {
    width: "100%",
    padding: "12px 12px",
    borderRadius: 12,
    background: "rgba(255,255,255,0.02)",
    border: "1px solid rgba(81,173,229,0.22)",
    color: TEXT,
    fontWeight: 900,
    cursor: "pointer",
    marginBottom: 10,
}

const sideAccountRow: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: 12,
    borderRadius: 12,
    border: `1px solid ${BORDER_SOFT}`,
    background: "rgba(255,255,255,0.04)",
    marginBottom: 4,
}

const sideAvatar: React.CSSProperties = {
    width: 34,
    height: 34,
    borderRadius: 18,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    background: "rgba(255,255,255,0.06)",
    border: "1px solid rgba(255,255,255,0.10)",
    fontWeight: 900,
}

const sideAccountName: React.CSSProperties = {
    fontSize: 12,
    fontWeight: 900,
    color: TEXT,
}

const sideAccountEmail: React.CSSProperties = {
    fontSize: 11,
    color: MUTED,
    marginTop: 2,
    whiteSpace: "nowrap",
    overflow: "hidden",
    textOverflow: "ellipsis",
    maxWidth: 150,
}

const sideLogoutBtn: React.CSSProperties = {
    width: "100%",
    padding: "11px 12px",
    borderRadius: 10,
    background: "rgba(255,255,255,0.04)",
    border: `1px solid ${BORDER_SOFT}`,
    cursor: "pointer",
    color: MUTED,
    fontWeight: 900,
    fontSize: 13,
    marginTop: 8,
}

const sideDisabledItem: React.CSSProperties = {
    width: "100%",
    padding: "11px 12px",
    borderRadius: 10,
    background: "rgba(255,255,255,0.02)",
    border: "1px solid rgba(255,255,255,0.04)",
    color: "rgba(255,255,255,0.22)",
    marginBottom: 8,
    fontWeight: 800,
    fontSize: 13,
}

/* ---------- STEP LANDING / INTRO ---------- */

const stepsIntroWrap: React.CSSProperties = {
    marginTop: 14,
    padding: 18,
    borderRadius: 16,
    background: CARD,
    border: `1px solid ${BORDER_SOFT}`,
}

const stepsIntroTitle: React.CSSProperties = {
    fontSize: 13,
    fontWeight: 900,
    letterSpacing: 0.6,
    textTransform: "uppercase",
    color: "rgba(255,255,255,0.70)",
    marginBottom: 12,
}

const stepsList: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    gap: 12,
}

const stepRow: React.CSSProperties = {
    display: "flex",
    gap: 12,
    alignItems: "flex-start",
}

const stepNum: React.CSSProperties = {
    width: 22,
    height: 22,
    borderRadius: 999,
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 12,
    fontWeight: 900,
    color: "rgba(0,0,0,0.85)",
    border: "1px solid rgba(255,255,255,0.18)",
    background: ICE,
    flex: "0 0 auto",
}

const stepLabel: React.CSSProperties = {
    fontSize: 14,
    fontWeight: 900,
    marginBottom: 2,
}

const stepDesc: React.CSSProperties = {
    fontSize: 13,
    lineHeight: "18px",
    color: MUTED,
}

/* ---------- FORM / INPUT ---------- */

const jobHeaderRow: React.CSSProperties = {
    width: "100%",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "12px 16px",
    borderRadius: 14,
    border: `1px solid ${BORDER}`,
    background: GLASS,
    cursor: "pointer",
    userSelect: "none",
}

const jobHeaderLink: React.CSSProperties = {
    color: "rgba(255,255,255,0.65)",
    fontWeight: 700,
    fontSize: 12,
    textDecoration: "underline",
    textUnderlineOffset: 4,
}

const jobHeaderChevron: React.CSSProperties = {
    color: MUTED,
    fontWeight: 900,
    fontSize: 14,
}

const label: React.CSSProperties = {
    color: WRN_BLUE,
    fontWeight: 800,
    marginBottom: 6,
    display: "block",
}

const input: React.CSSProperties = {
    width: "100%",
    height: 44,
    background: GLASS,
    color: TEXT,
    padding: "0 14px",
    borderRadius: 16,
    border: `1px solid ${BORDER}`,
    boxSizing: "border-box",
    outline: "none",
    fontSize: 16,
}

const textarea: React.CSSProperties = {
    width: "100%",
    minHeight: 140,
    resize: "vertical",
    background: GLASS,
    color: TEXT,
    padding: 14,
    borderRadius: 14,
    border: `1px solid ${BORDER}`,
    boxSizing: "border-box",
    outline: "none",
    fontSize: 15,
    lineHeight: "22px",
}

/* ---------- SHARED CONTENT / PANELS ---------- */

const outputBox: React.CSSProperties = {
    marginTop: 16,
    width: "100%",
}

const outputText: React.CSSProperties = {
    fontSize: 16,
    lineHeight: "22px",
    color: TEXT,
    whiteSpace: "pre-wrap",
}

const divider: React.CSSProperties = {
    height: 1,
    background: BORDER,
    margin: "14px 0",
}

const jobContextBox: React.CSSProperties = {
    padding: "14px 16px",
    borderRadius: 14,
    border: "1px solid rgba(255,255,255,0.10)",
    background: "rgba(255,255,255,0.03)",
    marginBottom: 18,
}

const jobContextHeaderLine: React.CSSProperties = {
    fontSize: 22,
    fontWeight: 950,
    letterSpacing: 0.6,
    color: TEXT,
}

const hintBox: React.CSSProperties = {
    marginTop: 10,
    padding: 12,
    borderRadius: 14,
    background: "rgba(255,255,255,0.04)",
    border: "1px solid rgba(255,255,255,0.1)",
    color: MUTED,
    fontSize: 13,
    lineHeight: "18px",
}

const errorBox: React.CSSProperties = {
    color: ERROR,
    fontWeight: 800,
    marginTop: 10,
    whiteSpace: "pre-wrap",
}

const upgradeNote: React.CSSProperties = {
    margin: "10px 0 6px",
    padding: 12,
    borderRadius: 14,
    background: "rgba(255,255,255,0.04)",
    border: "1px solid rgba(255,255,255,0.1)",
    color: MUTED,
    fontSize: 13,
    lineHeight: "18px",
}

/* ---------- BUTTONS / LINKS ---------- */

const primaryBtn: React.CSSProperties = {
    padding: "12px 18px",
    borderRadius: 16,
    background: "linear-gradient(135deg, #FEB06A, #51ADE5)",
    fontWeight: 900,
    border: "none",
    cursor: "pointer",
    color: "#0B1020",
}

const jobfitHeroBtn: React.CSSProperties = {
    padding: "12px 18px",
    borderRadius: 14,
    background: "linear-gradient(135deg, #FEB06A, #51ADE5)",
    fontWeight: 900,
    border: "none",
    cursor: "pointer",
    color: "#0B1020",
}

const inlineLinkBtn: React.CSSProperties = {
    padding: "10px 14px",
    borderRadius: 14,
    background: "rgba(255,255,255,.06)",
    border: `1px solid ${BORDER}`,
    cursor: "pointer",
    color: TEXT,
    fontWeight: 900,
    fontSize: 12,
}

const smallLinkBtn: React.CSSProperties = {
    background: "transparent",
    border: "none",
    padding: 0,
    margin: 0,
    fontSize: 13,
    fontWeight: 800,
    color: WRN_BLUE,
    cursor: "pointer",
    textDecoration: "underline",
    textUnderlineOffset: 4,
}

const spinner: React.CSSProperties = {
    width: 14,
    height: 14,
    borderRadius: 999,
    border: "2px solid rgba(0,0,0,0.25)",
    borderTop: "2px solid rgba(0,0,0,0.85)",
    animation: "wrnspin 0.9s linear infinite",
}

/* ---------- POSITIONING ---------- */

const posSectionWrap: React.CSSProperties = {
    marginTop: 12,
    padding: 16,
    borderRadius: 16,
    background: CARD,
    border: `1px solid ${BORDER_SOFT}`,
}

const posSectionTitleBtn: React.CSSProperties = {
    width: "100%",
    background: "transparent",
    border: "none",
    padding: 0,
    margin: 0,
    textAlign: "left",
    color: "rgba(255,255,255,0.78)",
    fontWeight: 900,
    fontSize: 13,
    textDecoration: "underline",
    textUnderlineOffset: 4,
    cursor: "pointer",
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
}

const posChevron: React.CSSProperties = {
    color: MUTED,
    fontWeight: 900,
}

const posBody: React.CSSProperties = {
    marginTop: 12,
}

const posPrimaryBulletText: React.CSSProperties = {
    fontSize: 18,
    lineHeight: "24px",
    color: TEXT,
    whiteSpace: "pre-wrap",
}

/* ---------- NETWORKING ---------- */

const netSummaryBox: React.CSSProperties = {
    marginTop: 8,
    padding: 16,
    borderRadius: 16,
    background: CARD,
    border: `1px solid ${BORDER_SOFT}`,
}

const netSummaryTitle: React.CSSProperties = {
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 0.8,
    textTransform: "uppercase",
    color: "rgba(255,255,255,0.70)",
    marginBottom: 12,
}

const netSequenceRow: React.CSSProperties = {
    display: "grid",
    gridTemplateColumns: "90px 1fr",
    gap: 12,
    alignItems: "start",
}

const netSequenceDay: React.CSSProperties = {
    fontSize: 12,
    fontWeight: 900,
    color: WRN_BLUE,
    textTransform: "uppercase",
    letterSpacing: 0.6,
}

const netSequenceStep: React.CSSProperties = {
    fontSize: 14,
    lineHeight: "20px",
    color: TEXT,
}

const netLaneCard: React.CSSProperties = {
    padding: 16,
    borderRadius: 16,
    background: CARD,
    border: `1px solid ${BORDER_SOFT}`,
    width: "100%",
    color: TEXT,
}

const netLaneHeader: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    cursor: "pointer",
    userSelect: "none",
    gap: 16,
}

const netLaneTopRow: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: 10,
    flexWrap: "wrap",
    marginBottom: 6,
}

const netLaneStage: React.CSSProperties = {
    fontSize: 12,
    fontWeight: 900,
    letterSpacing: 0.8,
    textTransform: "uppercase",
}

const netDoFirstTag: React.CSSProperties = {
    fontSize: 11,
    fontWeight: 900,
    letterSpacing: 0.4,
    textTransform: "uppercase",
    color: "#0B1020",
    background: ICE,
    borderRadius: 999,
    padding: "4px 8px",
}

const netLaneTitle: React.CSSProperties = {
    fontSize: 15,
    lineHeight: "20px",
    color: TEXT,
    fontWeight: 900,
}

const netLaneTarget: React.CSSProperties = {
    fontSize: 16,
    lineHeight: "22px",
    color: TEXT,
    fontWeight: 900,
    marginTop: 6,
}

const netLaneOutcome: React.CSSProperties = {
    fontSize: 15,
    lineHeight: "22px",
    color: MUTED,
    marginTop: 8,
}

const netLaneMetaRow: React.CSSProperties = {
    marginTop: 12,
    fontSize: 13,
    lineHeight: "18px",
    color: WRN_BLUE,
    fontWeight: 900,
}

const netLaneToggle: React.CSSProperties = {
    color: MUTED,
    fontWeight: 900,
    flex: "0 0 auto",
    marginTop: 2,
    fontSize: 13,
    whiteSpace: "nowrap",
}

const netInnerDisclosure: React.CSSProperties = {
    marginTop: 10,
    borderTop: `1px solid ${BORDER_SOFT}`,
    paddingTop: 10,
}

const netInnerSummary: React.CSSProperties = {
    cursor: "pointer",
    color: WRN_BLUE,
    fontWeight: 900,
    fontSize: 13,
    listStyle: "none",
    outline: "none",
}

const netInnerBody: React.CSSProperties = {
    marginTop: 10,
}

/* ---------- JOBFIT V6 ---------- */

// Brand color constants (local to this block)
const JF_NAVY_BASE = "#13294A" // WRN Navy — true brand background
const JF_NAVY_CARD = "#0F1F38" // Cards sit on this — slightly darker
const JF_NAVY_FOOT = "rgba(0,0,0,0.22)" // Why card footer — darkened
const JF_BORDER = "rgba(255,255,255,0.10)"
const JF_BORDER_MID = "rgba(255,255,255,0.14)"
const JF_TEXT_1 = "rgba(255,255,255,0.92)" // Primary body text
const JF_TEXT_2 = "rgba(255,255,255,0.65)" // Secondary / move text
const JF_TEXT_3 = "rgba(255,255,255,0.55)" // Proof text in footer
const JF_TEXT_4 = "rgba(255,255,255,0.30)" // Labels, eyebrows
const JF_TEXT_5 = "rgba(255,255,255,0.18)" // Dimmest labels

const jfBrandBar: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    paddingBottom: 16,
    marginBottom: 16,
    borderBottom: `1px solid ${JF_BORDER}`,
}

const jfLogoMark: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    gap: 3,
}

const jfLogoRow: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
}

const jfLogoText: React.CSSProperties = {
    fontSize: 22,
    fontWeight: 950,
    fontStyle: "italic",
    letterSpacing: -0.5,
    color: "#ffffff",
    lineHeight: 1,
}

const jfLogoDash: React.CSSProperties = {
    width: 14,
    height: 4,
    background: WRN_ORANGE,
    borderRadius: 1,
    marginLeft: 4,
    marginBottom: 1,
    flexShrink: 0,
}

const jfLogoByline: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: 4,
}

const jfLogoBy: React.CSSProperties = {
    fontSize: 8,
    fontWeight: 700,
    color: JF_TEXT_4,
}

const jfLogoWorkforce: React.CSSProperties = {
    fontSize: 8,
    fontWeight: 900,
    letterSpacing: 0.8,
    color: WRN_ORANGE,
    textTransform: "uppercase" as const,
}

const jfLogoReady: React.CSSProperties = {
    fontSize: 8,
    fontWeight: 900,
    letterSpacing: 0.8,
    color: WRN_BLUE,
    textTransform: "uppercase" as const,
}

const jfEngineBadge: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.4,
    textTransform: "uppercase" as const,
    color: JF_TEXT_4,
    padding: "5px 11px",
    border: `1px solid ${JF_BORDER_MID}`,
    borderRadius: 999,
}

const jfCtxBar: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    gap: 12,
    marginBottom: 14,
    padding: "11px 16px",
    borderRadius: 12,
    background: "rgba(255,255,255,0.06)",
    border: `1px solid ${JF_BORDER_MID}`,
    flexWrap: "wrap" as const,
}

const jfCtxLeft: React.CSSProperties = {
    display: "flex",
    alignItems: "baseline",
    flexWrap: "wrap" as const,
    gap: 2,
    minWidth: 0,
}

const jfCtxRole: React.CSSProperties = {
    fontSize: 14,
    fontWeight: 900,
    color: JF_TEXT_1,
}

const jfCtxSep: React.CSSProperties = {
    fontSize: 13,
    color: JF_TEXT_4,
    margin: "0 3px",
}

const jfCtxCo: React.CSSProperties = {
    fontSize: 13,
    color: JF_TEXT_2,
    fontWeight: 700,
}

const jfCtxPills: React.CSSProperties = {
    display: "flex",
    gap: 5,
    flexWrap: "wrap" as const,
    flexShrink: 0,
}

const jfCtxPill: React.CSSProperties = {
    fontSize: 10,
    fontWeight: 900,
    letterSpacing: 0.5,
    textTransform: "uppercase" as const,
    padding: "3px 10px",
    borderRadius: 999,
}

const jfHero: React.CSSProperties = {
    borderRadius: 20,
    border: `1px solid ${JF_BORDER_MID}`,
    background: JF_NAVY_CARD,
    overflow: "hidden",
    marginBottom: 12,
}

const jfHeroStripe: React.CSSProperties = {
    height: 3,
}

const jfHeroInner: React.CSSProperties = {
    padding: "26px 28px 24px",
}

const jfHeroTop: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: 20,
}

const jfHeroEyebrow: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 2,
    textTransform: "uppercase" as const,
    color: JF_TEXT_4,
    marginBottom: 6,
}

const jfHeroWord: React.CSSProperties = {
    fontSize: 58,
    fontWeight: 950,
    letterSpacing: -0.5,
    lineHeight: 0.95,
    textTransform: "uppercase" as const,
}

const jfScoreCol: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    alignItems: "flex-end",
    flexShrink: 0,
}

const jfScoreEyebrow: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.6,
    textTransform: "uppercase" as const,
    color: JF_TEXT_4,
    marginBottom: 3,
}

const jfScoreBig: React.CSSProperties = {
    fontSize: 52,
    fontWeight: 950,
    lineHeight: 1,
}

const jfScoreOf: React.CSSProperties = {
    fontSize: 15,
    fontWeight: 900,
    color: JF_TEXT_4,
    paddingBottom: 4,
}

const jfScoreBarTrack: React.CSSProperties = {
    width: 110,
    height: 3,
    background: "rgba(255,255,255,0.12)",
    borderRadius: 3,
    overflow: "hidden",
    marginTop: 7,
}

const jfScoreBarFill: React.CSSProperties = {
    height: "100%",
    borderRadius: 3,
}

const jfRule: React.CSSProperties = {
    height: 1,
    background: "rgba(255,255,255,0.08)",
    margin: "20px 0 18px",
}

const jfMoveRow: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    gap: 12,
    marginBottom: 18,
}

const jfMoveDot: React.CSSProperties = {
    width: 8,
    height: 8,
    borderRadius: "50%",
    flexShrink: 0,
    marginTop: 5,
}

const jfMoveLabel: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.8,
    textTransform: "uppercase" as const,
    color: JF_TEXT_4,
    marginBottom: 6,
}

const jfMoveText: React.CSSProperties = {
    fontSize: 16,
    fontWeight: 600,
    lineHeight: 1.5,
    color: "rgba(255,255,255,0.92)",
}

const jfCtaGo: React.CSSProperties = {
    width: "100%",
    padding: "15px",
    borderRadius: 13,
    fontWeight: 900,
    fontSize: 15,
    border: "none",
    cursor: "pointer",
    color: "#04060F",
    letterSpacing: 0.1,
}

const jfCtaPass: React.CSSProperties = {
    width: "100%",
    padding: "15px",
    borderRadius: 13,
    background: "rgba(255,255,255,0.06)",
    fontWeight: 900,
    fontSize: 15,
    border: `1px solid ${JF_BORDER_MID}`,
    cursor: "pointer",
    color: JF_TEXT_2,
}

// ─────────────────────────────────────────────────────────────────────────────
// REPLACEMENT STYLE TOKENS — paste over existing jfSec*, jfWhy*, jfRisk*,
// jfStack tokens in the file (around lines 6573–6760)
// ─────────────────────────────────────────────────────────────────────────────

// ── Section wrappers ─────────────────────────────────────────────────────────

const jfWhySection: React.CSSProperties = {
    borderRadius: 18,
    border: "1px solid rgba(74,222,128,0.22)",
    background: "rgba(74,222,128,0.04)",
    overflow: "hidden",
    marginBottom: 12,
}

const jfRiskSection: React.CSSProperties = {
    borderRadius: 18,
    border: "1px solid rgba(254,176,106,0.22)",
    background: "rgba(254,176,106,0.04)",
    overflow: "hidden",
    marginBottom: 12,
}

// ── Section headers ───────────────────────────────────────────────────────────

const jfWhySectionHeader: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "16px 20px 14px",
    borderBottom: "1px solid rgba(74,222,128,0.14)",
    background: "rgba(74,222,128,0.06)",
}

const jfRiskSectionHeader: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "16px 20px 14px",
    borderBottom: "1px solid rgba(254,176,106,0.14)",
    background: "rgba(254,176,106,0.06)",
}

const jfWhySectionTitle: React.CSSProperties = {
    display: "flex",
    alignItems: "center",
    gap: 10,
}

const jfWhySectionIcon: React.CSSProperties = {
    width: 30,
    height: 30,
    borderRadius: 8,
    background: "rgba(74,222,128,0.15)",
    border: "1px solid rgba(74,222,128,0.30)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 14,
    flexShrink: 0,
}

const jfRiskSectionIcon: React.CSSProperties = {
    width: 30,
    height: 30,
    borderRadius: 8,
    background: "rgba(254,176,106,0.15)",
    border: "1px solid rgba(254,176,106,0.30)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    fontSize: 14,
    flexShrink: 0,
}

const jfWhySectionLabel: React.CSSProperties = {
    fontSize: 13,
    fontWeight: 800,
    letterSpacing: 0.2,
    color: "#4ade80",
}

const jfRiskSectionLabel: React.CSSProperties = {
    fontSize: 13,
    fontWeight: 800,
    letterSpacing: 0.2,
    color: "#FEB06A",
}

const jfSectionInstruction: React.CSSProperties = {
    fontSize: 11,
    color: "rgba(255,255,255,0.40)",
    lineHeight: 1.4,
    maxWidth: 260,
    textAlign: "right" as const,
}

const jfSectionCount: React.CSSProperties = {
    fontSize: 10,
    fontWeight: 700,
    padding: "3px 10px",
    borderRadius: 999,
    flexShrink: 0,
}

const jfWhySectionCount: React.CSSProperties = {
    ...jfSectionCount,
    background: "rgba(74,222,128,0.12)",
    color: "#4ade80",
    border: "1px solid rgba(74,222,128,0.25)",
}

const jfRiskSectionCount: React.CSSProperties = {
    ...jfSectionCount,
    background: "rgba(254,176,106,0.12)",
    color: "#FEB06A",
    border: "1px solid rgba(254,176,106,0.25)",
}

// ── Card list ─────────────────────────────────────────────────────────────────

const jfStack: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    gap: 0,
    padding: "10px 16px 14px",
}

// ── Why cards ─────────────────────────────────────────────────────────────────

const jfWhyCard: React.CSSProperties = {
    borderRadius: 13,
    border: "1px solid rgba(74,222,128,0.14)",
    background: "rgba(255,255,255,0.03)",
    overflow: "hidden",
    marginBottom: 8,
}

const jfWhyCardAccent: React.CSSProperties = {
    width: 3,
    borderRadius: "3px 0 0 3px",
    background: "linear-gradient(180deg, #4ade80 0%, #22c55e 100%)",
    flexShrink: 0,
    alignSelf: "stretch",
}

const jfWhyCardInner: React.CSSProperties = {
    display: "flex",
    alignItems: "stretch",
    gap: 0,
}

const jfWhyBody: React.CSSProperties = {
    padding: "14px 16px 10px",
    fontSize: 14,
    lineHeight: "22px",
    color: "rgba(255,255,255,0.92)",
    flex: 1,
}

const jfWhyAction: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "8px 16px 12px",
    borderTop: "1px solid rgba(74,222,128,0.10)",
    background: "rgba(74,222,128,0.04)",
}

const jfWhyActionLabel: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.4,
    textTransform: "uppercase" as const,
    color: "#4ade80",
    flexShrink: 0,
    marginTop: 1,
}

const jfWhyActionText: React.CSSProperties = {
    fontSize: 12,
    lineHeight: "17px",
    color: "rgba(255,255,255,0.55)",
}

const jfWhyFoot: React.CSSProperties = {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    borderTop: "1px solid rgba(74,222,128,0.08)",
    background: "rgba(0,0,0,0.18)",
}

const jfWhyCell: React.CSSProperties = {
    padding: "10px 16px",
}

const jfWfNeedsLabel: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.2,
    textTransform: "uppercase" as const,
    color: "rgba(255,255,255,0.22)",
    marginBottom: 4,
}

const jfWfProofLabel: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.2,
    textTransform: "uppercase" as const,
    marginBottom: 4,
}

const jfWfText: React.CSSProperties = {
    fontSize: 12,
    lineHeight: "18px",
    color: "rgba(255,255,255,0.50)",
}

// ── Risk cards ────────────────────────────────────────────────────────────────

const jfRiskBase: React.CSSProperties = {
    borderRadius: 13,
    overflow: "hidden",
    marginBottom: 8,
}

const jfRiskCardHi: React.CSSProperties = {
    ...jfRiskBase,
    border: "1px solid rgba(248,113,113,0.28)",
    background: "rgba(248,113,113,0.05)",
}

const jfRiskCardMd: React.CSSProperties = {
    ...jfRiskBase,
    border: "1px solid rgba(254,176,106,0.24)",
    background: "rgba(254,176,106,0.05)",
}

const jfRiskCardLo: React.CSSProperties = {
    ...jfRiskBase,
    border: "1px solid rgba(255,255,255,0.09)",
    background: "rgba(255,255,255,0.03)",
}

const jfRiskCardInner: React.CSSProperties = {
    display: "flex",
    alignItems: "stretch",
}

const jfRiskAccentHi: React.CSSProperties = {
    width: 3,
    borderRadius: "3px 0 0 3px",
    background: "linear-gradient(180deg, #f87171 0%, #ef4444 100%)",
    flexShrink: 0,
    alignSelf: "stretch",
}

const jfRiskAccentMd: React.CSSProperties = {
    width: 3,
    borderRadius: "3px 0 0 3px",
    background: "linear-gradient(180deg, #FEB06A 0%, #f97316 100%)",
    flexShrink: 0,
    alignSelf: "stretch",
}

const jfRiskAccentLo: React.CSSProperties = {
    width: 3,
    borderRadius: "3px 0 0 3px",
    background: "rgba(255,255,255,0.12)",
    flexShrink: 0,
    alignSelf: "stretch",
}

const jfRiskContent: React.CSSProperties = {
    flex: 1,
    padding: "13px 16px 11px",
}

const jfRiskTop: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: 10,
    marginBottom: 6,
}

const jfRiskTitle: React.CSSProperties = {
    fontSize: 13,
    fontWeight: 800,
    lineHeight: "19px",
    color: "rgba(255,255,255,0.92)",
}

const jfRiskSevBase: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 0.8,
    textTransform: "uppercase" as const,
    padding: "3px 9px",
    borderRadius: 999,
    flexShrink: 0,
    whiteSpace: "nowrap" as const,
}

const jfRiskSevHi: React.CSSProperties = {
    ...jfRiskSevBase,
    background: "rgba(248,113,113,0.18)",
    color: "#f87171",
}

const jfRiskSevMd: React.CSSProperties = {
    ...jfRiskSevBase,
    background: "rgba(254,176,106,0.18)",
    color: "#FEB06A",
}

const jfRiskSevLo: React.CSSProperties = {
    ...jfRiskSevBase,
    background: "rgba(255,255,255,0.08)",
    color: "rgba(255,255,255,0.40)",
}

const jfRiskAction: React.CSSProperties = {
    display: "flex",
    alignItems: "flex-start",
    gap: 8,
    padding: "9px 16px 11px",
    borderTop: "1px solid rgba(254,176,106,0.10)",
    background: "rgba(254,176,106,0.04)",
}

const jfRiskActionHiTop: React.CSSProperties = {
    ...jfRiskAction,
    borderTop: "1px solid rgba(248,113,113,0.10)",
    background: "rgba(248,113,113,0.04)",
}

const jfRiskActionLabel: React.CSSProperties = {
    fontSize: 9,
    fontWeight: 900,
    letterSpacing: 1.4,
    textTransform: "uppercase" as const,
    color: "#FEB06A",
    flexShrink: 0,
    marginTop: 1,
}

const jfRiskActionLabelHi: React.CSSProperties = {
    ...jfRiskActionLabel,
    color: "#f87171",
}

const jfRiskActionText: React.CSSProperties = {
    fontSize: 12,
    lineHeight: "17px",
    color: "rgba(255,255,255,0.50)",
}

// ── Kept for legacy ───────────────────────────────────────────────────────────
const jfSecHd = { display: "none" }
const jfSecLabel = { display: "none" }
const jfSecLine = { display: "none" }
const jfSecCount = { display: "none" }

const jfRiskBody: React.CSSProperties = {
    fontSize: 13,
    lineHeight: "20px",
    color: JF_TEXT_2,
}

const jfRiskQuote: React.CSSProperties = {
    fontSize: 11,
    marginTop: 8,
    fontStyle: "italic" as const,
    lineHeight: "16px",
    color: JF_TEXT_4,
}

/* ---------- GENERIC ---------- */

const grid: React.CSSProperties = {
    display: "grid",
    gridTemplateColumns: "repeat(auto-fit,minmax(420px,1fr))",
    gap: 16,
}

const card: React.CSSProperties = {
    padding: 14,
    borderRadius: 16,
    background: "rgba(255,255,255,.03)",
    border: "1px solid rgba(255,255,255,0.08)",
    width: "100%",
    color: TEXT,
}

const pill: React.CSSProperties = {
    fontSize: 12,
    fontWeight: 900,
    textTransform: "uppercase",
    letterSpacing: 0.6,
}

/* ---------- UPGRADE PANEL ---------- */

const upgradePanel: React.CSSProperties = {
    padding: 16,
    borderRadius: 16,
    background: CARD,
    border: `1px solid ${BORDER_SOFT}`,
}

const upgradeTitle: React.CSSProperties = {
    fontSize: 16,
    fontWeight: 900,
    color: TEXT,
    marginBottom: 6,
}

const upgradeBody: React.CSSProperties = {
    fontSize: 13,
    lineHeight: "18px",
    color: MUTED,
}

const upgradeBullets: React.CSSProperties = {
    fontSize: 13,
    lineHeight: "18px",
    color: TEXT,
    whiteSpace: "pre-wrap",
}

const upgradeBtn: React.CSSProperties = {
    width: "100%",
    padding: "12px 18px",
    borderRadius: 16,
    background: "linear-gradient(135deg, #FEB06A, #51ADE5)",
    fontWeight: 900,
    border: "none",
    cursor: "pointer",
    color: "#0B1020",
}
